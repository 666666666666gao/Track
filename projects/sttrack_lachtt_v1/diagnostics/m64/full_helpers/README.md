# M64全量VOT及三数据集终验代码：仅完成源码与既有契约检查

2026-09-07 02:59:18 CST在实际STTrack Python 3.8环境完成CPU检查。两份新程序通过语法解析；M64原有底座、接口、文字协议及M58文本权重指纹复核通过，实际checkpoint仍为已完成130条DepthTrack Train拟合序列的版本。没有调用新程序中的全量输入准备、caption、跟踪、指标计算或最终核验入口。

## 准入后执行的代码

- `m64_full_vot_20260907.py`：要求既定M64低22全部准入条件通过。全127初始化与既有303条逐项核对图像、接收坐标及整条帧顺序；剩余初始化使用共享生成器，既有303条文字张量与mask逐元素保留。全量运行只复用M64自身的909个已封存输出文件，补跑其余1,462个anchor，不复用原生STTrack预测。完整manifest绑定元数据与代码指纹；最终使用官方toolkit分析及1,765个失败anchor核查。
- `verify_m64_three_datasets_20260907.py`：等待三数据集均完成后，检查同一个bundle和文本协议，逐文件校验预测收据；从保存的OPE框和置信度重新计算P/R/F，并核对完整VOT官方分析及所有失败记录。数值条件全部通过也不自动宣称项目完成，还需执行者核查、文档同步及发布。

源码核对发现既有P/R/F评测器允许输出空的最优阈值，新校验器因此使用阈值原值一致性比较；同时明确检查预测收据中的文本bank指纹。这是尚未执行的新校验代码修改，不改动共享评测器或运行中的结果。

复用低22结果的前提是相同初始化、文字张量、权重和运行配置。全量由既有及新执行轨迹组成，不据此宣称统一硬件条件下的FPS。全量目标保持DepthTrack P/R/F至少65.2/64.9/65.1、CDTB至少72.9/75.6/74.2、VOT EAO/ACC/ROB严格超过77.9/82.1/93.7。

## 实际元数据与运行状态

02:42:57 CST使用现有完整VOT工作区及toolkit实际初始化接口，统计1,765个初始化区域，全部为Rectangle。该普查没有新模型图像解码、caption、跟踪或指标计算；记录仅说明现有矩形接口覆盖这份实际初始化清单，不能证明全量模型效果。

02:59运行检查：M63已完成完整CC一致性对照，SS仍运行；M64跟踪日志显示61/303个anchor完成。两项尚无完成态结果。此次准备没有创建full_evaluation目录，没有提前启动完整三数据集评测。

磁盘可用2,749,612,032字节。Qwen3_8B保留5个权重分片、16,381,516,776字节；Qwen2.5-VL-3B-Instruct保留2个分片、7,509,337,976字节。本次没有删除模型权重。

本目录是执行者的源码、元数据和checkpoint契约检查，不是独立审阅PASS，不是全量流水线实际运行通过，也不是M64晋升或正式指标。
