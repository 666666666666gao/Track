# M64全量准备程序：源码和元数据检查完成，未启动全量评测

2026-09-07 02:31:22 CST完成CPU检查，exit0。复核了M64同一bundle/文字协议、底座与接口指纹，以及此前原生评测的输入清单和GT文件指纹表。

| 完整评测范围 | 序列 | 图像或计划帧位置 | anchor |
| --- | ---: | ---: | ---: |
| DepthTrack Test | 50 | 76,373 | 单起点 |
| CDTB | 80 | 101,956 | 单起点 |
| VOT-RGBD2022 | 127 | 1,327,004 | 1,765 |

这次只检查已有源码与元数据。没有打开公开图像给模型，没有新caption、训练或跟踪调用，没有全量分数，也没有用未完成的M64低22结果宣称晋升。

程序的prepare_ope、bank_ope和export_vot入口会先读取M64低22完成收据，复核同一bundle/协议、全部准入条件、303个结果与7条成功序列保护。未完成或未通过时不得准备后续模型输入。OPE准备使用此前已封存的首帧框、序列顺序和帧数，后续沿用共享六位小数输出及P/R/F计算；VOT导出直接复用toolkit实际初始化导出程序。

首次检查发现新程序中手工抄录的导出器SHA遗漏一段字符，检查在打开后续数据前中止。已对照服务器实文件与既有GitHub发布清单，确认原导出器未变化，仅纠正新程序的指纹常量。首次失败日志和exit1保留；未重跑或修改任何模型结果。

当前只验证了check_sources入口。其余条件入口尚未执行，不称完整评测流水线已跑通。VOT全量在准入后还须封存完整文字bank和运行manifest，并逐元素保留已有303条初始化文字输入；不能因重新生成或编码改变已经验证的输入。完整三数据集始终绑定同一个M64 bundle，不能按数据集换权重或引用历史SRTrack结果。

02:31:54实际检查：M63完整CC的PID364300仍存活，M64已完成19/303个anchor；可用空间2,762,076,160字节。两个Qwen保留。当前没有M63或M64完成态结果。
