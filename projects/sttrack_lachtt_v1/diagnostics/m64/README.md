# M64：独立冻结类别保留候选，进入VOT低22验证

类别保留是在M59结果之后、重复使用的DepthTrack Train开发22上选定的输入协议。M60提供原类别优于置换类别的固定权重证据，但不追认原M58完整属性方案通过，也不把开发集称为未见测试。

同一个M58文本final已经在130条DepthTrack Train拟合序列上完成186694次track、5798次优化。M64没有新增网络参数或改变架构：保留自动类别词向量，将有效属性槽替换为CLIP空字符串向量，保留原mask、padding及槽结构。22条开发输入与M59类别路径逐元素一致。保持原生模板更新规则；不启用在线caption、候选关联头、M55 Clone或M63交叉控制。

冻结bundle SHA=1273c87110bdd023d256660d8764586223fcc4f7b85667911e4545c2e7d9fa17；组合文字协议SHA=20d3e4ee754822053239e6c7073b7819c8928ce4904620596ef4596e51912846。组合协议明确区分原始初始化描述生成规则和类别保留步骤，三个数据集必须使用相同bundle与协议。

共享生成器先对预选的三张Train初始化图执行真实Qwen2.5-VL-3B GPU生成。原始回复、类别、属性及图像SHA均与训练生成结果完全一致，之后才打开公开低22初始化图。303个实际toolkit初始化全部独立生成，358.07秒完成，无类别提示、后续帧或人工修正；再由冻结CLIP在CPU编码。类别输入bank SHA=332d92cafa874bfc34a4354af0e49969503c50fbfd09e632224c498ece6c8496，303个实际图像/输入框键均通过绑定检查。错误自动类别也保留，未根据序列名称改字。

输入打包首次运行在local checked变量遮蔽同名函数处报UnboundLocalError，input_job.exit=1保留。02:12只将该局部变量改名为checked_input；保存原源码、原spec、两行diff和失败日志，再运行low22_bank_fixed，exit0。此前一次过早准备评测因完成收据尚无而中止，未创建评测目录。原始caption、权重、gate及生成结果均未改；原spec只更新修正后的source SHA，保留原spec副本。后续完成条件逐项检查六个阶段收据，包括修复阶段，不覆盖原失败exit。

02:13:18 CST在空闲GPU1启动screen m64_low22_20260907，四个不重叠anchor shard，poll240秒。完整22序列/303 anchors/220483计划帧位置；无结果预填充。实际入口复用M62验证的共享运行器，六个接口、160份底座源码、训练权重及所有输入指纹绑定。尚无M64完成态指标。

在任何M64跟踪结果出现前已冻结全量准入门：

| 指标 | 原生M39低22 | M64要求 |
| --- | ---: | ---: |
| EAO（%） | 57.135993 | ≥57.635993 |
| ACC（%） | 75.719622 | ≥75.719622 |
| ROB（%） | 73.022401 | ≥73.022401 |
| 确认失败anchor | 124 | ≤118 |
| 原生零失败序列 | 7 | 全部保护 |

必须完整303 anchors、相同bundle/文本协议且全部门通过，才扩展完整DepthTrack Test、CDTB和VOT127。不做阈值扫参、换caption后重算或按序列选模型。低22本身也是重复开发集，不能用来声称未见泛化。该候选独立于M63结果，没有新独立审阅PASS。

两个Qwen均保留，没有新增或删除权重。启动前可用磁盘约2.78GB，采用已有权重路径，没有复制大型权重。当前正式三数据集数字仍为原生STTrack，整体研究目标未完成。
