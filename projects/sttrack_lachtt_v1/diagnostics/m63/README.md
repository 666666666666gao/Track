# M63：定位与模板更新的类别输入2×2完整递归对照

2026-09-07 02:00:14 CST起，GPU0专用screen m63_factorial_20260907运行。相同M58文本权重、空属性、原mask、原生50帧/0.75更新检查，新增训练参数和优化步均0。

| 组 | 定位文字 | 更新打分文字 |
| --- | --- | --- |
| CC | 原自动类别 | 原自动类别 |
| SS | 冻结的置换类别 | 同一置换类别 |
| CS | 原自动类别 | 置换类别 |
| SC | 置换类别 | 原自动类别 |

定位文字决定每帧Hann峰、解码框及报告分数。仅在定时更新检查，另一文字在同一克隆的适配器输入上运行同权重适配器/Center头，读取定位分支已选峰处的Hann分数，判断是否严格大于0.75。写入时裁剪定位框，遵循原生动态模板append/pop；另一头的框与query不会提交。

四组各3条202图像前缀已通过，共2424图像/2412次track。CC和SS与各自封存参考逐帧一致；交叉组在首次更新决策分叉前框/分数一致。SC的bag05在第200帧首次出现写入分叉，记录保留，没有把分叉后的不同误报为保真失败。

02:03:11进入完整CC重放。GPU0依次完成CC、SS，再运行CS、SC；必须先通过CC/SS全部22条逐帧复现才进入完整交叉组。总计132520图像/132432次track预算。四组全部封存后才读取后续GT并计算结果。当前没有完整M63结果。

这是固定训练权重的运行输入通道因果消融。置换类别仅用于诊断，不作为可部署候选。输出包括按帧/按序列IoU、低帧/H10、写入数、首次分叉及四项交叉差值；不能把这轮称为训练了记忆效用头。M64不会根据本轮中途结果改配置。
