

### 5.60 M53历史模板容量完成：支持训练读取器，尚无递归或公开指标增益（2026-09-06）

§5.57—5.58的原始计划、采集和分析绑定不变。完整采集和CPU分析均结束，collection/controller/analysis.exit均为0。63条DepthTrack Train拟合序列、1,511个固定事件、93,362跟踪步、14,349历史模板影子前向，1,131次原生动态更新；采集耗时5,514.94秒。所有主路径bbox和score对封存原生输出误差为0，当前模板重放完全一致，影子分支未修改主状态，采集没有后续GT输入或优化器步骤。完整采集用封存原生输出比较，两序列独立原生实例契约另行完成，二者不混称。

分析先验全部63份事件封存，再读GT。共1,300有效/211无效事件；有效事件为严重错误325、部分重叠213、正确762。下表是固定状态、GT事后选优容量，非在线策略结果：

| 读取集合 | top1正确 / 1300 | top10有正确框 / 1300 | 严重错误恢复 / 325 | 恢复涉及序列 | 单帧top1平均IoU |
| --- | ---: | ---: | ---: | ---: | ---: |
| 当前模板 | 762 | 872 | 0 | 0 | 0.568563654 |
| 当前＋全部过去模板，GT选优 | 851 | 916 | 26 | 12 | 0.608571591 |
| 当前＋写入时GT IoU≥0.5的过去模板，GT选优 | 851 | 916 | 26 | 12 | 0.608379786 |

原先固定的容量门槛是至少10个严重错误事件、至少3条序列，本次通过。26/325=8%为单帧容量，不能写成救回26条失败轨迹。61个事件属于当前top10已有正确候选、换历史视图后top1正确；44个事件属于当前top10无正确候选、历史视图top10新增正确框。44不要求历史top1正确，故不能与61相加解释top1收益。恢复涉及bag04、ball13、bottle02、colacan02、cube04、cup05、cup06、milkbottle、mushroom01、paintbottle、toiletpaper04、toy08对应indoor序列。

每个oracle都保留当前控制，因此其不下降由集合包含关系保证，不是安全性实测。原生正确事件上的6,108次过去模板读取中18次造成IoU从≥0.5降至≤0.1，涉及4个事件；只留下写入时GT正确的模板后，5,731次读取仍有1次这样的伤害。归档共1,194次写入含63次初始化；1,168次GT有效，其中1,004次写入IoU≥0.5。GT写入质量筛选不能部署。

完成态独立只读复算验证63份原始事件、63份GT、189行逐序列表及全部汇总，零不一致。复核为同GPT家族Type-A建议性审计：内部完整性通过，结论范围限定为单帧特权容量；不称跨家族认可。原文发布于diagnostics/m53/EXPERIMENT_AUDIT_COMPLETED.md。

**原筛选后的拟合集探索，单独列明：**当前＋初始模板的GT选优可恢复16个严重事件/7序列，当前＋最近一个旧写入8/4，当前＋初始＋最近一个仍16/7，当前＋初始＋最近三个19/10，全部过去视图26/12；直接按原生置信度最高仅2/1。相应单帧均值依次0.591128569、0.579772688、0.593546796、0.598930187、0.608571591、0.569933897。探索不改变原筛选、不构成新晋升条件。STTrack第一模板已是初始视图，把初始再次放进动态槽改变视图占比，不能称新增视角。因而第一版应先验证小规模读取选择，不能据此宣称大视图库必要；直接最高置信度也没有显示足够效果。

下一步：只针对不同模板产生的视觉证据设计最小读取实验，保留默认模板写入，不同时修改文本、搜索或主干。学习模块在DepthTrack Train训练，再完成22条训练开发序列的真实递归；门槛仍为相对原生mean IoU至少+0.01、严重低帧减少、H10不增、至少3序列改善、原生零H10序列受保护。通过后才进入低22，再以同一base＋模块＋策略验证三个完整数据集。M52两组失败不因M53容量通过而改判；自身状态训练和长期分叉安全仍未解决。

读取预算探索另行独立从原始框及GT复算全部1,300有效事件，各选项、并列处理和摘要均一致。此探索未按GT写入质量过滤历史模板，IoU并列按列表顺序优先当前控制；选中时刻不是唯一有用视图的证明。审计原文EXPERIMENT_AUDIT_READ_BUDGET.md与主结果审计分开保存。

主结果capacity_result.json：3,746,270字节，SHA256 `60e40d6e7a4efe1301f3f4153986f1d03825ab01e33bc0127b7f62f47c0cbe6a`。完整新增表、189行逐序列结果、原始退出码和日志均发布；无M53新权重、无新正式指标。
