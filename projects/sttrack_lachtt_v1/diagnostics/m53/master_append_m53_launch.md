

### 5.57 M53历史模板读取容量：一致性检查通过，固定拟合集采集中（2026-09-06）

M52完整配对结果未通过，因此没有将其权重进入公开评测。下一项按模板方向收敛为：在相同的当前搜索、query和初始模板条件下，过去真实写入过的动态模板是否产生更好的目标候选。先确认读取容量，再考虑训练读取器；不将尺度变化直接转换成新增写入。

**结构与采集协议。** 保持原生STTrack基础权重、两个模板输入、factor-4搜索、固定query窗口和50步/置信度严格大于0.75的默认更新。归档初始化及所有实际原生模板写入。在原63条DepthTrack Train拟合序列的1,511个固定事件上，以同一冻结输入逐一读取严格过去的历史动态模板，初始模板始终不变。当前动态模板重放作为同状态控制。第t帧原生写入只在该帧全部旁路完成后加入归档，从t+1起才可读取。

归档包含所有过去原生写入，故本轮是历史归档容量，不是已验证的小容量多视图记忆。原事件manifest来自此前训练诊断，保持不变，不宣称其筛选是无GT部署触发；本次采集只读图像、允许的初始化框和已封存预测，不读后续GT。旁路输出不提交bbox、模板或query，不包含关联头或语言，也没有优化器步骤。

| M53已完成的运行一致性检查 | 实测 |
|---|---:|
| 拟合序列 / 跟踪步 | 2 / 240 |
| 当前模板同状态重放事件 | 238 |
| 历史模板旁路前向 | 110 |
| 原生模板写入 | 3 |
| 原生框 / 分数最大误差 | 0 / 0 |
| 与独立原生路径的输出、模板、query | 精确一致 |
| 当前模板重放score/size/offset图 | 精确一致 |
| 每次旁路后的原生状态 | 未改变 |

复用M41的query克隆和状态摘要。此处存在实际代码依据：STTrack前向会改写传入query列表，故每个旁路获得独立克隆，避免改变主路径。首次启动因运行tools目录缺少已发布M41辅助脚本而在导入阶段退出；补齐绑定的原字节文件后检查通过，未修改collector/spec，失败日志保留于contract_attempt1.log/.exit。

**实际运行。** 00:08:44 CST，在GPU0空闲仅1MiB时启动screen `sttrack_m53_template_reads`；00:10核实controller125490、Python125491均存活，显存2,447MiB，无终态退出文件。固定原生预测预估主流程93,362步、当前模板重放1,511次、历史旁路14,349次，合计109,222次前向。00:15已完成前4条、5,724步、113事件和436次历史旁路，已完成序列的框/分数误差均0；累计实测316.379秒。初估整批约90分钟，01:40附近结束，在预计完成前几分钟核对，再按180—300秒间隔观察，不因日志间隔重启。

运行根目录 `/root/autodl-tmp/sttrack_m53_template_read_capacity_v1_20260905`；协议SHA256 `d6f753537170f6f0a4d8dcfe3ec3b869af3e8fbb08ffc224a969fc9ab83450f2`；契约SHA256 `41c3cf1a37b1262948665181f0351e410d15b663523ae9a34b8d896afec43c4b`。完整方案、源码、契约、启动绑定和限制见 `diagnostics/m53/` 及 `tools/collect_sttrack_m53.py`。GPT-5.5 xhigh独立上下文建议性审阅未发现明确缺陷；完整采集和GT容量分析尚未完成，整体WARN保留，不称为外部认证。

**完成采集后的判断。** 先核对全部63份封存文件，再打开GT；报告当前模板与所有历史视图的top1/top10容量、健康事件上的有害替代，以及仅使用历史写入IoU≥0.5视图的特权子集。明确区分“当前top10已有正确框，历史模板使其成为top1”和“当前top10没有，历史模板才生成正确候选”。最优模板和历史GT筛选都仅是离线特权诊断。预先固定的10个严重错误事件、至少3条拟合序列的容量门只决定是否值得研究读取器，不是完整跟踪晋升门，也不自动启动训练或公开评测。

当前M53没有新权重或性能指标。后续若增加学习模块，仍须在DepthTrack Train训练、通过完整递归与固定低22，再以相同base＋模块权重＋运行策略验证DepthTrack Test、CDTB和完整VOT。GPU1原生full127继续独立运行，项目最终目标仍未完成。
