# M82/M84已封存开发轨迹P/R/F补充结果

2026-09-21完成CPU补算。范围为反复使用的DepthTrack Train开发22，每组含初始化33130帧。没有重新训练或跟踪，仅按既定六位格式导出原框，未重新生成递归轨迹或改变默认模板规则，没有评测DepthTrack Test、CDTB或VOT。

采用与历史开发PR相同的固定评价源码：VOT有界矩形重叠，100个阈值，先逐序列计算PR曲线再宏平均，在宏平均曲线上报告最佳F点对应的P/R。框与保存的Hann分数导出六位小数，初始化分数为1。无效GT依照该历史评价器处理，不能直接把它解释为图像中已经确认目标不存在。最佳F阈值只作报告统计，不用于部署或改变0.75模板规则。

| 固定权重及输入条件 | P（%） | R（%） | F（%） |
|---|---:|---:|---:|
| M82 Category | 75.976369 | 72.335438 | 74.111213 |
| M82 独立训练Empty | 70.958973 | 66.619752 | 68.720933 |
| M82 Category权重、Empty输入 | 77.953506 | 72.759401 | 75.266950 |
| M82 Category权重、Swapped输入 | 75.606774 | 71.041085 | 73.252857 |
| M84 Category | 75.838584 | 66.740024 | 70.998995 |
| M84 同权重Empty／已审计原生轨迹参照 | 69.655222 | 66.769487 | 68.181834 |
| M84 同权重Swapped | 76.741867 | 68.372314 | 72.315730 |

M84 Empty的P/R/F在1e-10个百分点容差内复现历史原生开发PR。M82 Category高于独立训练Empty，但低于同权重Empty；M84 Category高于Empty但低于Swapped。这是已有轨迹在长期PR统计下的内容条件比较，不建立正确语义的因果贡献，也不改变原先的IoU/H10或晋升结论。

这组PR使用图像边界内的VOT矩形重叠、包含初始化和无效GT的既定统计约定；它与先前排除初始化/无效GT的连续矩形平均IoU不是同一指标。不能用这些开发数值填写官方测试表，不能把最佳F阈值视为新增控制器。

输入、完整7组回执、六位导出、GT、首帧尺寸及图像SHA、重叠/可见性/分数数组和源码已封存。归档345个manifest记录，文件SHA与大小下载后逐项验证。真实退出码为0；新增跟踪调用0、优化0，CUDA屏蔽、CPU线程限制为1。完成态语义审阅另存EXPERIMENT_AUDIT.md/json；原result.json保留计算完成时independent_audit_completed=false，不以事后改写结果文件冒充先前已审阅。

M88仍为独立在运行的实验，本补算没有读取其中间权重或产生M88性能指标。两套Qwen保持不动。
