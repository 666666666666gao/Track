# M82/M84已封存开发轨迹P/R/F补充统计

仅对既有结果补充报告，不训练、不运行跟踪器、不读取M88中间权重、不新增晋升门。

## 范围与固定口径

- 反复使用的DepthTrack Train开发22，含初始化共33130帧。
- M82：Category、独立训练Empty、Category同权重Empty、Category同权重Swapped。
- M84：Category、同权重Empty、同权重Swapped。M84 Empty已完成原生轨迹一致性审计，此次作为原生PR复现检查。
- 使用既有`depthtrack_pr.py`，SHA256为`05879f2e732aed982fbcbebd9756ce063ed0fa945c1f6b0c04092c3e487466cc`。
- 保存的bbox及实际Hann峰值导出六位小数，初始化分数1；不重新标定、归一化或修改分数。
- 使用VOT有界矩形重叠，100阈值，逐序列曲线宏平均后选最佳F。该阈值仅是报告统计，不转为部署或模板更新阈值。
- 初始化和无效GT处理沿用历史PR代码；因此分母不等于排除初始化与无效GT后的28897有效帧，也不从连续IoU反推P/R/F。

## 完整性与资源

计算前冻结源码、七组来源及回执SHA。先核验全部预测，再访问后续GT；保留六位TXT、GT、首帧尺寸与图像SHA、重叠/可见性/分数数组和归档manifest以便复算。数据与原始数组仅存远端和本地临时目录，不上传公开仓库。

复用现有mplt CPU评价环境，屏蔽CUDA，计算线程设为1。预计数分钟，不重启或修改M88。两个Qwen保持不动，不新增大模型副本。

完成后由新上下文同系列审阅者核验数据来源、实际评价路径、计数、阈值曲线和报告，注明same-family/provisional及不能独立复现的环节。最终只补充旧实验的开发指标表，旧晋升结论不变，不解释为正式DepthTrack Test/CDTB/VOT成绩。
