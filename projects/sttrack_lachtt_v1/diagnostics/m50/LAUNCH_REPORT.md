### 5.51 M50尺度事件模板更新递归试验与原生STTrack全127基线补齐（2026-09-05，启动）

M49给出了相邻帧运动、尺度和错误选框的关联证据，但无条件运动排序没有通过包含健康样本的缓存对照。本轮按用户提出的模板适应方向，先隔离一个推理策略改动，同时补齐一直缺失的原生STTrack full127参考。两项工作彼此独立。

**M50架构与唯一改动。** 使用原生STTrack、同一官方checkpoint、两模板、factor4搜索、固定4query。继承每50步/置信度严格大于0.75的原生模板更新。在其他步骤，如果原生置信度同样大于0.75，且当前预测框的 `sqrt(w*h)` 相对最近模板写入框的尺度变化达到1.25倍（放大/缩小对称），则额外写入动态模板。当前帧的框和query仍由原生路径产生，新模板只影响后续帧。1.25是运行前固定的设计值，不扫描、不按本次开发结果调整。

本轮没有神经网络结构改动、新训练参数或优化器步数；这是模板策略实验，不能称为训练出了新权重。M49已表明均匀尺度有裁剪归一化，额外写入是否改善外观适应仍需实测，不能把尺度相关性当作已证实原因。

实现检查已完成：仅根据M44拟合序列的封存预测、不读GT，选定chair01_indoor的早期触发例。关闭额外写入时，120帧的框、置信度、模板张量和RGB/Depth query与原生完全一致，其中包括1次原生模板更新。开启后实际第9帧发生首次额外写入，当前帧及之前的输出精确匹配原生；额外模板逐次验证为当前预测框裁剪，初始化模板保持不变。检查又覆盖开启后的120帧。`contract.json`状态PASS。

| 本轮任务 | 模型/范围 | 运行状态（启动核实） | GPU与进程 | 本次指标 |
|---|---|---|---|---|
| M50尺度事件模板更新 | DepthTrack Train既有22条开发序列，33,130帧 | 18:59:18启动；真实Python进程和显存占用已验证 | GPU0；screen sttrack_m50_scale；controller29906；Python29908 | 尚未完成，不填入估计值 |
| 原生STTrack full127基线 | 127序列，1,765anchors；复用M39原生303条，新增1,462条 | 18:59:18启动；4个VOT worker已占用GPU，初始303条均来自已验SHA的复用结果 | GPU1；screen sttrack_default_full127；controller29909；调度Python29910 | 尚未完成，不填入旧SUTrack指标 |

M50完整递归结束并封存轨迹后读取GT，报告每条序列平均IoU、低重叠帧、H10失败段及额外写入数。冻结门保持：相对原生均值至少+0.01、低重叠帧减少、H10不增加、至少3条改善且原生零失败段序列不新增失败；通过后才评测这个策略的低22。该开发集已多轮使用，不宣称全新未见。若后续改可学习模块，必须先DepthTrack Train训练，再做预测裁剪递归和同模型三数据集验证。

**原生完整基线的范围说明。** M39原生低22相对旧SUTrack已有明确改善，符合用户“低集合明显改善后补完整指标”的授权。补原生full127不是让M48/M49/M50绕过晋升门，也不替代最终训练权重的三数据集目标。准确复制原M39 wrapper与bridge，复用同一checkpoint，未引入实验关联头或文本。通过原冻结full127 anchor清单逐项匹配，确认127序列/1,765anchors、方向和帧数完全相同。303条旧轨迹的909个结果文件分别对照M39 merge SHA核实，补跑量预计1,106,521帧（含每条初始化）。只在全部轨迹、源文件和复用文件验证后，才调用toolkit0.7.1计算新的完整EAO/ACC/ROB。

M50预计约25—40分钟。原生full127根据M39同GPU四worker的实际结果完成时间跨度9,276.9秒及帧量比例，初估需要约13小时，之后按当前吞吐修正。全量控制器每240秒检查进度；不会因观测超时或日志暂未变化重启任务。启动时两卡均只有1MiB占用；验证时GPU0的M50约2.43GiB，GPU1四个原生worker各约2.1—2.43GiB。

产物根目录分别为 `/root/autodl-tmp/sttrack_m50_scale_template_v1_20260905` 和 `/root/autodl-tmp/sttrack_default_full127_v1_20260905`。M50协议SHA256 `3cfd152ebb1e92a76d58f1f9158d7418c4807171ac9e8351c23b57c9b3d4078e`；原生full127协议SHA256 `4fd889243450b42b38c3428ceba504346fb98e210297ce3df1fb8a390f3c19be`；重分片清单SHA256 `8e76256f1c7c135a65a1b262506356769b59557db60eb83bc21ef1392890dd01`；复用receipt SHA256 `92114e567e77bd4c916d3a5823bc02bc953df690f5eb8a028889b4a6aa85647c`。

新运行源码为 `lib/test/tracker/sttrack_scale_template.py`、`tools/check_sttrack_m50.py`、`tools/run_sttrack_m50.py`，既有模型实现未修改。全量准备与结果收集脚本位于公开 `diagnostics/m50/`。当前没有这两项的最终结果；后续应核对实际controller、子进程、终止码与产物，再追加完整结果表。项目最终目标继续保持未完成。
