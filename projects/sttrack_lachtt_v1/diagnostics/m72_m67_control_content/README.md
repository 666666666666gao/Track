# M72：固定M67 Control权重的类别内容诊断已准备并持久排队

2026-09-07，15:09 CST实查。本轮M67 Control有明显整体开发集收益，但它自己的类别内容贡献尚未验证；M69使用M65 Null权重，不能移接为当前Control的词义证据。因此准备M72固定Control内容诊断，并已启动持久等待进程，排在现有队列和M71之后执行。

## 当前实验范围

固定M67 Control最终适配器SHA `7a8907a45cec672f11563ad7cfaee21cb64acfee3fa13e854621c146cb8d058e`，不改网络和权重，不添加监督。仍为289,154参数的类别文本/Null适配器，支持损失权重为0；保留原STTrack底座、两模板、query窗口及默认50步/score>0.75模板代码。

这个权重是查看开发集结果后确定的研究对象。M72明确属于事后选择模型后的固定权重诊断，不能宣称新的无偏测试、多seed泛化或已经有新的语言主创新。原M67 Support的4/11条件和失败结论保持不变，原晋升用内容阶段仍被跳过。

| 内容条件 | 固定权重 | 实际文字干预 | 跟踪产物 |
| --- | --- | --- | --- |
| 原类别 | M67 Control final | 保留自动初始化类别；属性槽仍为空文本向量 | 复用已审计的开发22完整轨迹 |
| 空文本 | 同一Control final | 所有有效词槽均换成同一CLIP空文本向量 | 待完整递归 |
| 替换类别 | 同一Control final | 仅槽0换成M60已冻结的捐赠类别；其他槽不变 | 待完整递归 |

三组均保留5槽、相同mask和padding。空文本向量不是零向量。原自动类别可能错误，替换类别也不保证与目标构成经人工核验的语义冲突，因此对照只检验具体内容敏感性，不能直接证明caption真实性或同类实例区分。

算法、权重和初始化相同；文字干预后，各自的bbox、后续crop、query和模板内容可以随完整递归而不同。本实验测量的是文字对整条递归路径的总影响，包含响应经模板写入及搜索状态传播的作用，不是所有帧强行保持相同状态的单帧干预。

## 已完成的实际检查

在CPU实际加载了Control最终权重，核验架构、完成130序列/186,694调用/5,798优化步、支持损失权重0和全部289,154个有限参数。验证了M67类别缓存与M69原类别缓存逐字节相同，复用现有M69空文本/替换类别向量，不重新生成caption或embedding。

22条序列的替换类别向量全部改变；mask、padding、空向量和非类别槽逐项相同。M67与M69的序列、帧数、初始化框和GT文件哈希契约一致。两份shell脚本通过bash语法检查。

还实际尝试了提前运行eligible：因前序任务未完成、wait.exit不存在而按预期退出1，没有创建prefix、empty、swapped或result目录。这是CPU接续条件检查，不是GPU运行失败，也不是独立模型审阅。

真实GPU入口一致性尚未验证。等前序完成且两张卡空闲后，先重放bag05、cup08、mobilephone02各102帧，共303次跟踪调用，要求原类别bbox与原始分数逐值等于封存Control前缀；通过后才运行两组22序列完整对照，共新增66,216次跟踪调用。

## 计划指标与因果范围

沿用M67的连续矩形IoU和H10口径，初始化与无效GT不计入相应有效评价。两组新预测和原类别完整家族封存、校验后才打开后续GT。报告按帧均值、序列等权均值、低重叠帧、H10、逐序列结果及重建的模板写入数。

描述性比较沿用已有内容实验定义：原类别相对每个对照按帧均值至少+0.001、序列等权不低、低重叠帧和H10不多。这些只作固定内容比较，不是新晋升门；即使全部满足，也不能推翻原M67失败、自动准备低22或启动正式三数据集。

需要特别避免把不同权重的结论混用：M69回答M65 Null的词义作用，M72回答当前Control的词义作用，M68回答M67两组的分数/支持量作用，M70/M71回答M65同状态跨区域容量。后续若采用Control继续研究，仍须结合预先设计的重复验证和同一模型配置的最终三数据集评测。

## 持久运行与当前进度

M72目录：`/root/autodl-tmp/sttrack_m72_m67_control_content_20260907`。screen为`m72_control_content_20260907`；15:09实查等待进程PID477500、start_ticks4578959259，命令行和工作目录一致。

它按240秒间隔等待已登记M71进程PID474465正常结束，并核验M71结果绑定；原M71又等待原M67后诊断队列。顺序为正在运行的M69、随后M68/M70、M71 CPU分析，最后M72。原队列源码和M71接续源码未修改。

开始M72 GPU阶段前要求两张卡显存占用均低于500 MiB、磁盘空闲超过1 GB；错误或前序失败会停止，不重跑训练、不切换权重或放宽比较条件。目前M72只是已核验的等待状态，没有GPU前向、优化或正式结果。

| 15:09实查正在运行的任务 | 完成序列 | 完成跟踪调用 | 状态 |
| --- | ---: | ---: | --- |
| M69空文本 | 13/22 | 20,559 | 原进程存活 |
| M69替换类别 | 13/22 | 20,559 | 原进程存活 |
| M72 | 0 | 0 | 等待M71及其前序完成 |

磁盘空闲约2.215 GB，本次没有删除权重。Qwen3_8B的5个权重分片共16,381,516,776字节、Qwen2.5-VL-3B-Instruct的2个分片共7,509,337,976字节均实际核验保留。

M72源码SHA：`6d1b1cbcaf236590cd5c3bab5c5bb8f5ef5aa45088c8939fbb4ad7ef7b6657a2`。
spec SHA：`cb220985b1b8d2bd910ddf1dc9a7dbd9bb5eae563fd591740fbf1afc952b357b`。
CPU检查结果SHA：`96d3c4ccb66e93a698365749448c517faa92044b8213fc1a69b5a613d573b1a3`。
实际启动核验SHA：`bcbde928f00f4389772a08c7ee1026a992336bc14ab57cdb3c9edc159fe2e55a`。
没有新的独立模型审阅通过，也没有新的正式三数据集指标。
