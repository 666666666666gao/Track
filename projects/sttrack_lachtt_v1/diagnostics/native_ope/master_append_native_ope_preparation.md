
### 5.59 当前STTrack权重的DepthTrack Test/CDTB统一评测入口已准备（2026-09-06）

当前STTrack强基线仍缺少同一权重的DepthTrack Test与CDTB正式结果；两数据集此前成绩属于SRTrack。原生STTrack full127仍是独立运行的参照评测，不能和旧模型成绩拼接。本节补齐统一入口，不晋升M52失败权重，M53模板容量及后续新模块训练主线继续保留。

**结构与输入。**直接使用与M39相同的原生STTrack类、基础权重、YAML、RGB-D处理和默认模板/query策略，不新增网络参数或文本。新增 `tools/run_sttrack_native_ope.py`；按已验证的序列级单次初始化协议运行。输入清单覆盖50条DepthTrack Test和80条CDTB，图像帧数、GT行数、八位一基编号的RGB/Depth配对均已重新核对，与5.36等既有清单一致。准备阶段只将合法首帧框值写入推理清单；后续GT只做行数元数据检查，不进入模型。跟踪阶段不读取GT文件。

**计划与指标口径。**先用Train的chair01/cube04各运行至第120步，对照既有封存原生预测，检查bbox/score误差、实际模板更新和六位小数输出回读误差。两数据集完整预测封存后，复用已验证的 `depthtrack_pr.py`（SHA256 `05879f2e732aed982fbcbebd9756ce063ed0fa945c1f6b0c04092c3e487466cc`），保持bounded VOT重叠、resolution=100的置信度网格、逐序列PR曲线后宏平均及最大F-score。内部递归状态保持未舍入，输出框和原生置信度保留六位小数，初始化置信度为1。所有产物绑定同一基础权重、源代码、配置、输入清单和评测器。

**当前证据。**代码、完整输入清单及准备记录已生成，独立GPT同家族Type-A咨询复核未发现具体实现缺陷；尚未执行该入口的Train接口验证或公开数据推理，不能登记新的PR/F-score。已准备逐数据集控制脚本：成功完成跟踪并通过封存核验后才进入CPU指标分析，失败保留退出码并终止相应路径。

**下一步。**M53释放GPU0后执行短接口验证；通过后按GPU容量运行两套原生参照，保持现有VOT任务。根据M53真实容量结果决定后续模板读取实验。后续任何新训练模块仍需通过Train递归及低22门，最终用同一模型组合验证三数据集；当前原生参照补全不替代这一目标。

证据目录：`projects/sttrack_lachtt_v1/diagnostics/native_ope/`。本节只有入口与清单准备结果，模型指标待真实完整执行后另行登记。
