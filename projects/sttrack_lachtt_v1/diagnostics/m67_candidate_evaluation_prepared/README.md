# M67同权重评测准备：完成态与内容门通过后才能绑定最终模型

2026-09-07 13:01:54 CST完成CPU输入准备，13:03:07完成源码、沿用接口与提前绑定拒绝检查。本次没有加载M67最终head，没有执行新跟踪或公共数据集评测，也没有生成候选bundle。当前M67训练和既有后续队列继续运行。

## 本次完成的工作

新增独立`candidate_evaluation`目录及三个入口：条件式最终bundle绑定器、真实OPE/TraX前缀验证器、完整VOT低22准备与分析器。另有两个尚未启动的shell运行入口。没有修改冻结中的M67训练、模型、原门槛、M67内容对照或持久诊断队列。

绑定器只接受M67 Support在130条拟合序列完整训练后的唯一final.pth，检查support架构、Null开关、Support分组、0.1辅助损失权重、186,694次跟踪调用、5,798优化步、底座SHA和训练规格。实际bundle须同时绑定160份模型源码、五份M67评测接口、底座、最终适配器、训练来源及初始化文本协议。

M58/M64的旧head及旧训练来源不进入这套绑定。置信度采用M67原递归路径的Hann最大值，模板保持每50步且原始分数严格大于0.75的规则。M68单独诊断的支持分数读取没有接入该候选。未来三数据集须使用这同一完整bundle，不能跨模型拼接指标。

## 输入已经真实核验，GPU入口验证仍待运行

| 检查 | 当前结果 |
| --- | --- |
| Train开发22初始化 | 22条首帧图像与框精确路由；tokens、mask、empty与M67开发输入逐值一致 |
| 既有VOT低22初始化 | 303条初始化键、图像SHA和实际wire框全部匹配 |
| VOT文本内容 | 复用已生成的M64类别保留bank；keys、tokens、mask、empty逐值不变，仅绑定新的M67训练协议SHA |
| 五槽语义 | 槽0保留自动类别；有效属性槽仍为CLIP空词向量；槽数、mask及padding不变 |
| 新生成开销 | Qwen调用0，文本编码0，GPU跟踪0，优化0 |
| 真实M67 OPE/TraX前向 | 尚未运行；CPU路由不能替代该项 |

复用的是因果初始化文字，不是旧跟踪结果或旧权重。原自动类别可能错误；本次不根据已知VOT结果修改描述。

## 条件与顺序

1. M67两组训练、完整开发22递归和分析全部正常结束。
2. 完成态审计确认原11项开发门全部通过。
3. 固定Support最终权重的类别、空词及替换类别对照完成，原内容门全部通过。
4. 才能生成唯一最终bundle与开发、低22输入plan。
5. 既有M67后续诊断队列全部完成、两GPU空闲、磁盘大于1GB后，运行真实OPE/TraX入口检查。
6. 入口检查通过，才准备并运行低22全部303个anchor。
7. 低22原冻结门通过，才允许下一阶段同模型完整DepthTrack Test、CDTB和VOT127评测。

本次已直接调用真实绑定器，验证第1项尚未完成时会因缺少实际完成态审计而退出；没有替换门槛、伪造结果或提供假权重。CPU检查器确认bundle、plan、entry_parity和low22_run都未被创建。此处的非零返回是预期的提前执行拒绝，不是M67训练失败。

真实入口验证固定为`bag05_indoor`、`container01_indoor`和`mobilephone02_indoor`前三个202帧片段：OPE共606帧文件，TraX三会话、603次跟踪回报，合计1,206次真实跟踪调用和六次初始化。参照将取M67 Support已封存原递归输出。OPE使用原六位小数误差上界5.01e-7；TraX框严格执行float32→四位小数文本→float32传输比较，置信度容差1e-6。没有因新模型扩大原容差。

源码级核验表明，M62已实际验证过的wire转换、帧通道对象、矩形读取及TraX client，在只替换参照目录后AST一致；低22指标和晋升计算与M64原函数一致。三份Python编译、两个bash语法检查通过。这些只证明准备代码和已有接口的关系，不证明新head已经通过真实前向。

## 低22门沿用原值，不事后放宽

| 条件 | 阈值 |
| --- | ---: |
| EAO | ≥57.635993 |
| ACC | ≥75.719622 |
| ROB | ≥73.022401 |
| 确认失败anchor | ≤118 |

同时保护原生七条零失败序列、要求全部303个anchor完整、保持同一bundle和文本协议。对应原生低22为57.135993 / 75.719622 / 73.022401、124次失败。当前只是将此前已声明的明显改善要求原样绑定到新候选，未取得新的低22结果。Train开发22与VOT低22都属于反复使用的开发证据。

## 当前运行状态与磁盘

13:04:50 CST实查：Control完成90/130条、127,410次调用、3,974优化步；Support完成88/130条、124,096次调用、3,869优化步。原训练PID465923和465924存活。持久后续队列PID471187身份、cwd和起始ticks一致，每240秒等待原M67控制进程，尚未进入后续GPU诊断。

剩余磁盘约2.25GB。Qwen3_8B的五份权重共16,381,516,776字节，Qwen2.5-VL-3B-Instruct的两份权重共7,509,337,976字节，均核验保留；本次没有删除权重。新输入bank总量很小，没有复制底座或Qwen权重。

运行目录：`/root/autodl-tmp/sttrack_m67_supervised_semantic_support_20260907/candidate_evaluation`。后续只有条件满足且已有队列结束后才运行`/root/autodl-tmp/m67_candidate_entry_20260907.sh`；该入口通过后，才运行`/root/autodl-tmp/m67_candidate_low22_20260907.sh`。两者均已准备但未启动，也没有接入正在运行的持久队列。

准备spec SHA：`72b2d4140af7b360dfbcd4449a0a16ad0e59c5d84d4b90ea0e964286cd3fa947`；输入检查SHA：`1e7045c638fb8d6490956aa1909cc47a1143592599c985cda87b8a7ff61cb57c`；源码检查SHA：`2b1eec727eea5e416306183124634069b8b836ccc118f74d11a5ed63948e028a`。没有新增独立Astra/max审阅通过，没有新正式三数据集指标，原总体目标未完成。
