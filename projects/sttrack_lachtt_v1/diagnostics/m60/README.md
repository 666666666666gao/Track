# M60 属性输入固定为空时的类别词置换

M59已完成：保留类别、清空属性的同权重配置取得开发22平均IoU 0.753212、4063低帧和64个H10，原生零H10三条也保住。但M59异类条件同时改变类别和属性，不能分清类别词是否提供了有效信息。

M60只新增一个完整22序列对照：从M59类别保留银行出发，仅把第0个类别词向量替换成先前冻结异类捐赠者的类别向量；所有属性词向量、padding、mask、初始RGB-D、STTrack源、最终权重、query和默认模板策略逐项保持。不是把一个不同训练头当作内容对照。

| 条件 | 序列/图像帧 | 本次执行 |
| --- | ---: | --- |
| 原自动类别，属性为空 | 22/33130 | 复用M59完整结果 |
| 替换自动类别，属性仍为空 | 22/33130 | 新增33108 track调用 |
| 全有效词槽为空 | 22/33130 | 复用M59同权重empty |

准备时验证22个类别槽全部发生数值变化，其他token及mask完全相同；22个实际初始化RGB/框索引均准确返回对应词向量。原始mask仍泄露caption槽数，因此这里是词义内容隔离，不是改成一个词槽的新架构。原自动类别不是人工语义真值，不能把“原词”称作始终正确的类别。

同一M58文本final SHA仍为0f2303ae1f9c12b4a25e6e6531b1fe39fa79578938aa9bc8c517e10bec06b629。M60新增权重/网络参数/优化步/caption生成均0。源码SHA=f8c1f789a7f927121e9f50cefc1d2995774d6bd9bf73d047ccb11e7ea920303b，spec SHA=567944c9dbe2e79340bfabc737a0975563fa7e5be17d16a8b2fc3d75fd1990c5，bank SHA=90ea3f11ba34470a15ed5078177dcd4cf41d47a564c22a4f5f3d94815ecc7986。

先按原类别银行重放bag05/cup08/mobilephone02各102帧，与M59类别保留轨迹比较，bbox与score最大误差均0，19.53秒，exit0。这些前缀均0次模板写入，不扩大解释成更新分支、全部内部状态或整条轨迹复现，也不是TraX验收。

23:45:41 CST在空闲GPU0以专用screen m60_category_isolation_20260906启动，前缀通过后进入完整对照。23:47:40实际Python PID358782存活，首条bag05的889帧已完成并写入SHA，GPU1空闲。预计次日00:16左右完成，接近ETA再检查；22条完整封存后才计算后续GT指标。

原类别减去替换类别的平均IoU是否达到0.001，仅作为本次预定的内容诊断margin，全部结果及损害都会报告，不自动晋升到VOT低22或全量。类别保留是在M59结果之后选择，仍是重复使用的Train开发集。结果决定是否值得继续训练类别/属性交互，或另行冻结候选输入协议的验证方案；修改网络模块和可学习参数仍需重新进行DepthTrack Train训练。

CPU准备初次因共用RGB-D读取器未加载父源码目录而退出；只增加与原运行器一致的父代码路径后完成，未改变网络文件、词向量干预或任何已跑结果。初次源/log/exit保留，没有GPU调用或训练中断。

当前没有M60完整指标，没有新独立Astra/max审阅PASS，也没有新的正式三数据集分数。两个Qwen继续保留，本次新建的是小型词向量银行，未删除或复制模型权重。

- [省略真实初始化框的方案摘要](spec_summary.json)
- [22个初始化输入核对](preparation_result.json)
- [实际前缀复现](parity_result.json)
- [发起记录](launch.json) · [实际进程/首条轨迹](first_live_status.json)
- [实际程序](source/category_isolation.py)
- [M59类别保留的事后原生保护检查](m59_category_posthoc_comparison.json)
