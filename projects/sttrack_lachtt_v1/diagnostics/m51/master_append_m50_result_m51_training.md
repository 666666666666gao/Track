

### 5.52 M50尺度模板策略完整负结果；M51目标相对几何完成训练并进入递归（2026-09-05）

**M50已经完整运行，未通过性能门。** 跟踪33,130帧（包含22条初始化帧），用时1,689.44秒；全部22份轨迹封存成功，跟踪退出码0。保持5.51中预先冻结的策略和代码，没有事后改变1.25尺度阈值。

| 本次新增Train开发结果 | M50 | 相对实际原生STTrack变化 |
|---|---:|---:|
| 有效GT帧 | 28,897 | 0 |
| 平均IoU | 0.65257015894436 | +0.0003438957681162 |
| 严重低重叠帧，IoU≤0.1 | 7,204 | −193 |
| H10连续失败段 | 87 | +12 |
| 平均IoU提高的序列 | 7 / 22 | — |
| 原生零失败段被破坏的序列 | mobilephone02_indoor | 新增1条 |
| 实际原生模板写入 / 尺度额外写入 | 335 / 520 | — |

均值只增加0.03439个百分点，H10段数增加；该规则不能晋升到公开低22。ball07平均IoU增加36.44个百分点、低帧减少873；book06增加15.34个百分点，colacan01增加20.11个百分点。另一面，glass03下降58.81个百分点，mobilephone02下降28.53个百分点，notebook02下降24.65个百分点并新增9个失败段。逐序列的全部22条和增减量保存在 `diagnostics/m50/per_sequence.csv` 与 `RESULT_REPORT.md`。不能只拿球序列的明显改善代表整体收益。

这是已反复使用的DepthTrack Train开发单起点结果，不是VOT低22的ROB，也没有新增EAO/ACC/ROB。低重叠帧减少和失败段增加同时发生，严格按既定代理门判定未通过；不宣称已经测得VOT下降。

**额外模板到底有没有更新到目标？** 对全部520次追加写入进一步作只读统计：497次当前GT有效，其中478次IoU≥0.5，16次IoU≤0.1，3次居中；23次GT不可用。16次低重叠写入中9次发生前已连续低重叠至少10帧。当前和参考写入时GT均有效的491次中，真实线性尺度变化中位数1.30248，354次实际达到1.25倍，只有11次小于1.1倍。因此不能把本轮负结果简单解释为大量虚假的尺度触发或全是错误实例模板；多数写入覆盖正确目标，也确有尺度变化。正确定位与尺度变化仍不足以保证新模板对之后轨迹有益。该检查是全部写入的事后描述，不是每次写入的反事实因果实验，没有据此扫描阈值。

**汇总修复，没有重复跟踪。** 初次分析错误地把M44 `inference_inputs.expected_rows`当成完整基线；该字段每条比完整帧数少4。原始 `analysis.exit=1`、`controller.exit=1`及错误日志保留。独立 `finalize_m50.py` 改用原M44协议早已绑定SHA的两份完整baseline trace，重新计算原生每项指标并与M44完整报告逐项一致。结果阶段再次核验22份预测SHA、全部帧序号与初始化、正有限框、每次模板写入以及第一次额外写入当帧之前的原生框/分数精确一致。`analysis_r2.exit=0`。原协议、运行源码和预测没有变，不能把报告修复称为一次新的实验。

M50结果SHA256 `7fbdeee80209ac6ea7faf6aa957e80e7be2d366164c969137d6287a8becd2383`；轨迹receipt SHA256 `a7c9b5d36ffd94e7f06d5fac69a46e2b731b261987123c4cb5cff349a6919269`。默认更新继续保留，M50固定额外更新版本停止。

**M51架构与单变量。** 现场核对现有候选观测，4维geometry实际上为图像归一化的绝对 `(cx/W, cy/H, w/W, h/H)`；不能把它误称为已按目标大小表达位移。M51以真实前一选择 `previous_choice` 对应的候选框为参考，把当前和前一帧20个候选统一表达成 `((cx-cx_prev)/w_prev, (cy-cy_prev)/h_prev, log(w/w_prev), log(h/h_prev))`。保持原有局部RGB-D token、M45目标优先标签、损失、响应分数和关联头参数量448,739。没有新增距离门、固定速度外推、候选删除或模态；这一轮是一阶位置/尺度表示，不宣称实现了加速度或三帧运动建模。

按照用户“改模块后先DepthTrack训练”的要求，重新从配对初始化训练关联头，官方STTrack主干保持冻结。拟合63条/1,511事件，开发22条/590事件；fresh seed2026、20epochs、960steps、固定末轮。训练和评估计时32.88秒，不含开始的数据读取。与M45全部2,101事件的当前/上一目标标签、每个参数初始SHA、样本顺序SHA及步数完全一致；因此改动的解释限于这一几何表示。

| M51新增静态缓存诊断 | 拟合 | 开发 |
|---|---:|---:|
| 候选选择变化 | 106 | 17 |
| 严重错误→正确 | 29 | 6 |
| 正确→严重错误 | 1 | 2 |
| IoU≥0.5候选数量相对原生净增 | +98 | +7 |

这些是缓存选择，不是轨迹救回。沿用的训练静态统计对GT不可用事件赋0，不能与M49有效GT均值混比，也不据此提前晋升。

训练权重重新加载后logits逐位相同；真实tracker封装与训练forward在previous-choice0至9的logits及affinity逐位相同。CPU审计覆盖全部85个原特征文件，尺寸为正、输出有限，公共平移和缩放不改变相对表示，数值误差小于1e-10。初始化head在chair01拟合序列120帧保持原生框、置信度、query和模板逐位一致，其中有1次原生模板写入。

GPU0在M50实际结束且显存仅1MiB时启动M51。19:36:07创建screen `sttrack_m51_relative`，controller45877；训练和runtime_contract退出码均0；19:38核实完整递归shard0真实进程46661。控制器顺序完成两个16,567/16,563帧分片，之后统一封存/分析，不自动启动公开评测。当前完整递归仍在运行，不能填写最终均值或成功结论。GPU1独立原生full127继续运行，不属于M50/M51晋升。

本轮源码为 `lib/models/sttrack/lachtt_relative_geometry.py`、`lib/test/tracker/sttrack_relative_candidate_set.py`、`tools/train_sttrack_m51.py`、`tools/run_sttrack_m51.py`；既有模型/运行文件不变。训练前修正了新trainer中source hash变量覆盖样本顺序digest的报告问题，原/新协议SHA保存在 `pre_execution_amendment.json`；没有训练后修改配置。新协议SHA256 `13ef61e8ca7ab07c401f503f201e6f29767763f97e62b6e26ee63f2d33675851`；新关联头 `/root/autodl-tmp/sttrack_m51_relative_geometry_v1_20260905/geometry_final.pth`，1,811,499bytes，SHA256 `7eb1cc4e9d1e4c1c63e18429febf6999630e644763b4496686751dfee29fbf85`。

下一步必须依据完整递归结果，再按原生均值至少+0.01、低帧减少、H10不增加、至少3条改善、原生零失败保护判定。通过后才固定低22；进一步明显改善后才用同一训练模型组合验证DepthTrack Test、CDTB与完整VOT。M51不叠加M50模板策略、M48门或文本。项目完整目标仍未完成。
