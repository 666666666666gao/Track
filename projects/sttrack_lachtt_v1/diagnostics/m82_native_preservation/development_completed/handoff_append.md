

## 5.171 M82配对训练与四组开发评测完成：聚合指标改善，冻结内容条件未通过

2026-09-20重新连接服务器核实：M82两组训练、四组完整递归、分析、controller及独立标量核验的退出码均为0。独立标量核验实际完成于2026-09-09北京时间07:43:19；本节是随后补充的完成态记录，覆盖§5.170及冻结前EXPERIMENT_TRACKER.md的历史“运行中/等待”快照。没有重启已完成训练。冻结源码、结果与两组最终权重的SHA重新核验一致，当前远程证据保存在current_verification_20260920.json。

M82仍使用§5.169的首帧完整正向、Raw竞争、可靠同状态原生空间KL训练；只训练289154个adapter参数。两组均完成130条拟合序列、186694次跟踪、5798次优化，固定seed2027、相同初始适配器和数据顺序，底座参数及buffer未变。Category保持项启用150053帧，累计KL为3736.785264；Empty启用149792帧，累计KL为3691.065196。两条训练轨迹各自由自己的预测推进，不能把启用次数差异当成固定输入上的因果比较。

以下为反复使用的DepthTrack Train开发22；每组22条完整轨迹、33130个位置，排除初始化及无效GT后28897个有效位置。四组全部封存后计算指标；独立标量审计复算了88条轨迹、132520个位置及冻结条件。这些不是DepthTrack Test、CDTB或VOT成绩，H10也不是VOT ROB。

| 条件 | 按有效帧平均IoU | 序列等权平均IoU | IoU≤0.1帧 | H10段 |
| --- | ---: | ---: | ---: | ---: |
| 原生STTrack | 0.652226 | 0.684336 | 7397 | 75 |
| M78 Category，匹配的无保持项参照 | 0.719009 | 0.701514 | 5223 | 73 |
| M78独立Empty训练 | 0.634547 | 0.651447 | 8125 | 83 |
| M82 Category | 0.729521 | 0.744777 | 4952 | 67 |
| M82独立Empty训练 | 0.685318 | 0.690941 | 6414 | 80 |
| M82同Category权重Empty内容 | 0.736695 | 0.751616 | 4694 | 66 |
| M82同Category权重Swapped内容 | 0.707575 | 0.735480 | 5744 | 67 |

冻结主条件10/10通过，Category相对匹配M78的保持增量4/4通过；独立Empty相对M78 Empty的四项描述性比较也全部改善。同权重内容条件只有4/8通过：原类别相对Swapped四项通过（H10相等也满足原非退化规则），相对Empty四项全部未通过。不得将条件改为只比较Swapped，不事后晋升Empty内容为最终语言模型。

Category相对M78按帧提高1.051231个百分点、宏平均提高4.326354个百分点，低重叠减少271帧、H10减少6段；相对原生按帧提高7.729463个百分点、低重叠减少2445帧、H10减少8段。这支持本轮可靠同状态空间保持训练的聚合增量，但不证明每条原生正确轨迹逐帧不受损，也不证明跨数据集泛化已改善。原生与本轮独立Empty的零H10序列保护通过；历史M65 Control的glass03零H10优势未完全保持，只作原定描述，不累加新门。

训练条件与内容干预应分别解释：Category相对独立Empty训练的按帧增量为4.420310个百分点；固定Category权重后，原词相对Empty内容反而低0.717390个百分点、宏平均低0.683904个百分点，多258个低重叠帧、1段H10。不能将两种对照直接相减进行精确贡献拆分。Empty仍经过训练后的adapter，不是移除adapter或回到独立原生历史。

内容收益分布存在集中性。原类别相对Swapped按帧提高2.194584个百分点，但移除cup13后增量转为-0.126302个百分点；cup13原词IoU为0.799618/H10=2，Swapped为0.396378/H10=7。原类别相对Empty总体负增量，在移除colacan01后转为+1.561829个百分点。所有22项移除单序列比较已由封存统计重新计算，完整数值随结果公开，不据此删除序列或改变评价范围。

代表性正负结果：glass03原生0.757594/H10=3，M82 Category为0.887392/H10=1；bag05为0.846122/H10=0，cup08为0.888403/H10=0；mobilephone02保持0.843326/H10=0。与此同时，colacan04 Category仅0.250918/H10=12，同权重Empty为0.604439/H10=5；colacan01 Category为0.266137/H10=10，Empty为0.485658/H10=10。book06则原词0.808323、Empty0.651270，均H10=3。既不能称语言普遍无用，也不能把聚合提升直接写成稳定词义收益；这些不同历史轨迹尚未给出逐帧同状态因果归因。

按实际冻结运行规则frame_id%50==0且Hann最高响应严格大于0.75，从已封存score重建模板写入：Category260、独立Empty282、同权重Empty270、Swapped275次。这里只重建执行次数，不是独立分支中的写入效用；相同归一化空间KL不约束响应绝对尺度，因此不能声称已经消除语言与模板控制耦合。不得仅根据次数调低阈值。

完成态决定：保留M82作为有实测训练增量的研究权重，但本轮内容条件失败，不自动进入正式三数据集评测，不以固定权重空词的较好结果替换主条件。完整功能保持主张仍未满足预先要求的主条件、内容条件和Category增量同时通过。下一项最小工作是同状态只读内容诊断：在原Category递归状态上比较Category、Empty、Swapped及未适配头，先验证封存轨迹复现，再测量固定Category历史下的当前内容干预；这不能精确分解完整递归差值中的历史贡献。诊断不训练新权重、不提交反事实状态、不修改Hann或模板规则；GT仅在输出封存后用于统计。该计划不预设文字错误、模板污染或KL冲突为根因，也不宣称已完成。

2026-09-20接续时远程可用空间约1.40GB，GPU无训练进程。本轮补充审计使用小型结果与源码，不复制底座或Qwen权重；继续保留两个Qwen，不增加seed。正式三数据集仍以已封存原生STTrack结果为参照，同一最终完整语言模型的项目目标尚未完成。

固定结果SHA256：`823d0560db3acfdd594c53ecfae239ebb59ef77b3b2776bc9037dcdaa3a259ea`；Category最终权重：`581a044bbba8514fb5f26d283a9dd038f46e27f8608c08724228eebfff47f859`；Empty最终权重：`9c94d2776f7898699b6a7af8e107e8c74f41fa7f411f41f20882a231ade67515`。完整结果、训练receipt、四组递归receipt、逐序列CSV、冻结条件CSV、标量审计与后验描述性分析发布于diagnostics/m82_native_preservation/development_completed/。独立模型审阅另有记录，不能把标量复算写成模型审阅通过。
