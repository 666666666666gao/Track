# M67完成态复核与同权重文字对照准备完成

2026-09-07 11:04:39 CST完成源码、输入和条件队列检查。M67双卡正式训练仍按上一节的冻结方案运行；本节工具未修改训练、推理、权重或性能门，也没有启动新的GPU作业。

## 完成态复核器

`audit_m67_completed_20260907.py`的reference模式已于10:51:25退出0：读取M65 Control、Null及独立原生共三组、每组22条封存轨迹，先校验完整文件，再按M63标量IoU方法读取GT复算。按帧均值、序列等权均值、低重叠帧和H10与原报告一致。这只验证读取与指标计算部分，不能作为M67已经完成的证据。

completed模式尚未运行。它要求M67训练、开发递归、分析及控制器全部正常结束，检查两组130条顺序、186,694次调用、GT有效性与梯度累积对应的优化预算，核对最终权重、源文件、原生底座冻结收据和全部开发轨迹。两组必须都是Null结构，支持损失权重分别为0和0.1；Support有效GT样本须有有限支持损失及正负位置统计，Control和无效GT样本不能出现该监督记录。

审计重算原11项门槛，包括原生、本轮Control及历史M65 Control成功集合的保护，glass03仍在保护名单中。它同时列出Support持续H10而参考路径逐帧正确的区间。与历史M65 Null的差异只作重复训练诊断，不要求独立CUDA优化逐比特重复，也不把历史轨迹替代本轮Control。

源码SHA为`1773887deb3be27aa11a4409aa77bd7581f32b8d891ecab2a03454d277e769bf`；历史参照收据SHA为`82832f5fdf846b5ab0a591aa730108cd8e156cfd7951d49171db4bf07308dfcd`。

## 条件文字内容对照

仅当上述完成态审计和全部原冻结开发门通过，才能加载M67 Support的固定final权重运行本对照。它比较原自动类别、空文字、M60预先冻结的替换类别，保持同一底座、head、5槽、mask、非类别槽、初始化和默认状态更新。22条替换只改变类别槽0，不新生成caption或编码向量。

先对bag05、cup08、mobilephone02各102帧检查原类别与M67 Support封存公共输出完全一致，再分别完整运行空词及替换类别，共新增66,216次跟踪调用；原类别复用已有完整预测。GT仅在全部预测封存后用于分析。

原类别按帧均值须分别比两种内容对照至少高0.001，序列等权均值不低、低重叠帧和H10不多。该门检验固定模型是否依赖具体类别内容；自动类别可能错误，替换类别也不必然与GT矛盾，因此不能据此宣称真实属性可见性或同类细粒度身份消歧。

内容方案于10:52:49、M67开发输出产生前冻结。源码SHA为`40b765b22c398bc18ed4ac3f5ef33edc7b9611cf1c8d3172062f518d435f8285`，spec SHA为`c107a0b08a6825803f6a57f0c7ab5f469e06288c3df203dfbb5ce880a4f6c400`。脚本check及bash队列语法检查通过；prefix、empty、swapped和analyze均未执行。

训练开发门与内容门同时通过后，也只允许准备绑定新权重、文本协议和运行配置的低22候选；不会直接启动完整三数据集。M65、M64的历史失败结论保持不变。当前没有M67完成态指标，也没有新的独立模型审阅通过；项目目标仍未完成。
