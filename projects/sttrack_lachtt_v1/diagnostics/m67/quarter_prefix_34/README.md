# M67前34条训练序列只读机制检查：监督后的空间分离开始出现

2026-09-07 11:42:38 CST完成，退出0。本项固定两组在11:36均已完成的前34条拟合序列，再读取既有训练记录；没有按支持量、loss或追踪收益挑选序列。两组各完成48,421次跟踪、1,512步优化，约为总训练预算的四分之一。只读取首个跟踪帧、每50帧及序列最后帧的既有采样日志，每组1,019条，不运行新模型或读取新的GT文件。

## 已观察到的空间支持

Support组959条采样具有有效GT，其中882条目标中心位于crop内、77条在外；另外60条GT无效，不用于支持监督统计。

| 量 | 结果 |
| --- | ---: |
| GT中心位置Null质量，中位数，882条 | 0.023815 |
| 明确框外区域的平均Null质量，中位数，959条 | 0.873564 |
| 同帧框外均值减GT中心值，中位数，882对 | 0.812833 |
| 框外Null均值高于GT中心的比例 | 97.5057% |
| 支持损失中位数，959条 | 0.120314 |

按事先固定的优化步区间统计：

| 步数区间（左闭右开） | 采样数 | 中心Null中位数 | 框外Null中位数 | 配对框外高于中心 |
| --- | ---: | ---: | ---: | ---: |
| [0,250) | 165 | 0.144532 | 0.513021 | 89.5522% |
| [250,500) | 165 | 0.026102 | 0.848660 | 100.0000% |
| [500,1000) | 341 | 0.007829 | 0.907122 | 99.2857% |
| [1000,1512) | 348 | 0.022920 | 0.911725 | 98.0892% |

这支持一个有限但有用的判断：加入明确的框级支持监督后，现有训练日志中的内部量出现了目标中心与框外的空间分离。训练时的该量不再普遍维持接近零。但是部分序列仍明显较差，例如最早的cube04配对比例仅68%；GT中心Null最大值接近0.978。不能宣称所有位置都已学对。

## 不能据此声称的结论

这些采样来自不断变化的训练权重和不同序列，不是固定最终权重的评测。区间之间也同时改变了序列与递归状态，不能把所有差异单独归因于训练步数。GT中心值不是模型实际选中峰的值；本项尚未证明正确峰与错误峰可分，更没有证明文字真假判断、同类实例消歧、局部存在性校准或跨序列泛化。M66使用的是另一套权重和事后窗口，不能把两次统计直接相减作为配对性能收益。

两组采样覆盖全部50帧更新检查，可重建Control写入405次、Support483次；由于预测状态已经不同，这个计数不是只改变一次写入的因果对照。原模板代码没有改。

本项核验了记录与采样快照SHA、源文件完整性、有限loss/梯度，以及Control和无效GT没有附加支持监督。没有选择checkpoint、修改监督系数、训练预算或原晋升门。双卡正式训练继续，尚无M67完整开发结果及新三数据集成绩。

结果SHA：`f7df7098467d8601c9dd250f3f967849d72216753f9fbbcf3f0390a42bd89c40`。原始采样快照保留在服务器，公开包只含来源哈希、分析源码、规格与聚合/逐序列统计。没有新增独立模型审阅通过。
