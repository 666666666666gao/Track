

### 5.179 M87仅目标裁剪文字协议：生成、编码与前检完成，单seed训练启动（2026-09-21）

M85/M86之后，固定新输入协议，只用原152个t0协议框的相同整数RGB紧裁剪生成短类别，取消整帧输入并明确类别输出。模型仍为已保留的Qwen2.5-VL-3B，greedy、最多48输出token，不提供序列名、旧文字、M86判断、后续图像或性能。该实验同时改变图像上下文与生成指令，是文字协议对照，不声称单独隔离移除整帧或形成新网络贡献。新文字仍为自动输出，不作为已核验语义真值。

首次调用返回带明确Markdown json围栏的JSON，严格parser停止，0条解析记录、0编码、0训练。原source/plan/spec/log及raw封存；经独立代码复审，只允许剥离该完整围栏，prompt/图像/类别不改。首条原回复以SHA绑定直接复用，不重新调用生成器；随后151次生成完成，累计实际Qwen调用152。格式修订在所有跟踪训练与开发评测前完成，没有按性能改词或补默认词。

152条生成与bank准备真实退出0。相对旧M84类别向量，拟合改变73/130、开发改变16/22；共94种新类别字符串，其中44种复用原CLIP bank的相同字符串向量，50种按原冻结ViT-L/14流程CPU编码。只替换槽0，五槽mask、其他有效空属性、padding及Empty向量逐值保持；Empty直接复用旧bank文件。Swapped按冻结序列顺序取首个不同字符串，不能称已核验冲突描述。

准备入口曾因Python3.8相对脚本路径令R为点目录，在trainer路径精确比较处停止；首轮bank及部分复制产物已封存。改用同一源码的绝对入口重新准备，无caption重生成、无训练损失或协议变更。随后实际spec与父M84逐字段核验。

训练沿用M84差分结构F+D(x,q)-D(x,empty)、原零初始化checkpoint、全部主干及loss代码、130条拟合顺序、186694次跟踪调用与5798次优化；seed2027，不加多seed或纯Empty训练臂。不改Hann、模板阈值、记忆、恢复分支。代码审查为gpt-6-astra/max请求路由、same-family/provisional，不声称服务端独立模型遥测。前检的两组101帧原生一致性和96帧3优化检查通过，有限梯度/冻结主干/训练后Empty恒等成立，前检权重丢弃。

训练完成后控制器运行同权重新Category、Empty、旧Category、Swapped四组完整开发22，全部封存才计算GT指标。冻结18项条件：M84旧协议增量4、原生聚合及零H10保护5、同权重新词对旧词4、对Swapped4、Empty原生一致1；M82仅历史参考。失败时保留结果，不自动进入正式三数据集评测。

本节是启动与前检证据：控制器PID 25357，训练PID 25358；启动健康检查已完成1条拟合序列，没有最终训练或开发性能。预计训练4–5小时、四组开发约1小时，按实际吞吐更新，固定最终完整pass权重。两个Qwen保留，不下载新模型。正式三数据集指标及M84 8/14未晋升结论不变。

文字records SHA256 `8141f3746b6ddec25beab547fe2bbb7d242b5bb617da830f971af530678208f2`；训练spec `d14946d36262ee9c23153af8927bce7f3d07aa0383ae2f1c2c02933662448860`；冻结回执 `62dcab32fdca9baa34776ec7843f4fb2ed479f0bcf0b6b6a8a87b7cbc0663f0c`。源码、协议、审查及运行回执发布于`projects/sttrack_lachtt_v1/diagnostics/m87_crop_only_text/started/`；带初始化坐标的完整spec与bank保留于私有证据包，公开binding仅为脱除坐标的可读视图。
