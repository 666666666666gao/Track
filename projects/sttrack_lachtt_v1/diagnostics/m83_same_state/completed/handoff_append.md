

## 5.173 M83同状态诊断完成、独立复核及下一项参数化前检（2026-09-20）

# M83：同状态文字与未适配头诊断完成

2026-09-20。此报告只解释M82 Category访问的固定状态，不是四个独立跟踪器的正式评测。采用原封存最终权重与全部DepthTrack Train开发22；没有训练、改变文字bank、修改Hann或模板策略。全量33,108次跟踪位置耗时2245.60秒，28,897个位置具备有效GT。全量推理、分析及保存结果复核均exit=0。结果SHA256：`7fb8a4703870a600a3cdde956a4810d29b209958d6f9aec953a25726abab6681`。

四种分支共享同一次ViT/TSG/Mamba前向的搜索特征、初始化视觉参照及Category历史；只有Category实际提交bbox、query与模板。Empty/Swapped是同一Category权重更换词向量；Native是同状态绕过adapter的head，绝不是独立原生轨迹。每帧原Category框与score、上一框都与封存M82逐值一致，全部预测保存后才读取数据集GT。

| 同状态读出 | 单步平均IoU | IoU≤0.1 | IoU≥0.5 | 相对Category本帧救错 | 相对Category本帧伤对 |
|---|---:|---:|---:|---:|---:|
| Category | 0.729520896 | 4952 | 23662 | 0 | 0 |
| Empty | 0.729481836 | 4953 | 23663 | 0 | 1 |
| Swapped | 0.729643949 | 4950 | 23664 | 0 | 0 |
| Native same-state | 0.727848473 | 4953 | 23657 | 4 | 7 |

“救错”定义为Category IoU≤0.1而替代框≥0.5；“伤对”反之。其余跨阈值变化不包含在这两列中。这些是单帧框的读出计数，没有执行替代轨迹，不能报告为已救回的H10段、VOT anchor或长期恢复。

Category相对Empty仅高0.003906个百分点，相对Swapped低0.012305个百分点。此前完整递归中，Category相对同权重Empty低0.717390个百分点、相对Swapped高2.194584个百分点。两类结果的条件不同：M83固定历史，完整递归允许每一步改变未来输入。结果不支持“当前词义在大量严重错误位置上直接完成纠错”的解释；它提示应分析少量框、分数及历史状态差异怎样随时间演化。不能用两种均值相减，精确分解所谓语言贡献与状态贡献。

同状态未适配头平均IoU为0.727848，而独立原生完整轨迹为0.652226。这个差别表明未适配头在Category访问的输入上也能给出较好框；它不等于当前关闭adapter会把整条轨迹恢复到该数字，也不能把二者差额全部归因于某种记忆机制。

GT中心位于实际搜索区域内的24,323个有效位置中，Category单步均值0.866625，385个低重叠；中心位于区域外的4,574个位置中，4,567个低重叠。后者占全部4,952个低重叠位置的92.225%。这是按帧、按已访问状态的描述，长失败会重复贡献很多帧，不是“92.225%的失败起因是出画”；中心在crop外也不等于整个目标框完全不与crop相交。当前没有记录全部256个解码框的oracle，不能据此声称框内385帧全部是可被重排救回的错误。

Category分支由Hann选峰改读Raw最高峰，会在相同状态上把62个严重错误位置变成正确框，同时把99个正确位置变为严重错误。不能依据局部Raw正例全局删除Hann；这仍是读出诊断，并未执行新的递归策略。

| 同状态分数来源 | frame%50=0且Hann max>0.75的资格数 | 相对Category资格分歧 |
|---|---:|---:|
| Category | 260 | 0 |
| Empty | 255 | 7 |
| Swapped | 261 | 9 |
| Native same-state | 308 | 50 |

只有Category的260次是当前实际写入；其他只是同状态资格。此前各自完整递归的同权重Empty/Swapped实际写入为270/275，不能与本表混为一次只改变模板的实验。当前Category与Native的平均空间KL为0.016033，仍有50次资格分歧；空间概率相近不会保证绝对分数与模板行为一致。M83没有监督或测量模板未来效用。

M82主10/10、增量4/4、内容4/8及不晋升结论保持不变。M83没有生成新的三数据集正式指标，也没有将任何诊断读出转成部署策略。此前cup13的phone/apple、book06的cup/bird初始化输入核查提示自动词不应被当作语义真值，但三个事后样本不能估计全体错误率。

后续的中心化语义残差已完成独立方法审阅、CPU合成张量检查及bag05的101帧真实CUDA接口探针；探针验证Empty同状态特征及预测头输出恒等，但未进行GT性能评测，也尚无新训练结果。其Empty恒等性质不同于“所有非空词都能正确使用”，历史CPSD也已有显式Empty回退。必须依据明确的新对照判断是否值得训练，不能将代数性质写成语言收益。


完成态由fresh gpt-6-astra/max独立审阅，same-family/provisional；原始预测、GT与标量均复算，未独立重建未保存的完整响应图或远端模型二进制。审阅报告及独立复算脚本、JSON随完成态归档，不能将完整数值复核等同新的性能晋升。

中心化方案尚无正式训练结果。方法审阅为WARN，CPU及101帧CUDA检查仅验证代数、梯度与Empty接口；后续M84仍须单独代码审阅、真实训练前检和冻结协议。原生三数据集指标不变，M82内容4/8及不晋升结论不变。
