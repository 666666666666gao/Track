# M67后续持久诊断队列已启动：每240秒核验真实父进程

2026-09-07 12:35:14 CST启动，12:35:39完成真实进程核验。独立screen会话为`post_m67_diagnostics_20260907`，Python协调进程PID为471187。其可执行命令、cwd和Linux进程起始ticks均已核验，当前事件为等待既有M67控制进程。

本队列将已经准备好的实验串联运行，减少训练结束后的空档和人工重复轮询。不会重新启动、终止或修改当前M67训练。长期等待在远端持久进程中执行，间隔固定240秒；不在交互工具中进行数分钟阻塞等待。

## 已绑定的父进程与完成条件

父控制进程为PID465920，起始ticks为4577349167，命令为M67的原`run_m67.sh`，cwd为M67目录。等待期间每次都重新核验这个具体进程，不能仅凭锁文件或旧日志认定训练存活。

父队列必须先完成两组训练、两组开发22完整递归及原聚合分析，并使controller.exit、training_control.exit、training_support.exit、control_recursive.exit、support_recursive.exit和recursive_analysis.exit全部为0。之后才执行已冻结的M67完成态审计器。等待过程中若父进程消失、身份改变或完成码异常，协调器停止并保留日志，不自动重启训练。

## 冻结的后续顺序

| 顺序 | 阶段 | 条件及作用 |
| --- | --- | --- |
| 1 | M67完成态产物与原门槛审计 | 核验训练、最终权重、完整递归及原11项条件 |
| 2 | M67同权重内容对照 | 仅原开发门全部通过时执行，否则明确记录跳过 |
| 3 | M69固定M65 Null内容诊断 | 独立纯诊断，保留M65原失败结论 |
| 4 | M68固定M67最终head置信度诊断 | 检查既定支持分数读取，对预测框逐值复现 |
| 5 | M70同状态跨区域候选诊断 | 先严格GPU前缀检查，再检验新区域候选容量及文字作用 |

M67门失败只跳过其原来有门槛要求的内容实验。M69/M68/M70本身是已授权的诊断，不将这次跳过包装成M67晋升。任何已启动阶段出现运行错误、状态不一致或产物核验失败，整个后续序列停止，等待具体诊断；不重试、不放宽条件、不改换模型。

每个GPU阶段启动前检查两卡显存均低于500MiB、剩余磁盘大于1GB，并再次核验源码、规格和队列SHA。M70还执行其自己的状态与前缀验证。各阶段有独立stdout日志、退出码、结果状态及SHA记录。协调器不启动公共数据集评测，不创建晋升bundle，也不会把全部诊断完成写成原三数据集目标已完成。

## 启动时的实际训练进度

| M67组别 | 已完成拟合序列 | 跟踪调用 | 优化步 |
| --- | ---: | ---: | ---: |
| Control | 74/130 | 102,374 | 3,187 |
| Support | 72/130 | 98,583 | 3,070 |

两组原训练PID465923、465924均存活，训练已超过一半。上述数字是12:35:39的实时检查，尚无最终M67开发结果。协调队列也尚未启动任何GPU诊断阶段。剩余磁盘约2.26GB，没有删除权重或改变两个Qwen的保留要求。

运行位置：`/root/autodl-tmp/sttrack_post_m67_diagnostic_queue_20260907`。查看`running_identity.json`确认进程身份，`latest.json`确认阶段，`events.jsonl`查看事件链；各阶段终止状态需结合真实进程及对应退出码核实。公开的launch日志是启动快照，不是最终运行记录。

协调源码SHA：`4a4bc5debaf8581ede1cec7bd1c2020b3361ee5963509b7c7b18679fab3688cb`；spec SHA：`5f34ea8f38dfe6f7047d4cf65fba388da076c4c178d0b7e3b3afa3f4c078eddf`；启动核验SHA：`ce7d5abae2a79222b8ea65bd067c99d8b6c2fb58c9c3629e7b43c0d1ce2c3100`。没有新增独立模型审阅通过。
