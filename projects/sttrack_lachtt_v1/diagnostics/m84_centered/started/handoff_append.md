

## 5.174 M84中心化语义残差：单seed前检通过并启动（2026-09-20）

M83归档后，仅启动一个新的Centered Category训练臂：F+[D(x,q)-D(x,empty)]，共享两分支均反传，先相减再加冻结融合特征。它让Empty在同状态下严格回到原生接口；这不保证非空词具有正确语义，也不能撤销先前非空路径造成的历史漂移。不是已取得语言收益。

沿用M82零初始化、130条拟合序列及顺序、seed2027、186694跟踪调用和预期5798次优化；原始窗口上限仍5896。Raw竞争、可靠同状态native KL、Hann和每50帧/score>0.75模板策略不变。不增加多seed、文本dropout、多起点、记忆或恢复模块。仅训练289154个存储参数，其中末层768维bias在差分中抵消；每步增加一次adapter分支前向，不称严格等计算。M82拟合及开发非空输入已逐bank验证，复用其非空路径作为hard-Empty-mask控制，不训练梯度为零的Empty臂。

正式启动前完成fresh gpt-6-astra/max代码审阅（same-family/provisional）与真实前检。零初始化Category、非零M82参数Empty各101帧，与独立native的bbox/score/query/template精确相等；另96帧3次优化验证可训练、梯度有限、底座及buffer不变、训练后Empty特征恒等。前检权重未保存或用于正式训练。完整来源、前检及冻结SHA见started目录回执。

最终完整pass权重将依次完成Category/同权重Empty/同权重Swapped三组开发22递归，全部预测封存后计算GT指标。预先冻结14项条件：对M82的4项增量、对原生的4项聚合及原生成功序列保护、对Swapped的4项内容对照、Empty与原生逐序列指标一致。未通过就保留负结果，无自动正式外部评测；不累加所有历史模型成功轨迹门，也不事后修改本轮门。

此处只有启动与前检证据，没有M84完成态训练或开发性能。预计训练约4–5小时、三组开发评测约1小时，后续以实际吞吐更新。保留两套Qwen、原生底座及历史final。M82内容失败、M83诊断结论与正式三数据集指标均不变。
