

### 5.175 M84 完整训练结束与保存产物核验（2026-09-20 23:30 CST）

M84 Category 单 seed2027 的完整拟合已结束，训练阶段退出码 0，最终权重按预定单次全遍历保存。网络仍为冻结 STTrack 与共享双支差分语义适配器，架构和实验计划见 §5.174；本次没有新增模块、修改损失或选择中间 checkpoint。

| 训练完成项目 | 已核查数值 |
|---|---:|
| DepthTrack Train 拟合序列 | 130 |
| 实际跟踪调用 | 186694 |
| 实际优化次数 | 5798 |
| 累计训练用时 | 14427.053秒（约4小时27秒） |
| 目标中心在crop内的监督帧 | 156814 |
| 有效GT中心在crop外的监督帧 | 17665 |
| 无效GT、仅推进状态的帧 | 12215 |
| 核查的隔离源码文件 | 161 |
| 核查的采样状态记录 | 3917 |

最终权重 `training/category/final.pth` SHA256：`c63605ebcb1f702de66f96967255e5301bfdca1e3c40450b6d4b8a952791b1e0`；训练结果 SHA256：`9dbb64e252ed42ab265a6bd1154760f67c23a732203facbb3180275a7a090f20`。原始权重在远端保留，不上传GitHub。

独立于训练入口的保存产物核验已通过：逐序列顺序、帧数、标签计数、累计优化数与冻结计划一致；最终权重及日志哈希匹配；适配器、优化器张量有限；最终模型与最后latest模型张量逐项相同；空词参照buffer与初始化相同；确有适配器参数发生变化。底座参数及buffer未变化由已核对源码的训练入口在结束时执行张量哈希断言。该检查没有独立重跑全部训练帧，也不能从采样记录重建完整访问状态流摘要，不能将保存产物核验称为独立训练复现。

原类别和同权重Empty的两组完整开发递归已由原控制队列自动并行启动，进程14246、14247经命令核查存在。随后按预定顺序运行Swapped；三组预测全部封存后才执行GT指标分析。目前没有开发完成态指标，更没有新的正式三数据集成绩。下一步仅完成既定14项条件、同权重内容归因和Empty/原生逐序列一致性检查，再依据完整结果决定后续优化或外部验证；不因训练完成而晋升。

证据目录：`projects/sttrack_lachtt_v1/diagnostics/m84_centered/training_completed/`，包含原训练日志、逐序列记录、采样状态、结果及保存产物核验源码/回执。既有全量DepthTrack、CDTB、VOT正式参照不变。
