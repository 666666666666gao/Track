

M84等待期间的三数据集输入与接口准备核查（2026-09-20 11:34 UTC）：

# M84 正式三数据集评测准备状态

2026-09-20，只读输入核查与隔离接口源码准备。M84 正在训练，此记录不表示开发条件通过、外部评测启动或目标指标达成。

| 范围 | 当前核对结果 | 仍需完成 |
|---|---|---|
| DepthTrack Test | 50序列，76373帧，逐序列RGB/Depth文件数及首帧存在性匹配原生输入清单 | 新协议下50个初始化文本输入、最终权重绑定、完整预测与正式P/R/F |
| CDTB | 80序列，101956帧，逐序列RGB/Depth文件数及首帧存在性匹配原生输入清单 | 新协议下80个初始化文本输入、最终权重绑定、完整预测与正式P/R/F |
| VOT-RGBD2022 | 127个序列目录存在，冻结清单1765个anchor、计划1327004个跟踪位置；未重新清点所有VOT帧文件 | 保留303个既有低22初始化输入并重新绑定新协议；覆盖其余1462个anchor初始化观测；完整正式EAO/ACC/ROB |
| 低22文本bank | 303个不重复初始化key，向量形状303×5×768；空词与M84拟合集完全相同 | 当前元数据仍绑定M78协议，不能直接称为M84已绑定输入 |
| 公共预测入口 | 复用既有OPE、TraX、VOT桥与初始化key逻辑，只把隔离semantic_runtime架构断言改为centered_v1；Python语法检查通过 | 绑定最终base/adapter/config/text/protocol，并以真实最终权重检查OPE/TraX一致性 |

旧M78的full_evaluation目录当前不存在，旧记录的“full sources prepared”仅代表脚本/清单准备，不能据此宣称完整文本bank已经生成。1462是待覆盖anchor观测数，不是已经完成或承诺必须执行的caption调用数；初始化key去重应由真实观测清单确定。

本轮没有解码外部图像、打开后续GT、生成新caption、运行跟踪或优化。仅核对原生参考材料和输入目录；源文件SHA、bank SHA与核查范围见readiness.json。没有修改M84冻结的training_spec、recursive_spec、运行code或后台队列。

后续顺序：等待M84最终训练及三组开发结果并完成审阅；按完成态决定是否进入外部验证；若进入，冻结同一最终权重和文本协议，完成真实接口一致性后再执行外部评测。完整评测仍须报告同一个模型的三个数据集，不移接历史SRTrack成绩，不选择中间checkpoint，不追加seed。
