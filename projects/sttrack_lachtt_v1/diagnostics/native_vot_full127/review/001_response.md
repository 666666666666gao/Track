Scoped Type-A advisory review completed. I found **no arithmetic, count, hash-binding, or result-number defect** in the downloaded native STTrack full127 artifacts. The correct verdict is **PASS for the bounded local artifact audit**, with a **scope WARN**: this verifies the official VOT report, returned-curve reaggregation, result/manifest hashes, and local bundle consistency; it is **not** a fresh raster-overlap recomputation from images and does not verify live server state.

Root used below: `C:\Users\gb\.codex_remote_staging\native_full127_completed_20260906`.

| Check | Status | Result |
|---|---:|---|
| A. GT provenance / prediction-before-GT export | PASS | GT is dataset GT in the VOT workspace; bundle records prediction sealing before GT copy; no model-produced GT path found. |
| B. Official VOT metric semantics / normalization | PASS | VOT 0.7.1 RGBD2022 multistart stack is bound; EAO/A/R are official fractions with percent fields equal to ×100; no self-normalization found. |
| C. Coverage / hashes / reused anchors | PASS | 127 sequences, 1765 anchors, 5295 prediction files, 7231 manifest records, and 303 reused M39 anchors are internally consistent. |
| D. Result numbers / curve reaggregation / thresholds | PASS | Result metrics match analysis JSON; EAO reaggregates exactly from the returned official curve; threshold booleans are correct. |
| E. Failure outcome coverage | PASS | 1765 outcome rows, 183 confirmed failures, 127 per-sequence rows; per-sequence anchor/failure sums match. Confirmed-failure count is distinct from ROB. |
| F. Scope / attribution | WARN | This is a native STTrack baseline, no new checkpoint, no optimizer steps, no language module; no M55/M54/new-trained-model performance claim is supported. |

Evidence and checks:

- The frozen spec declares 1765 anchors, 127 sequences, 303 reused anchors, 1462 new anchors, language disabled, no new weights, and zero optimizer steps: `spec.json:2-19`. It also states that the baseline alone does not satisfy the final new DepthTrack-trained bundle requirement: `spec.json:10-12`.
- The runtime config points at the VOT2022 RGBD stack: `run\master\config.yaml:1-4`. The bundled VOT stack defines `multistart_eao_score` with `low: 115`, `high: 755`, `multistart_eao_curve`, and `multistart_average_ar`: `source_snapshot\vot\stack\vot2022\rgbd.yaml:4-16`.
- The official analysis JSON records toolkit `0.7.1` and tracker command/environment: `run\master\analysis\sttrack_default_full127_analysis.json:1-19`. Its baseline parameters bind the same EAO/AR settings: burnin 10, grace 10, bounded true, threshold 0.1, ignore mask `_ignore`: `run\master\analysis\sttrack_default_full127_analysis.json:659-699`.
- The VOT source confirms A/R denominator behavior: per-sequence accuracy sums overlaps up to progress, robustness is progress over total proxy length, and the sequence aggregator weights by returned sequence weights: `source_snapshot\vot\analysis\multistart.py:97-135` and `source_snapshot\vot\analysis\multistart.py:164-188`.
- The EAO score source computes the mean of the returned curve slice `x[0][self.low:self.high + 1]`: `source_snapshot\vot\analysis\multistart.py:404-442`. The returned curve has length 755, so the official Python slice for `low=115, high=755` uses indices 115..754. My independent local reaggregation over those 640 returned curve values equals **0.773216535962124**, exactly matching the analysis score at `run\master\analysis\sttrack_default_full127_analysis.json:705` and result fraction at `result.json:15909-15912`.
- A/R values match exactly: analysis JSON has ACC **0.8247118980990448** and ROB **0.9366910936768168** at `run\master\analysis\sttrack_default_full127_analysis.json:1471-1475`; result JSON repeats those values at `result.json:15909-15912`. Percent fields are exact ×100: ACC **82.47118980990447**, EAO **77.3216535962124**, ROB **93.66910936768168** at `result.json:15914-15917`.
- Threshold comparisons are correct. Spec targets are ACC 82.1, EAO 77.9, ROB 93.7 percent: `spec.json:33-37`. Finalizer uses strict `metrics[k] * 100 > target` comparison: `finalize_default_full127.py:70-75`. Result booleans are ACC true, EAO false, ROB false: `result.json:7-11`, which matches the recomputed comparisons.
- Execution is marked complete by all terminal exits: `evaluate.exit:1`, `finalize.exit:1`, and `controller.exit:1` are all `0`. The analysis log reaches `100 %` and reports success: `analysis.log:61-63`.
- Finalizer validates the result pipeline before writing `result.json`: it checks `evaluate.exit`, source hashes, checkpoint hash, shard manifest hash, preseed receipt hash, merge completion, 1765 anchors, 5295 expected result files, and every merged result hash: `finalize_default_full127.py:18-47`. It then runs official VOT analysis and extracts metrics from the analysis arrays: `finalize_default_full127.py:48-58`. It records no optimizer steps, no new checkpoint, language disabled, and the native-baseline scope: `finalize_default_full127.py:66-76`.
- Merge coverage is internally consistent. `run\merge_result.json:1-5` records `anchor_count: 1765`, master workspace, and `result_file_count: 5295`; `run\merge_result.json:5298-5306` closes the 5295-result map with `status: complete` and tracker `sttrack_default_full127`. I independently rehashed all 5295 local prediction files against `merge_result.json`: **0 missing, 0 mismatched**.
- Shard coverage is complete: `run\shard_manifest.json:1-4` states the native source is the exact M39 default wrapper/checkpoint with no experimental head or text. The manifest lists the 127 sequences through `run\shard_manifest.json:5-132`, declares `shard_count: 4` at `run\shard_manifest.json:134`, and shard anchor counts are 441/441/441/442 at `run\shard_manifest.json:136-138`, `run\shard_manifest.json:3678-3680`, `run\shard_manifest.json:7220-7222`, and `run\shard_manifest.json:10762-10764`. The footer records `total_anchor_count: 1765`, `total_estimated_frames: 1327004`, and tracker `sttrack_default_full127`: `run\shard_manifest.json:14950-14953`.
- Reused-M39 anchor handling is bound. `preseed_receipt.json:1-5` records 303 reused anchors and a file map. The receipt ends with source merge SHA, source tracker `sttrack_m39_default_low22`, and `status: complete`: `preseed_receipt.json:3643-3645`. My local check verified all 909 preseed-recorded files are present in the master result tree with hashes matching the preseed receipt. The local review bundle does not include the original remote M39 source tree, so I did not independently rehash the original M39 source paths; the finalizer source states that it checked both the preseed source file and the master copy at runtime: `finalize_default_full127.py:41-47`.
- The review bundle is consistent. `review_archive.json:1-7` records 7232 archive members and the manifest SHA. `download_binding.json:10-17` records 7231 files verified, all bytes/hashes matching, GT from dataset, images not included, no tracking rerun, and no metrics recomputed by the downloader. I independently rehashed all 7231 `review_bundle_manifest.json` file records locally: **0 missing, 0 mismatched**.
- Prediction-before-GT export is recorded in both source and manifest. The exporter includes all tracker result files, then sequence metadata files, and asserts 1765 `.bin` files and 127 `groundtruth.txt` files: `export_review_bundle.py:12-26`. The produced manifest records `images_included: false` and `predictions_sealed_before_GT_copy: true`: `review_bundle_manifest.json:1-7`.
- GT is dataset GT, not prediction-derived GT. The VOT multistart analysis reads tracker trajectories and compares them to `proxy.groundtruth()` using `calculate_overlaps`: `source_snapshot\vot\analysis\multistart.py:111-116`. The downloaded binding also states `groundtruth_from_dataset: true`: `download_binding.json:14-17`. Local sequence-tree inspection found 127 sequence directories, 127 `groundtruth.txt` files, 1906 metadata/GT files total, and 0 image-like files; this supports GT/metadata review but not fresh image-based runtime verification.
- Failure outcomes are consistent and distinct from ROB. `result.json:6` reports 183 confirmed failures; `result.json:15900-15905` gives failure settings burnin 10, grace 10, `_ignore`, threshold 0.1. The helper marks a confirmed failure when progress is shorter than the overlap run and accumulates a boolean failure count per anchor: `source_snapshot\finalize_vot_transaction_low22.py:179-190` and `source_snapshot\finalize_vot_transaction_low22.py:219-258`. I independently counted 1765 outcome records, 183 `failed=true`, 127 per-sequence records, per-sequence anchor sum 1765, and per-sequence failure sum 183. Recomputing official ROB from the stored outcomes requires VOT’s sequence-length weighting; using that weighting gives **0.9366910936768168**, exactly matching the official ROB. A raw all-anchor progress/run-length ratio is **0.9286264397093**, so it should not be reported as ROB.

Claim status:

- Supported: “Completed native STTrack default full127 official VOT-RGBD2022 analysis produced EAO/ACC/ROB = **77.3216535962124 / 82.47118980990447 / 93.66910936768168 percent**.”
- Supported: “The result has **127 sequences, 1765 anchors, 5295 prediction files**, and **183 confirmed failure outcomes**.”
- Supported: “Only ACC exceeds the requested target; EAO and ROB do not.”
- Supported with scope: “303 M39 anchors were reused and are byte-bound in the full127 master result tree.” I verified local master copies against the preseed receipt; I did not independently rehash the unavailable remote M39 source paths.
- Unsupported: any claim that this is a new trained checkpoint, language-module result, M54/M55 result, or final DepthTrack-trained three-dataset model bundle.
- Unsupported by this review: fresh raster-overlap recomputation from raw images. I verified official report arithmetic, hash bindings, curve reaggregation, and failure-outcome consistency from the downloaded bundle.