# 原生 STTrack full127 完整 VOT 结果

2026-09-06 完成官方 VOT-RGBD2022 multi-start 分析，EAO／ACC／ROB 为 **77.3216535962124／82.47118980990447／93.66910936768168%**。只有 ACC 严格超过目标；EAO、ROB 均未通过，不能把 ROB 四舍五入到 93.7 后判为达标。

| 指标 | 完整结果（%） | 目标（严格大于） | 差值（百分点） | 通过 |
| --- | ---: | ---: | ---: | --- |
| EAO | 77.3216535962124 | 77.9 | −0.5783464037876 | 否 |
| ACC | 82.47118980990447 | 82.1 | +0.3711898099045 | 是 |
| ROB | 93.66910936768168 | 93.7 | −0.0308906323183 | 否 |

覆盖全部 **127 条序列、1,765 个 anchor、5,295 个预测文件**，确认失败 anchor 为 **183**。其中 303 个 anchor 沿用已封存的 M39 default low22 结果，其 909 个文件与 preseed 收据一致，其余 1,462 个 anchor 新运行。evaluate、finalize 和 controller 的退出码均为 0。失败数是离散事件计数，ROB 使用官方按序列长度加权的生存比例，两者不能互换。

## 结果属于哪一个模型

这是原生 STTrack default 的完整参照结果，基础权重 SHA 为 `cacbd799115be1aaeb049cee0db89270851e3b6dd68997553b4c2c31c1104f98`，语言关闭、无新增关联头、优化步数 0。源码、配置、权重、M39 复用及合并结果均由 spec 和完成态 result 绑定。它不是 M54、M55 或新训练权重的结果。

相对历史 SUTrack identity-only full127 的 74.020583／82.579344／89.565651%，此次 EAO 增加 3.301071、ACC 减少 0.108154、ROB 增加 4.103458 个百分点。这是底座与对应运行路径的比较，不能归因于语言或某一新增模块。

同一原生权重及绑定的模型／tracker／配置在三个数据集上的完整参照现已齐全：

| 数据集 | 指标顺序 | 完整原生结果（%） | 达到该数据集全部目标 |
| --- | --- | --- | --- |
| DepthTrack Test，50 条 | P／R／F | 62.415336／62.682004／62.548386 | 否 |
| CDTB，80 条 | P／R／F | 69.933113／68.067744／68.987821 | 否 |
| VOT-RGBD2022，127 条 | EAO／ACC／ROB | 77.321654／82.471190／93.669109 | 否 |

前两组原始报告位于相邻 `native_ope/completed_depthtrack`、`native_ope/completed_cdtb`。这组表可以作为同一原生底座的参照，不能替代用户要求的新 DepthTrack Train 权重验证，也不与历史 SRTrack 指标拼接。

## 官方协议与复核范围

使用 VOT toolkit 0.7.1 的 `vot2022/rgbd` 栈，EAO low=115、high=755，A/R 阈值 0.1、grace=10、burnin=10、ignore mask 为 `_ignore`。官方 JSON 返回的 EAO 曲线长 755，绑定源码实际取 `curve[115:756]`，得到 640 个已有值；复算遵循这一官方切片，没有另换端点。

本机已下载并逐文件验证包含原始预测和数据集 GT／元数据的私有归档，7,231 个记录的大小与散列全部一致。独立 Type-A 审阅再次核对了全部本机文件、5,295 个预测文件、909 个复用文件，以及每个 anchor 的失败结果；官方 EAO 曲线重聚合与 ROB 按序列长度重聚合均一致。`verify_report.py` 可用标准 Python 重复报告级计算。

这次审阅没有重新计算 GT 与预测的栅格重叠，也没有重新运行跟踪或检查远程 M39 原始目录。该限制不等于缺少 GT：私有包包含原始数据集 GT；公开包保留报告、代码、元数据清单和散列，未再次分发原始图像、GT、轨迹归档或模型权重。官方 analysis 中的图像尺寸也有记录。本报告的审阅范围是已封存报告及其计算、字节绑定的一致性。

M55 两组全模型配对微调仍在运行。只有完成训练、固定 22 条 Train 开发递归及后续低22晋升后，才评估新权重的完整三数据集结果。
