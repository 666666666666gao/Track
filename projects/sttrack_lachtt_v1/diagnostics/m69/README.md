# M69：M65固定Null权重的独立文字诊断已准备，尚未执行GPU回放

2026-09-07 11:54:53 CST完成准备与CPU检查。用户明确要求另立纯诊断，固定M65 Null权重比较类别、空文本及既有替换类别，且不能因此自动晋升。本实验依此建立独立M69目录与脚本。

M65原计划要求全部开发门通过后才执行的内容实验保持原样；其glass03保护失败、9/10门通过及原content_counterfactuals_allowed=False均没有修改。M69承认是在已知M65失败后建立的诊断，不能称为M65结果出现前预注册的新实验，也不能事后为M65补发晋升资格。

## 固定干预

| 条件 | 唯一文字内容差异 |
| --- | --- |
| Category | 复用M65 Null已封存的完整类别保留轨迹 |
| Empty | 所有有效词槽替换成既有CLIP空字符串向量 |
| Swapped | 只替换槽0类别，使用M60事先冻结的捐赠类别映射 |

三组固定同一个M65 Null最终head、原生base、5槽结构、mask、padding、初始化框及运行代码。类别以外的有效属性槽继续为空向量；不生成新caption或embedding，不训练新参数。自动类别仍可能错误，捐赠类别也不必然是经核验的语义矛盾，因此实验衡量词义敏感性，不能单凭胜负证明同类实例识别。

固定Null head SHA：`5caf9c65c850cdd79d1aee75a002ffd0b74d5876292680349b9387318780e758`。
新诊断源码SHA：`c3feaa2f1507dc5174331d0a1ebd0fbf96032fc5434f9f82675bd83cc7292ce0`。
新spec SHA：`1edc827b9f5aaf9dffee70b4f85e456b6f66c9a2e0dfab5c189bb4132ceeab8e`。

## 已完成检查与待执行工作

新代码的跟踪函数、父产物校验函数及读写函数与原M65内容实验的AST完全一致；两份新控制bank与M65原来冻结但未执行的bank逐字节一致。22条类别向量均确实改变，5×768槽结构、mask与padding保持一致。源码可编译、队列通过bash语法检查，原完成态证据和失败门均通过SHA校验。CPU检查没有初始化CUDA或加载学习head。

GPU执行时先在bag05、cup08、mobilephone02各重放102帧类别前缀，共303次跟踪，要求bbox/score与M65封存轨迹逐值完全一致；通过后才执行Empty与Swapped各22条完整递归，各33,108次跟踪。所有预测封存后才读取GT，沿用相同mean IoU、宏平均、低重叠帧和H10口径，并重建实际模板写入数。

截至本节，前缀与两组完整回放均未启动，没有词义对照新结果。双卡仍被M67训练占用，独立队列在启动时要求两卡空闲，防止干扰现有训练；不会自动终止或挤占M67。预计空闲后两组并行完整回放约30分钟，实际时长须以前缀与进度记录为准。

沿用原来的内容差值准则只作描述性统计。无论结果怎样，新脚本的low22_candidate_preparation_allowed和full_three_dataset_evaluation_allowed都固定为False，不创建新公开候选或三数据集评测。M67自己的原晋升门与条件内容实验也不受M69影响。

本项回应的是M65权重下尚缺失的内容归因。跨区域恢复仍须另行验证：同时覆盖健康、局部失败和目标重现窗口，先统计新观察能否产生正确候选，再设计部署触发与身份确认；不能用本项词义对照或M67训练支持量代替该证据。
