# Four sealed M76 onset visualizations

See EXPLANATION.md and render_receipt.json. The renderer uses all four originally selected M76 onsets, the exact source images, stored Category-state boxes and dense response arrays. Every panel uses the same actual zero-to-one score scale; no tracking or optimization runs during rendering. All16 head-score rows reproduce the sealed M76 result. These are historical selected Train cases, not M77 outcomes or recursive rescues.

PNG/PDF deliverables remain on the server and in the local Desktop/document directory. This folder contains the source, numeric plotting data, CSV and provenance, not the dataset image pixels. Master section5.148 records the delivery and interpretation.
