# M57词义证据范围审计：同类属性与类别变化分开解释

这是对已经封存的静态结果进行的后验子组审计，没有新推理、训练、候选选择或阈值调整，也没有读取本轮完整递归预测或逐帧GT。原错配规则与所有权重不变；“同类别”子组在静态结果出来后划分，因此不称为预注册确认性实验。

## 同类别替换目前覆盖有限

| 同类别、不同描述的固定mask替换 | 事件／有效GT | 真实文字 mean IoU | 替换后 mean IoU | 选框变化 |
| --- | ---: | ---: | ---: | ---: |
| `candidate_text` | 114 / 94 | 0.428385922 | 0.426924361 | 1 |
| `initial_text` | 114 / 94 | 0.419868382 | 0.419868382 | 0 |

四条目标序列及文字来源分别为ball07→ball19、cup08→cup10、car01→car02、flower02→flower03。它们保持类别与slot mask，文字描述存在差异，如白色杯子与红色杯子、黑色带8号标记的球与棕色皮革球。这里只证明不同描述来自相同类别，没有对每个替换属性进行独立真假标注。

initial_text在这114个事件中选框不变，不能推断所有logits、注意力或属性表示都不变。更关键的是，114个事件中仅3个满足“默认IoU<0.5且候选oracle IoU≥0.5”，覆盖cup08与flower02；两种文本头在这3个事件的真实／错配选择都不变。由于真正可以验证纠错的覆盖很小，不能据此断言属性文字没有价值，也不能把该子组当作已经充分检验了同类实例消歧。

## 明显正例集中在两条序列，并且改变了类别

| initial_text真实正确／错配严重错 | 真实文字IoU | 错配IoU | 错配文字来源 | 原文字复核来源 |
| --- | ---: | ---: | --- | --- |
| `egg_indoor@2439` | 0.863486 | 0.000000 | `flowerbasket_indoor` | `corrected_after_cross_frame_identity_audit` |
| `egg_indoor@3547` | 0.851642 | 0.000000 | `flowerbasket_indoor` | `corrected_after_cross_frame_identity_audit` |
| `ghostmask_indoor@1160` | 0.895081 | 0.000000 | `mobilephone02_indoor` | `accepted_visual` |

原先汇总的3个“真实文字正确、错配后严重错误”事件来自egg的两帧和ghostmask的一帧；分别用花篮文字替换鸡蛋文字、手机文字替换面具文字。它们都是类别也发生改变的替换，不能将其归因为具体同类实例属性的区分。

egg的文字具有已披露的历史跨帧身份复核来源，因此其中两次正例也不能作为严格独立首帧自动注释协议的证据。全部85条文字中78条accepted_visual、5条cross_frame_identity_audit、2条independent_gt_crop_adjudication；此处只核对既有来源，没有更改文字。3个事件不是3条独立序列重复，也不是完整轨迹救回数。

## 训练标签覆盖与后续判断

固定拟合标签1511个中，762个为默认候选0、639个为NONE、110个为其他候选；其中51个其他候选标签为候选1。重复20轮不增加独立事件数。这是训练数据分布描述，不是已经证明的不均衡致因，也不自动支持重采样或放宽提交条件。

完整递归仍按既定四组门槛完成。若主实验未通过，后续应先验证增加了什么同类属性区分监督与可纠错事件，再决定是否修改语言融合；已有M52的数据扩充失败不能被忽略，重复旧事件或换名称不足以解决问题。持续身份传播、语义视角记忆仍是待验证方向，没有因这几个局部正例而晋升。

## 原始证据

结果SHA为60b16f801e75acfd80262ac000b482b60c69c31089ee8b08a76be01cb7dee8a6，读取的M57静态结果SHA为1987764e5db554232289366600cbc06c979767f105ca886d246b121a45057556。均值使用已封存逐事件IoU以Python双精度汇总，与此前float32聚合的末位微小差异不代表重新推理。

- [全部审计记录与文字来源](audit.json)
- [审计源码](audit_static_content.py)
- [M57完整实验范围](../README.md)

M57完整结果尚未生成时已固定后续分叉诊断与导出源码，见[工具绑定](../completion_tools_spec.json)。它们需要四组预测和统一分析正常结束才执行；本次仅检查语法并冻结来源，不声称已完成88条轨迹核验。
