

## 5.160 M80启动：固定seed2027的训练期文本置空对照

M79完成后继续回到DepthTrack Train。M80于UTC2026-09-08T07:28:31.409548+00:00启动，进程120925在GPU0实际训练，screen为`sttrack_m80_dropout_20260908`；启动核查UTC2026-09-08T07:31:49.929124+00:00已完成1条拟合序列。当前没有完成态训练或评测指标。

### 架构与唯一训练干预

保留M78的冻结STTrack（ViT/BSI、TSG、Mamba与Center Head）以及TSG/Mamba后、Center Head前的289154参数密集Null语义适配器。类别位于槽0，属性使用既有CLIP空文本向量，五槽、mask、padding、初始化4×4 RGB-D参考不变。推理仍是原类别、Hann选峰、原生query、factor4搜索、每50步且分数严格大于0.75的动态模板规则。无在线Qwen、新候选头、重检测或新模板规则。

本轮唯一新增干预是训练期文字内容：用独立的Python随机数生成器seed2027，在训练前固定每个32帧块是否置空，概率0.20；186694次跟踪调用中36917次为空文本、149777次保留类别。只替换有效槽内容，保留padding、mask和初始化RGB-D证据，不把空向量当零向量。日程不会消耗网络随机数流，也不依赖GT、训练损失或开发结果。切换不重置bbox/query/模板，当前预测先提交，再用GT计算当前损失，状态detach。它是缺词训练增强，不是在线caption协议。

历史SRTrack曾使用15%文本dropout，因此不能把“首次引入文本dropout”写为创新。M80检验的是当前密集适配器、完整预测裁剪与原始响应竞争训练下，缺词条件是否缓解M79暴露的外部脆弱性。训练中的块切换与静态部署存在差异，需要由完整递归结果检验，不能预先宣称稳健性成立。

| 实验项目 | 冻结配置或当前状态 |
| --- | --- |
| 随机种子 | 仅2027，无其他seed |
| 拟合数据 | 同一130条DepthTrack Train序列，同一顺序、首帧初始化和完整正向轨迹 |
| 训练预算 | 一轮186694次跟踪调用，预期5798次优化；仅训练一个M80模型 |
| 初始化与优化 | M78相同初始适配器tensor；AdamW，lr=1e-4，weight_decay=1e-4，32帧累积，clip=1 |
| 损失 | 原生focal＋2GIoU＋5L1＋weight1原始响应竞争，与M78一致 |
| 固定对照 | 复用封存M78 Category与独立训练Empty；没有重新训练对照或伪称新配对运行 |
| 本轮评测 | 最终M80同一权重分别运行原类别、空词、既有替换类别的Train开发22 |
| 完成态指标 | 尚无，不填预测结果 |
| 正式外部评测 | 不自动启动；开发与内容归因通过后另行审计低22资格 |

预先冻结检查：相对原生按帧IoU至少＋0.002、相对同预算M78 Empty至少＋0.001；宏平均不低、低重叠帧与H10不高，并分别保护两参照的零H10序列，共10项。另有M80相对M78 Category的4项机制对照（按帧/宏平均不低，低重叠/H10不高）；同权重原类别对空词和替换类别的按帧/宏平均严格更好、低重叠/H10不高，共8项。三组完整预测都封存后才读取GT做分析。内容诊断无论主门通过与否都运行，但不能覆盖失败的主门。全部条件见evaluate.py，不积累所有历史模型的成功序列并集。

因果预检查完成：两条零残差路径各101帧与原生框、分数、query、模板逐项一致，均在第100帧更新；使用96帧拟合样本、每路径3次优化，Category路径中第33—64帧实际置空、随后恢复，梯度有限，RGB/Depth/Text投影有梯度，底座参数及buffer保持不变；检查权重未保存且不进入正式训练。合成竞争检查排除了同实例伪负例，并验证对几何掩码无梯度。检查不是独立模型审阅PASS；此前指定gpt-6-astra/max审阅额度不可用，本轮没有改用其他模型或伪造审阅。

源码与计划冻结：training_spec SHA256 `eb1e4b9217733006075a90aeaee8a540875c9722129a3101ca665c0a8c509972`；文字日程`f9e90e8eede50c90e4c4eac3750651dd624b9c6f7aaa26a0ab331f716081afff`；检查报告`613b19bfb63a228af6e9c1f2f2ec713bbfed6b8b0f10269ccf5a56c89efa9130`。原始M78训练、网络与运行源码均保留，M80改动单独落在`/root/autodl-tmp/sttrack_m80_block_text_dropout_20260908`。预估训练约3.5—4小时，后续三种内容完整递归约1小时；screen断开不影响任务，初次检查后靠近预计完成时间再查询，需要轮询时按240秒。未因观察超时重启。

磁盘启动前约1.60GB可用；仅新增轻量适配器latest/final与日志、预测，不复制底座或下载新模型，预估新增不足100MB。Qwen3_8B和Qwen2.5-VL-3B-Instruct都保留，本轮未删除权重。下一步完成M80开发/内容对照；若不能同时保留语言增量和原生性能，保持负结果，不能通过选择空文本部署、阈值或早期checkpoint制造晋升。最终同一模型三数据集目标仍未完成。

用户在M80已启动后补充了以初始化覆盖为优先的研究顺序：后续单独准备DepthTrack Train内多起点Category/Empty配对，保持M78目标、固定seed2027和预算，不加入身份一致性损失、反向训练或新模板策略。M80按已经冻结的原方案完成，不在运行中改变条件。新的多起点实验此刻尚未创建或启动，需先核定合法初始化、对应短描述与预算；不得借用VOT起点清单，也不能把数据覆盖实验本身包装成已成立的新网络贡献。
