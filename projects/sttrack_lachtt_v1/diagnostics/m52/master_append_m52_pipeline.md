

### 5.54 M52成对训练与递归验证实现冻结（2026-09-05，训练前）

本节记录已实现的执行链，不新增性能结论。M52仍沿用上一节的固定采集协议和M45原始几何关联头；新代码未修改正在运行的采集器、基础跟踪器或模型架构。`tools/train_sttrack_m52.py`、`tools/run_sttrack_m52.py`、`check_runtime.py`、`run_training.sh`已完成，训练绑定SHA256为 `d308fbef99d65ffe53caaa6c9f62b4f475ec1fb8ab1ad2ce56a7c5b422e3ea4d`。

训练器先核对原85份特征、新63份特征和63份完整选择轨迹的封存校验值，再读取既有DepthTrack Train监督。每个视图按自己的候选框计算标签，候选0对应其当前预测状态下的原生框，实际选中输出另存；mixed输入使用采集到的真实previous_choice。两组均fresh seed2026、448,739参数、20epochs和1,900步，验证初始参数与逻辑样本顺序摘要一致。3,022个逻辑视图仍只对应1,511个物理采样事件，不能扩大有效样本数。

| 执行阶段 | 已实现的检查与产物 | 本节状态 |
|---|---|---|
| 数据审计 | 哈希、物理帧配对、fit/dev按序列分离、实际前一选择、候选0标签 | 等待完整采集后执行 |
| control / mixed训练 | 相同初始化、顺序及步数；固定末轮权重；重载输出精确一致 | 尚未启动 |
| 实际部署加载检查 | STTrackCandidateSet加载器与独立头对真实捕获输入的输出一致 | 训练后执行 |
| 两组完整递归 | 每组22条/33,130帧，合计66,260帧；全部完成后读取GT分析 | 尚未启动 |

**训练前晋升规则补充。** `PRE_TRAINING_NOTE.md`在本轮GT审计、优化器训练及权重结果之前明确：mixed必须同时通过原生门和配对control增量门，才令primary_pass为真并支持“状态数据有效”；若mixed未通过，但control独立满足全部原生门，control可作为“增加训练步数”的改进继续验证，不能将其称为状态数据收益。否则两组均停止。数值门、数据、模型和优化器设置未改变，原始spec字节未改变。该补充将研究假设是否成立与用户要求的模型性能晋升分开，避免把control真实改善归给新增状态数据。无公开评测自动启动。

独立上下文GPT-5.5 xhigh审查记录于 `diagnostics/m52/EXPERIMENT_AUDIT.md/.json`，原结论WARN保留。它是同属GPT系列的Type-A建议性审查，不称为跨模型家族的独立验收。审查时只有启动和240步采集一致性证据，没有M52训练或递归结果；报告也指出overlay未附原生sttrack.py。已从实际运行目录取回10,216bytes的原生源码供进一步复核，SHA256 `d67d551a612b80cee5b19a00f6fecd5d0f7ed0c907e800f452873afd684cc58f`与既有M44绑定完全相同，保存于 `diagnostics/m52/source_snapshots/sttrack.py`。此举只补齐审阅材料，未改变运行源码。

补充审查见 `EXPERIMENT_AUDIT_SOURCE_FOLLOWUP.md`：原生快照字节核验、初始化重置、模板/query/状态顺序与采集状态关联均未发现明确实现缺陷。整体WARN仍保留，因为完成态训练和递归产物尚未产生。240步契约只证明挂接记录器与未挂接的同一STTrackCandidateSet路径一致，不表述为候选策略在所有情况下等同于原生STTrack；scores与references来自真实hook捕获，也不夸大为全部字段都经过独立重算。

本地AST、远端Python3.8命令行导入和bash语法检查已通过；这些检查不等于数据审计、训练完成或性能提升。下一步待63条采集全部封存、GPU0释放后执行成对训练及两组完整递归，并据实际结果更新退出码、权重校验值和新指标表。GPU1原生full127继续作为独立参考运行。同一最终DepthTrack训练模型组合的三数据集验证要求保持不变。
