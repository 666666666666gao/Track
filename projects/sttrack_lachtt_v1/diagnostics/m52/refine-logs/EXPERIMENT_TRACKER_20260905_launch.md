# M52实际预测状态采集启动报告

2026-09-05 20:26:41 CST启动，GPU0，screen sttrack_m52_policy_data，controller67241，采集Python67242。20:28核实两个进程存活，GPU0约2,443MiB、利用率59%；第一条bag04已完成1,664帧/23事件，发生41次非零候选选择，采样事件的前一选择非零数为0。后一个计数如实保留，不把整条轨迹的改选数冒充采样事件数。

这是固定M45模型的拟合数据收集；当前没有M52新权重或性能指标。63条拟合序列、1,511个原事件、93,362个实际跟踪步；不使用开发序列采集训练数据。预计约95分钟，约22:00附近检查结束情况。首条速度与该估计一致。后续按180—300秒间隔在预计结束前核对，不因日志间隔重启。

采集器记录实际前向输入，不修改模型返回或状态。在chair01和cube04各120帧共240步中，挂接与未挂接的同一M45模型输出、模板和query精确相同；记录238事件，包含2个非零previous-choice事件与1次原生模板更新。contract退出码0、状态PASS。

启动前GPU0占用1MiB；可用磁盘4,222,398,464bytes，原同规模特征参考1,635,323,302bytes，另有较小轨迹日志。GPU1原生full127仍运行，20:28进度491/1765（含原303复用），没有终止码或最终全量结果。

采集全部封存并审计后，按已冻结计划进行两组配对训练：原缓存重复对照与原缓存加实际策略状态，均fresh seed2026、20epochs、1900steps、相同参数及顺序。完整递归需要两组都跑，不只挑静态较好的组。既有H4预测裁剪原型已存在；本次只增加当前候选头的实际策略状态数据，不声称首次实现递归训练。
