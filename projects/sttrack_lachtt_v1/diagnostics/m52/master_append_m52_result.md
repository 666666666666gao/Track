

### 5.56 M52两组完整递归均未通过；现有匹配输出直接读取也未改善（2026-09-05）

23:14:29 CST，M52四个递归分片、分析和pipeline全部以退出码0完成。两组各22条、33,130帧，合计66,260帧；每组有效GT帧28,897。原生完整轨迹基线已独立重算一致，候选首次改选前与原生前缀精确相同，输出、来源、权重和逐序列封存校验通过。这里是反复使用的DepthTrack Train开发集结果，不是VOT ROB或正式测试指标。

| M52本次新增完整递归指标 | Control：原缓存重复 | Mixed：原缓存＋固定M45策略状态 |
|---|---:|---:|
| 平均IoU | 0.643872982661 | 0.630894720264 |
| 相对原生平均IoU变化 | −0.008353280515 | −0.021331542912 |
| IoU≤0.1帧 | 7,429（+32） | 8,058（+661） |
| H10失败段 | 90（+15） | 80（+5） |
| 均值改善序列 | 8 / 22 | 6 / 22 |
| 原生零失败序列新增失败 | 0 | 0 |
| 非零候选选择 | 603 | 418 |

Mixed相对等预算Control平均IoU下降0.012978262397、低重叠帧增加629，虽然H10减少10，仍未通过增量门。两组各自的原生均值、低帧和H10门也均失败；覆盖与零失败保护两项通过不能替代其余条件。`primary_pass=false`、`extra_training_control_pass=false`、`advancing_arm=null`。保持训练前冻结规则，两组都停止，不进入低22，没有事后换epoch或改阈值。

逐序列44行新指标见 `diagnostics/m52/per_sequence.csv`。Control在notebook02的均值为0.374269、低帧1,396、H10为16，Mixed分别为0.848132、76、2；但Mixed在egg降至0.551240并出现1,232个低帧，在colacan01也没有保留Control的收益。这些是完整轨迹的取舍，不足以直接判定每次首次分叉的单一原因。此前手机反例对应的mobilephone02在本轮两组都保持H10为0，也不能据此忽略其他退化。

**对状态分布假设的更新。** 原缓存与部署状态确有差异，新增采样中也有实际分叉；但本次一轮固定M45状态扩充没有改善递归，额外训练旧缓存同样没有改善。不能继续将“补上策略状态后能提高跟踪”当作已证实结论，也不能由一次固定策略、单种采样、单seed结果否定所有迭代策略训练。3,022逻辑视图仍对应1,511物理事件，36个非零previous-choice事件的覆盖限制保持原样记录。

**现有affinity能否直接使用。** 在训练结束、完整递归仍运行时预先记录了补充静态诊断方案。CPU对两组、三种缓存范围共六组输入的分类选择逐事件精确复现已记录GPU选择；没有优化器步骤、阈值拟合或运行策略改变。因果读取只用实际previous_choice作为affinity列，未匹配行沿用候选0的对照语义，不建立新的失踪状态。

| 同一590开发事件的新增直接匹配读取结果 | Control | Mixed |
|---|---:|---:|
| 因果affinity读取平均IoU | 0.439262837 | 0.438714385 |
| IoU≥0.5事件 | 267 | 268 |
| 静态救回 / 严重破坏 | 1 / 1 | 2 / 1 |
| 同一336事件子集的上一GT候选特权读取均值 | 0.735241115 | 0.741889596 |

两组因果affinity读取都低于默认候选和各自分类输出。即使在上一GT候选可用的同一336事件子集中提供这个特权列，读取也没有超过分类输出。它支持“不能直接把当前辅助矩阵接入就声称改进”，不证明矩阵完全没有互补信息，更不否定重新训练身份传播。特权列与最优结果都是离线诊断，不可部署，也不是真正的全局容量上界；各静态分母沿用GT不可用计0，与递归有效GT均值不同。完整脚本、逐事件选择和限制见 `AFFINITY_REPORT.md` 与 `affinity_readout_diagnostic.json`。

**复核与封存。** 第四次独立上下文GPT-5.5 xhigh读取44份完整轨迹和22份GT私有副本，重算全部新组逐序列、CSV及汇总值，未发现不一致或明确实现缺陷。内部完成态完整性PASS；对外结论准备度仍为WARN，因为只具有单seed、重复Train开发范围，且性能门失败。这是GPT系列Type-A建议性复核，不称跨模型家族认证。完整原文见 `EXPERIMENT_AUDIT_COMPLETED_FOLLOWUP.md`，私有逐帧副本未发布到GitHub。

M52递归结果SHA256 `92e7b0f20883d8ccee6c3911aba70f0d5bc39c5f7a0bc7804aeb868a208953aa`；affinity诊断结果SHA256 `ec3d3031b04fc635d36eb0f5927050a37385270a52e933de91a160b897bbe56e`。基础网络与两组权重仍为上一节已冻结组合，无新权重替换。

**下一项有限问题。** 模板方向先检查“同一当前搜索/query状态下，过去真实保存的模板是否产生更好候选”，保持初始模板、两模板接口和原生更新路径。先覆盖固定拟合事件，封存后才用GT分析历史视图容量及健康样本损害；只有容量明确存在，才值得在DepthTrack Train训练读取器。这与M50追加尺度触发写入不同，也不等于因果读取器或完整恢复已有效。KeepTrack官方身份表、训练入口及DAgger原文核对记录于 `METHOD_NOTES.md`；没有直接复制其代码或阈值。

23:37原生STTrack独立full127参考日志为742/1765（含既有303个SHA核验复用anchor），仍运行，无终态正式指标。当前仍无同一新模型组合的DepthTrack Test、CDTB和完整VOT结果；最终三个指标严格超过目标的要求未完成。
