

### 5.55 M52采集与成对训练完成，两组完整递归运行中（2026-09-05）

22:02确认63条拟合序列采集全部完成，1,511事件、93,362跟踪步、耗时5,586.918秒；collection/controller退出码均0。采集未打开GT/训练标签、优化器0步，运行源码未变；1,635,416,915bytes新特征及完整轨迹封存在服务器。全轨迹956次非零选择不等于采样事件的前一选择：后者36个，分布于22条拟合序列。默认模板写入1,111次，本轮没有额外尺度更新或文本。

**数据审计与状态量级。** 85份原特征、63份新特征、63份新轨迹校验后才读取既有DepthTrack Train标签，原2,101条M45监督全部复现，fit/dev按序列分离检查通过。新旧同物理时刻视图的当前标签变化148个、前一标签105个；当前包含IoU≥0.5候选的事件872与885只是拟合候选容量，不是递归救回数。

精确比较发现1,052个不同预测框。训练完成后补充描述统计：这1,052个都至少一个坐标差超过0.001px；561个中心差超过1px，271个中心差超过原预测框面积平方根的0.25倍。全事件中心差中位数0.568051px、95分位171.456811px；对称线性尺度比中位数1.004142、95分位1.897885。这是**同一时刻两条预测轨迹**的差异，不是GT运动或相邻帧位移；仅用于解释状态数据长尾，不改变训练或晋升条件。

**实际训练。** 两组均已在DepthTrack Train拟合完成：M45原始绝对几何448,739参数候选关联头，基础STTrack冻结，fresh seed2026，20epochs、1,900步。control重复原1,511事件，mixed增加同事件的策略状态；仍为3,022逻辑视图而非独立事件。两组46项初始状态张量与逻辑样本顺序摘要完全一致，46项张量均有实际更新；固定末轮权重重载输出精确相同。没有改变模型输入、默认模板策略或既有晋升门。

| M52新训练头：既有590静态开发事件 | 平均IoU | IoU≥0.5事件 | 非零选择 | 静态救回 / 破坏 |
|---|---:|---:|---:|---:|
| control：原缓存重复 | 0.446131557 | 272 | 10 | 4 / 1 |
| mixed：原缓存＋策略状态 | 0.446681499 | 274 | 14 | 4 / 0 |

“静态救回”仅指同一事件候选0 IoU≤0.1、所选候选IoU≥0.5，“破坏”反之；GT不可用行沿用既有静态实现计0，因此该均值分母也不同于递归有效GT均值。mixed对control静态均值仅+0.000549942，不能认定明显跟踪改善。两组均进入完整22条递归，各33,130帧、总66,260帧，不根据静态排序挑一组。完整递归结果尚未产生，无primary_pass或advancing_arm结论。

control权重SHA256 `0bd04b696a38c3e9388f59abfe4180bda2026c62447f2fbc1c7db26f6d7abcf8`，mixed为 `a249081ff664aa98c643f9e8b75718ab1dc8f03f4680737faec1c0a742de56ce`，各1,811,435bytes。二者均与原基础STTrack SHA256 `cacbd799115be1aaeb049cee0db89270851e3b6dd68997553b4c2c31c1104f98`组合使用；小型head文件本身不是完整模型。实际STTrackCandidateSet加载器与直接关联头在ball05真实输入上的logits/affinity精确一致，覆盖前一选择0/1/2/3，契约不读GT。

运行根目录沿用M52；screen94999.sttrack_m52_paired_train、控制器95000于22:05:05 CST启动，22:08:54数据审计、两组训练与加载契约退出码均0，已进入control第一递归分片（Python96441）。四分片预计约65分钟，约23:15附近核对完成态。GPU1独立原生full127在22:02为629/1765，尚无最终结果；不重跑已完成锚点。

新证据见 `diagnostics/m52/TRAINING_REPORT.md`、`static_metrics.csv`、两组`training_result.json`、`collection_receipt.json`、`data_audit.json`与`runtime_contract.json`。collection_receipt SHA256 `29d72848bb96e565a84b6a7f7b6c29641d3b46b908ee9039c5b8035f90cebd67`；data_audit SHA256 `2fe646d4745bbe2587b27f31ce4967cba91027f0616ee400a86e406749c424e0`。第三次独立上下文GPT-5.5审阅已确认完成态产物，无明确完整性缺陷；总体WARN仅保留未完成递归/公开结果及有限Train范围等限制。这仍是GPT系列Type-A建议性复核，不称跨模型家族验收。

下一步等待两组完整递归并按训练前冻结门判定；若晋升，再以同一新模型组合进入低22，明确改善后才评完整VOT和另外两测试集。当前没有新的正式VOT、DepthTrack Test或CDTB指标，项目目标仍未完成。
