# M52采集、成对训练完成；完整递归验证运行中

截至2026-09-05 22:08:54 CST，采集、数据审计、control训练、mixed训练、实际跟踪器加载契约均已完成，相关退出码均为0。两组完整递归仍在运行，没有M52递归收益、晋升结果或公开测试集新指标。

## 实际数据与模型

固定M45策略在DepthTrack Train原63条拟合序列连续运行93,362步，采集原1,511个物理时刻，耗时5,586.918秒。采集没有打开GT或训练标签，优化器步数0，默认模板更新1,111次；没有M50额外尺度更新、M48准入门或文本。1,635,416,915bytes特征和完整轨迹保留在服务器，逐序列校验值在collection_receipt.json。

完整轨迹共956次非零候选选择；固定采样时刻只有36个事件的前一候选编号非零，分布于22条拟合序列。两个数字的分母不同。数据审计核对85份原特征、63份新特征和63份新轨迹后才打开既有训练标签。原2,101条fit/dev标签与M45记录完全一致。

每组仍为M45原始图像归一化几何的448,739参数候选关联头，基础STTrack冻结；首帧/默认动态模板、局部RGB-D候选特征、原始几何与前一选择输入不另加新模块。control使用原1,511事件重复为3,022视图；mixed使用原事件加同物理时刻的策略状态。两组均在DepthTrack Train拟合，fresh seed2026、20epochs、1,900步、相同初始46项状态张量和逻辑样本顺序。每组46项张量均实际改变，权重重载输出精确一致。

训练器按每个视图自己的候选框重新计算监督，而非把候选0与最终选择框混淆。新旧视图的当前标签有148个不同，前一标签有105个不同。包含IoU≥0.5候选的当前事件数为872与885，这是拟合缓存的候选容量，不能称为递归救回13次。配对时刻当前GT不可用211个、前一GT不可用305个，静态监督沿用已有None规则。

## 预测状态差异的量级

原审计以精确数值不等统计到1,052个不同预测框。训练完成后补充运行describe_state_difference.py，比较同一物理时刻原生轨迹的框与固定M45策略轨迹的实际输出框；这不是GT运动，也不是相邻帧位移。本统计只描述已采集状态，不修改训练或晋升门。

| 同时刻的两条预测轨迹差异 | 事件数 / 1,511 |
|---|---:|
| 任一框坐标差超过0.001px | 1,052 |
| 中心距离超过1px | 561 |
| 中心距离超过原预测框面积平方根的0.25倍 | 271 |

全部事件的中心差中位数0.568051px，95分位171.456811px；对原预测框尺度归一化后为0.010783与3.655107。对称线性尺度比中位数1.004142，95分位1.897885。因此状态差异有明显长尾，不能把所有事件都说成大幅漂移，也不能只用36个非零前一选择概括全部路径变化。

## 新增静态开发指标

下面仅为既有DepthTrack Train开发折的590个缓存事件，原生候选0均值为0.440273792、IoU≥0.5事件268个。GT不可用行沿用既有静态实现计0，因此分母也不同于完整递归的有效GT帧均值。静态“救回”指同一事件从候选0 IoU≤0.1变为所选候选IoU≥0.5；“破坏”反之，不代表多帧生存。

| 新训练头 | 静态平均IoU | IoU≥0.5事件 | 非零选择 | 静态救回 | 静态破坏 | NONE输出 |
|---|---:|---:|---:|---:|---:|---:|
| control：重复原缓存 | 0.446131557 | 272 | 10 | 4 | 1 | 322 |
| mixed：加入策略状态 | 0.446681499 | 274 | 14 | 4 | 0 | 272 |

mixed比control静态均值高0.000549942，差异很小，不能由此宣称已经获得明显跟踪改善。两组都继续完整递归22条开发序列，各33,130帧，共66,260帧；不只评估静态较好的mixed。

## 权重与执行证据

| 项目 | control | mixed |
|---|---|---|
| 权重相对路径 | control/geometry_final.pth | mixed/geometry_final.pth |
| SHA256 | 0bd04b696a38c3e9388f59abfe4180bda2026c62447f2fbc1c7db26f6d7abcf8 | a249081ff664aa98c643f9e8b75718ab1dc8f03f4680737faec1c0a742de56ce |
| bytes | 1,811,435 | 1,811,435 |
| 训练及静态检查耗时（秒） | 63.297 | 64.115 |

两组共享基础STTrack权重SHA256 `cacbd799115be1aaeb049cee0db89270851e3b6dd68997553b4c2c31c1104f98`，不是只凭小型head文件就构成完整模型。逻辑样本顺序SHA256均为 `b6981fcc0dbd0dda1b8229173dee33cc758cf41a7654c6dfa0e19c1444d348cd`。

实际STTrackCandidateSet加载器与独立候选头对ball05真实捕获输入的logits和affinity精确一致，覆盖previous_choice为0、1、2、3；此契约不读GT，不是跟踪性能测量。

运行根目录 `/root/autodl-tmp/sttrack_m52_policy_state_augmentation_v1_20260905`。训练控制器95000、screen94999.sttrack_m52_paired_train，于22:05:05 CST启动；22:08:54已进入control第一递归分片，Python96441。参考M51两分片分别约982与980秒，M52四分片预计约65分钟，约23:15附近核对终态；不因日志间隔重启。

collection_receipt SHA256为 `29d72848bb96e565a84b6a7f7b6c29641d3b46b908ee9039c5b8035f90cebd67`；data_audit为 `2fe646d4745bbe2587b27f31ce4967cba91027f0616ee400a86e406749c424e0`；运行绑定和训练前晋升条件不变。只有完整递归的primary_pass、extra_training_control_pass与advancing_arm能决定下一步；当前尚无这些结果。低22、全VOT、DepthTrack Test和CDTB均未开始使用这两组新权重。GPU1独立原生full127在22:02记录629/1765，没有最终结果。

完成态独立上下文复核见EXPERIMENT_AUDIT_TRAINING_FOLLOWUP.md。审阅者确认本地已有采集、封存审计、两组静态训练、加载契约和状态描述产物，未发现明确完整性缺陷；总体WARN保留，原因是完整递归及公开结果尚缺。这仍是GPT系列Type-A建议性复核，不是跨模型家族验收。历史启动报告和前两轮审查按当时证据保留，不用本节结果改写过去状态。
