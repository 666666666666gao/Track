

### 5.53 M51完整递归未通过；M52实际策略状态数据开始采集（2026-09-05）

**M51终态。** 固定权重完整跑完两个分片、22条开发序列、33,130帧，训练、接口检查、两个递归分片、分析及controller退出码均0。没有在结果后改变几何表示、权重或阈值。完整性通过，性能门未通过。

| 本次M51新增完整递归指标 | M51 | 相对原生STTrack变化 | 相对同训练设置M45变化 |
|---|---:|---:|---:|
| 平均IoU | 0.622033217091 | −0.030193046086 | −0.062069157188 |
| IoU≤0.1帧 | 8,365 | +968 | +2,177 |
| H10持续失败段 | 85 | +10 | +5 |
| 均值改善序列 | 9 / 22 | — | — |
| 原生零失败被破坏 | mobilephone02_indoor | 新增1条 | — |

本轮累计723次非零候选选择。全局均值下降3.02个百分点，且低帧、H10均恶化；只改变几何表示相对M45还下降6.21个百分点。因此停止M51相对坐标替换版本，不进入公开低22。M49测得的真实运动与预测跳动差异仍成立，但将该线索直接转换成这一输入替换并没有得到连续跟踪收益。当前表示依赖上一选择作为参考，错误参考是否放大后续判断仍只是待验证机制，不能由总体指标直接认定。

全部逐序列新结果见 `diagnostics/m51/per_sequence.csv` 与 `RESULT_REPORT.md`；原生基线各项指标已用完整轨迹独立重算一致，首次改选之前的输出轨迹精确匹配。M51结果SHA256 `79b16e50f1638c0aeb2f39ec9d08e20c0ce748b270fb9b6938a4863c66d78fb9`；权重仍为上一节固定的 `7eb1cc4e…fbf85`。本轮没有正式VOT/DepthTrack Test/CDTB新指标。

**M52依据与模型。** 现有M44/M45缓存全部来自原生STTrack预测路径；2,101个训练/开发记录的previous_choice均为0。实际关联模型会改选，之后搜索、模板、query、候选与前一选择都可能与训练缓存不同。这个差异有源码和运行记录支持，但尚未证实是主要根因。项目已有H4 predicted-crop工程和训练原型，不能称为从未做递归训练。

本轮保留表现更好的M45原始图像归一化几何、局部RGB-D特征和448,739参数候选集关联头，不使用M51相对变换，不叠加M48门、M50额外模板更新或文本。改变的是训练数据所处的预测状态。

**采集设计。** 固定M45的DepthTrack Train已训练head，从初始化连续运行原63条拟合序列，记录原1,511个固定采样时刻，运行至各自末事件共93,362个跟踪步。只使用fit folds2/3/4；开发fold5不采入拟合。采集时不读GT或训练标签，不依据GT重置或选择目标。直接捕获实际送入关联头的current/previous/reference tokens、geometry、scores、previous_choice；同时封存候选0的原生框、实际输出框和整段选择轨迹。

新缓存沿用字段public_bbox明确表示当前策略状态下的候选0原生解码框，实际输出另存selected_bbox，避免将已选择的其他候选误当作候选0监督。记录器只观察原始前向返回值。chair01与cube04两条拟合序列各120帧，挂接记录器与普通M45路径的输出、模板、query精确相同；238个记录覆盖2个非零previous-choice及1次原生模板更新，contract状态PASS、退出码0。

| M52当前执行项 | 范围与状态 |
|---|---|
| 固定策略采集 | 63条拟合 / 1,511事件 / 93,362跟踪步，运行中 |
| 运行位置 | GPU0；screen sttrack_m52_policy_data；controller67241；Python67242 |
| 启动时间 | 2026-09-05 20:26:41 CST |
| 首条实际产物 | bag04：1,664帧、23事件、41次改选；采样事件的前一选择非零数0 |
| 当前M52优化器步数 / 新权重 | 0 / 尚无 |
| 预计采集时长 | 约95分钟，约22:00附近核对终态 |

首条轨迹的41次改选不等于23个固定采样事件有41次非零前一选择；实际计数0如实记录。后续必须审计全部新缓存的状态变化、候选容量及前一选择分布，不能预先宣称每个采样事件都补到了新的选择状态。

启动前GPU0仅1MiB，可用磁盘4,222,398,464bytes；原同规模拟合特征参考1,635,323,302bytes，另有小型轨迹文件。20:28实际核对GPU0占用2,443MiB、进程存活，首条耗时100.70秒，与总时长估计一致。GPU1独立原生full127继续运行；20:28为491/1765（含303条复用），无终止码或最终结果。采用接近预计结束时、180—300秒间隔的观察，不因日志间隔重启。

**冻结的配对训练计划。** control把原1,511拟合记录重复为3,022个视图；mixed使用原1,511记录加相同物理时刻的1,511个新策略状态。两组均fresh seed2026、同结构/参数量、M45 default-priority标签规则和损失、20epochs、batch32、1900steps、AdamW lr0.0003、weight_decay0.01、clip1.0、固定末轮；初始参数和样本索引顺序必须一致。每个视图按自己的候选框与同一物理帧GT生成标签，mixed使用真实previous_choice。新增记录不等于新增独立物理帧，配对control用于区分新增状态与额外训练步数的作用。

采集全部封存并审计后再训练；两组新权重都必须完整递归既有22条开发序列。mixed除了通过原生均值至少+0.01、低帧减少、H10不增加、至少3条改善及原生零失败保护，还须相对配对control均值提高且低帧/H10不增加，才支持状态数据的增量，并进入冻结低22。静态开发590事件仍只作诊断。这不是通过裁剪链端到端反传，仅是一轮固定策略状态的数据扩充；不先假定它有效。

M52根目录 `/root/autodl-tmp/sttrack_m52_policy_state_augmentation_v1_20260905`；协议SHA256 `30a1cff2f81ff898df272ddbb3ac57589c40686b65b671fc66a8d9f3983c07c8`；采集源码 `tools/collect_sttrack_m52.py` SHA256 `5145ed01a6569b5ae3bde68ba65da6d2339a4773dd1a373c516f9cdb125fc0bf`。既有运行源码未修改。完整VOT门和同一DepthTrack训练模型组合的三个测试集要求继续保留，项目目标仍未完成。
