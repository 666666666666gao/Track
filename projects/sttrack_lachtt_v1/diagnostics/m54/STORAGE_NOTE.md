# M54缓存存储修正

排队后、长采集和训练均未开始时，2026-09-06 02:50 CST实测持久实验盘只余2,445,430,784字节，而240窗口契约缓存约48MB，按10,615窗口预计需约2.13GB。现有原生公开参照仍需要输出空间，故暂停了本轮等待进程174609；没有停止原生评测、没有删除既有实验数据。

修正只改两个源代码行：正式采集写入、训练读取同一个spec指定的`/dev/shm/sttrack_m54_features_v1_20260906`。共享内存挂载可用约90GiB，实际系统可用内存约690GiB。模型、采样、监督标签、优化参数、递归状态规则和晋升门槛不变。唯一final权重、日志、轨迹和结果仍保存在原持久实验根。

第一轮spec、源代码的两行旧版本、契约、排队观察及审计完整保存在[initial_storage_binding](initial_storage_binding/)。第一轮spec为`2ec9bd39c4b17ab37383e5e891ebb162b69a844137745916deff8dd01283c08b`，修正后为`1ca1387e6eb33c897e12d5e0c10b746d48257e7745906c431e8ac857f63d7267`。修正后重新执行两个契约：各240步，默认路径完全一致、不同读取90窗口、强制不同输出7次、各3次动态写入，均exit0；参数58,923。两次耗时79.31和79.52秒。

共享内存中的特征会在服务器重启后消失。为保留可审计的原始特征，新增独立的本机归档程序[archive_features.py](archive_features.py)：首次在2026-09-06 05:00 CST检查采集终态，之后每240秒检查；只在collection.exit=0且spec/receipt匹配后下载全部63份封存特征到用户本机私有工作区，逐文件验证大小和SHA256，再写archive_receipt.json。它不读取GT，不修改或删除服务器缓存，不进入模型和指标计算。原生运行脚本仍负责采集—训练—递归—分析，持久归档由单独的本机进程执行。

存储修正复核指出两点：归档需要实际实现，以及重新排队前应确认新契约完成。前者由上述独立程序执行；后者在重启等待队列前核验两个新JSON的PASS、当前spec SHA与退出码0。队列无需增加“等待正在进行的契约”分支，因为本次只在契约已完成后重新排队；正式collector仍会再次核验完整契约绑定。

本机归档进程于2026-09-06 03:09:53 CST启动，PID 48148；03:11核实进程存活、日志显示等待05:00首次检查，本机可用磁盘约13.33GB。修正后的远程队列于03:11启动，03:11:44核实PID 182350存活，尚未长采集或训练。对应[archive_launch.json](archive_launch.json)、[execution_binding.json](execution_binding.json)及[queue_observation.json](queue_observation.json)。当前没有archive_receipt；启动等待进程不能称为特征已归档，也不能称训练已完成。
