# M54读取器训练完成，完整开发递归已启动

2026-09-06。M54两种模板组合的读取器已在DepthTrack Train完成固定训练；当前还没有完成态开发递归或公开评测结果。架构与原EXPERIMENT_PLAN一致，新增58,923个参数，STTrack底座冻结，语言关闭，默认动态写入条件保留。

## 完成的采集与训练

| 项目 | 实际结果 |
| --- | ---: |
| 拟合序列 | 63 |
| 原生采集跟踪步 | 93,362 |
| 封存窗口 | 10,615 |
| 有效GT训练窗口 / 无效GT排除窗口 | 9,729 / 886 |
| 初始化组合为正标签的窗口 | 143 |
| epochs / optimizer steps | 20 / 6,100 |
| 第一epoch / 最后epoch交叉熵 | 0.421167318 / 0.036099138 |
| 实际发生变化的state_dict张量 | 22 |
| 最终权重严格回读 | logits逐元素一致 |

采集完成后才打开拟合GT，886个无效窗口未参与优化。6,100步来自20×ceil(9729/32)，与20行训练日志及最终checkpoint记录一致。采集的63条逐序列bbox/score最大回放误差均0，共1,131次默认动态写入；9,861个采样窗口中两种组合解码框不同。后一个计数只表示输出不同，不表示另一组合更正确。

## 同一批有效拟合窗口的静态回算

| 量 | 当前模板组合原生top1 | 训练读取器选择 |
| --- | ---: | ---: |
| mean IoU | 0.702840799 | 0.713070649 |
| 选择初始化重复组合的窗口 | 0 | 142 |
| 原IoU≤0.1、选择后IoU≥0.5 | — | 98 |
| 原IoU≥0.5、选择后IoU≤0.1 | — | 0 |

mean IoU增加0.010229850，约1.022985个百分点。这是对9,729个训练窗口的静态回算，包含被训练使用的数据；98次恢复不是98条轨迹或VOT anchors，也不证明健康开发轨迹受保护。原生状态缓存仍没有覆盖所有读取器自身分叉后的状态，完整递归门继续检验这一限制。

## 权重、归档与后续实际状态

最终读取器为服务器运行目录下reader_final.pth，SHA256为`53374903f5c7314dbc08c857d9ac6d56f2adc914908e62831176fb7c2b7d0c05`；本机同名文件已下载并核对，保存在`.codex_remote_staging/m54_training_completed_20260906/`。基础权重SHA256仍为`cacbd799115be1aaeb049cee0db89270851e3b6dd68997553b4c2c31c1104f98`。spec SHA为`1ca1387e6eb33c897e12d5e0c10b746d48257e7745906c431e8ac857f63d7267`，采集凭证SHA为`708d1b326cdca803cb7813a63ff48ab82943bdf5a8fe2bb5610136c489091e63`。

本机在05:44:10 CST完整归档63份特征，共2,132,202,561字节，之后又逐文件重新核对大小和SHA。归档不包含GT，也没有删除服务器共享内存中的源特征。完整归档凭证与下载绑定保留在本目录。

05:45:29 CST实际观察到控制器203098与递归进程244873存活，采集和训练exit均0，递归/分析尚无退出码；首条bag05_indoor完成889帧、15次初始化组合读取，用时79.09秒。剩余21条继续完整运行，共计33,130输出帧/33,108跟踪步。由首条吞吐估计全量约50分钟，完成时间约06:30，后续按实际进展修正；该估计不是完成凭证。

完整输出封存后才计算22条开发的GT指标、全部晋升条件及原生成功序列保护。通过才进入低22，之后同一base＋读取器权重＋策略验证三个完整数据集。当前没有依据修改标签、训练预算、阈值或选择其他epoch。

## 本阶段验证范围

已核对采集/训练日志与凭证、63条拟合名称及与22条开发的互斥关系、7份冻结源码、最终权重哈希、20epochs/6100步，以及本机63份持久化特征。服务器训练器已严格回读并比较logits。`completion_checks.json`明确这是执行器侧的来源和算术一致性核对；本阶段没有另行独立重算全部拟合IoU，也没有宣称完整递归通过。
