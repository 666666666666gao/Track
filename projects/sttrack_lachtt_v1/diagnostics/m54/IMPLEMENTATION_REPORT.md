# M54实现与实际契约

2026-09-06。新增两模板组合视觉读取器58,923参数，STTrack底座冻结。七个新增源文件实现读取头、单路径递归、数据采集、训练、完整开发评测及契约；未修改正在运行的原生参照所绑定的已有源码。完整设计与停止条件见[EXPERIMENT_PLAN.md](EXPERIMENT_PLAN.md)。

每个候选来源是一次真实的不同模板前向，而非同响应图中的另一个NMS峰。读取器联合自身top1的4×4 RGB-D RoI、初始化与上一目标参考、两张响应图、几何和分数评分。局部跨注意力允许参考与候选的不同位置对应；本轮不宣称旋转等变，也不将整个模块收益归给某个独立子模块。新增部件必须在DepthTrack Train训练后再验证，契约中的随机头不用于报告跟踪性能。

## 完成的运行检查

| 检查 | 实际范围 | 结果 |
| --- | --- | --- |
| 默认读取对独立原生 | chair01/cube04共240步，240缓存窗口，3次模板写入 | bbox、score、模板、query和模板图像完全一致 |
| 不同读取确实被计算 | 上述240窗口 | 90个窗口中两组合输出框不同 |
| 统一提交所选状态 | 另240步，16次固定初始读取 | 与独立原生临时替换读取槽后的完整状态一致 |
| 确实改变输出的固定读取 | cube04中7次；chair01中0次 | 未把16次读取全部当作16次不同预测 |
| 新头接线 | CPU梯度与视图交换；GPU实际choose_view输入路径 | 证据分支梯度有限且活跃，初始化分数与原生log分数一致，输入路径一致 |

缓存存储修正后，两组重新执行，分别耗时79.31和79.52秒；contract、runtime_contract及契约控制器退出码均0。原始证据见[contract.json](contract.json)、[runtime_contract.json](runtime_contract.json)。第一轮79.76/80.41秒的证据与旧规范完整保存在[initial_storage_binding](initial_storage_binding/)。这些是执行正确性检查，没有GT、优化器更新或新的性能分数。

全部帧编号沿用代码的零起点索引：`frame=10`对应第11张图像。契约固定时刻不进入部署策略。每次读取独立复制帧前query，输出框、分数、新query及上一RoI来自同一次前向；读取初始视图不永久替换动态记忆，定时更新仍使用所选预测。

## 已固定的长任务

拟合数据为63条序列的既定事件与每10步定期窗口并集，共10,615窗口，主路径重放93,362步。所有缓存封存后才打开拟合GT，无效GT不监督。标签仅在当前IoU<0.5且初始组合IoU≥0.5时选择初始，原生已正确时保留当前。读取头单seed训练20完整epochs，AdamW lr/weight_decay均0.0001，batch32，clip1；步骤按有效窗口数固定计算，保存唯一final权重。

训练后完整运行既有22条DepthTrack Train开发序列，随后用既有连续IoU/H10计算并应用全部原生改进与成功序列保护门。开发集已反复使用；此版本也未补充自身策略状态数据，不宣称解决M52揭示的问题。没有独立原生并行保护分支，选择当前组合不会恢复此前已分叉的轨迹。

GPU0长任务等待原生DepthTrack Test/CDTB两项跟踪退出0。存储修正后的等待队列于2026-09-06 03:11 CST实际重启，03:11:44核实PID 182350存活，尚无长采集或训练。首次检查定在03:30，此后每240秒检查；采集、训练、递归、分析按退出码顺序执行。公开评测不会自动绕过晋升门。排队不等于已开始采集或训练，实际状态以[execution_binding](execution_binding.json)、[queue_observation](queue_observation.json)和各阶段退出文件为准。

独立只读[第一轮实现审计](initial_storage_binding/EXPERIMENT_AUDIT.md)未发现具体模型代码缺陷，确认了GT隔离、训练/运行接线、query复制、完整状态提交和原门槛；总体WARN限定在尚无训练或递归结果。后续[存储复核](EXPERIMENT_AUDIT_STORAGE.md)指出实际缺少特征归档步骤，以及新版契约未完成时提前排队可能失败；原文保留，不改写成通过。当前已实现并启动独立本机归档进程，并在新版契约完成、核对PASS及当前spec SHA后才重启队列，详见[STORAGE_NOTE.md](STORAGE_NOTE.md)。截至上述观察时刻没有特征归档完成凭证。这些都是同GPT家族Type-A建议性审计。

审计所需的原生源码已有公开快照[sttrack.py](../m52/source_snapshots/sttrack.py)，SHA256为`d67d551a612b80cee5b19a00f6fecd5d0f7ed0c907e800f452873afd684cc58f`，与本次读取的服务器源码一致；overlay按README要求叠加到固定upstream，不能单独当作完整STTrack仓库运行。

第三次[范围限定的补充复核](EXPERIMENT_AUDIT_CLOSURE.md)重新计算当前绑定hash并检查归档实现，确认前两项存储问题对本次执行已处理，未发现新的具体完整性缺陷；总体仍为WARN，因为没有归档完成、训练权重或性能结果。队列脚本本身仍依赖启动前核验；后续重新运行也需完成同一核验，不能将本次操作检查写成脚本已自行验证全部条件。

当前spec SHA256：`1ca1387e6eb33c897e12d5e0c10b746d48257e7745906c431e8ac857f63d7267`。两行存储修正发生在第一轮契约结束后、长采集开始前；修正后的代码和规范在第二轮契约执行期间未修改。
