# M67配对训练完成，完整递归验证已启动

2026-09-07。M67 Control于14:10:54 CST完成训练，Support于14:16:01 CST完成训练，两组training退出码均为0。原控制程序随后自动启动两组开发22完整递归，未人工替换权重或重启训练。

14:18:45 CST完成配对训练产物核验。复用已经冻结并发布的`audit_m67_completed_20260907.py::training_artifacts()`，本次只提前调用训练产物部分；完整开发集与原冻结门的审计仍由原后续队列执行。

## 两组训练完成态

结构、输入与训练设计沿用前文M67，未增加新网络。两组均为289,154个可训练适配器参数、同一初始化、130条DepthTrack Train拟合序列；支持损失权重分别为0和0.1。

| 项目 | Control | Support |
| --- | ---: | ---: |
| 完成训练序列 | 130 | 130 |
| 完整预测链跟踪调用 | 186,694 | 186,694 |
| 优化步 | 5,798 | 5,798 |
| 有效GT监督帧 | 174,479 | 174,479 |
| 封存取样状态 | 3,917 | 3,917 |
| 支持损失权重 | 0 | 0.1 |
| 训练耗时，秒 | 12,695.479 | 13,002.876 |
| 最终适配器权重SHA前缀 | 7a8907a45cec | 2b0fa1c11694 |

完整权重SHA分别为Control `7a8907a45cec672f11563ad7cfaee21cb64acfee3fa13e854621c146cb8d058e`、Support `2b0fa1c11694790af7585277cede084edd8866d32596ea2257b014839882233e`。实际文件保存在原训练目录各组`final.pth`，未将权重上传GitHub。

## 核验范围与边界

实际加载了每组initial、latest与final权重，核验模型键、289,154个有限参数、最终状态、完整训练计数，以及所有优化器状态的5,798步。各组final与latest模型张量逐项一致；两组初始化张量逐项一致，最终模型确实不同。

核对了原训练脚本、160个集成源码、文本缓存、原生权重和配置哈希；核对130条序列顺序、训练GT哈希及有效帧计数、全部3,917个取样状态与定时模板写入记录。保留已有toy07截断数据契约，没有更改数据集。

Support的3,660个有效取样状态还通过支持监督字段检查：损失非负且有限，正负位置计数与原监督定义一致。这里没有进行新的训练或GPU前向。

底座参数和buffer不变的证据来自原训练前后断言及所绑定代码，本次没有重新执行整段训练。后续完整审计仍会校验最终递归产物与原门，不以本次训练检查代替性能验证。

## 训练访问状态，只作拟合集记录

| 训练记录 | Control | Support |
| --- | ---: | ---: |
| 有效GT中心在当前搜索区域内 | 151,756 | 155,482 |
| 有效GT中心在当前搜索区域外 | 22,723 | 18,997 |
| GT无效 | 12,215 | 12,215 |
| 核验的定时模板写入 | 1,768 | 1,870 |

这说明两组训练访问了不同的递归状态。权重在训练过程中持续变化，以上不是最终权重的固定模型测试；中心在crop内也不等于框正确或身份正确。不能把3,726个状态计数差异写成已救回轨迹，也不能据此预测正式VOT收益。模板阈值仍为原50步、score>0.75。

## 当前实际验证进程与下一步

14:18:45实查Control递归PID475087、Support递归PID475088，start_ticks均为4578650390；命令行为原`run_recursive.py --arm control/support`，工作目录、进程状态已核验。完整开发集结果当时尚未生成。

原后续诊断队列PID471187及M71等待进程PID474465均实际存活，继续按240秒间隔检查。新审计未改变其源文件、队列、模型或门槛。

后续首先完成开发22全部轨迹，报告按帧/序列等权IoU、低重叠帧、H10和成功序列保护。按M67原11项条件判断，不能用训练状态计数或少数已结束序列替代；通过后再执行既定同权重内容对照。M69、M68、M70及M71继续保持诊断用途和原顺序。没有新正式DepthTrack Test、CDTB、VOT指标，没有新的独立模型审阅通过。

实际磁盘空闲约2.238 GB；本次未删除权重。训练与验证均继续使用既定底座和适配器；两个Qwen保留策略不变。

训练审计结果SHA：`80d72148ce467fa7d09d18e7a1c6941df2045eb1f3f21b2c96201109d8b7283a`。
本次调用源码SHA：`0a8550987d9c9ecb360e3ec92bf1db025ab621378897f2e787a06759637d59bc`。
复用冻结审计器SHA：`1773887deb3be27aa11a4409aa77bd7581f32b8d891ecab2a03454d277e769bf`。
