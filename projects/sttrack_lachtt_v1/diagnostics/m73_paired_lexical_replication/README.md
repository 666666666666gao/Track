# M73：两seed类别与空文本配对训练已启动

M72固定M67 Control权重已获得原类别相对空词/替换类别的明显增量，结果见§5.132。本节启动新的前瞻配对训练，检验收益能否跨初始化复现。M65/M67原冻结门及M72仅诊断性质均保持原结论；本节不是把旧失败实验更名后晋升。

### 架构与唯一主要干预

使用原M67 Control实际结构：冻结原生STTrack的ViT/BSI、TSG、Mamba与Center Head，在TSG/Mamba之后、Center Head之前训练289154参数的密集语义残差适配器。保留首帧RGB-D 4×4实例参考、5个文本槽及固定零logit/零value的Null项。Null仍不宣称已校准成语义拒绝或目标不存在概率。

双方`use_text=True`，初始化及网络参数量相同。类别组保留原自动类别槽0，属性有效槽为空文本；空文本组把所有有效槽换为CLIP空文本向量。原mask、槽数和padding保持一致，空向量不等于零向量。适配器输入位置与结构不变，两个组的支持度辅助损失均为0，直接复用原生定位损失及因果训练器。160个模型源码逐文件与M67一致。

正常运行仍使用128模板、256搜索、factor-4、初始化与动态双模板、固定历史query窗口和每50帧且原始响应严格大于0.75的默认更新。没有新在线caption、重检测器、候选关联头或质量分数乘法。真实词义可能通过未来预测crop/query/模板产生总轨迹效应，这属于要检验的效应。

### 预先固定的配对、预算与选择规则

| seed/组 | 训练文本内容 | 拟合序列 | 一轮训练跟踪调用 | 可训练参数 | 用途 |
| --- | --- | ---: | ---: | ---: | --- |
| 2027 Category | 原自动类别 | 130 | 186694 | 289154 | 预先指定的唯一候选seed |
| 2027 Empty | 全有效槽空词 | 130 | 186694 | 289154 | 同初始化、同预算对照 |
| 2028 Category | 原自动类别 | 130 | 186694 | 289154 | 复现 |
| 2028 Empty | 全有效槽空词 | 130 | 186694 | 289154 | 复现对照 |

两个seed各自生成独立随机初始化，同seed两组使用逐字节相同的初始checkpoint；两seed之间的初始化不同。序列顺序沿用既定130条顺序，不随seed重新洗牌。四组总训练跟踪调用746776，随后四组各进行22序列/33108次跟踪调用的完整递归验证。每个seed的两组在两张3090上并行，两个seed依次运行；历史实测估计整队列约8—9小时。

优化采用AdamW，学习率1e-4、weight decay1e-4、32观察帧梯度累积、有效监督帧平均和梯度裁剪1。每条完整序列只用首帧GT初始化，后续自己的预测框决定下一帧crop，query和默认模板正常递归；当前预测/状态提交后才计算GT定位损失。GT invalid无损失但仍推进状态，中心在crop外使用全负中心监督。状态detach，不经时间或离散裁剪反向传播。既有toy07长度截断契约保持不变。

两个seed都将完成，第一seed性能门失败不会取消第二seed；执行错误则停止队列并保留日志，不因观察超时重启。seed2027 Category的最终权重预先指定为唯一候选，不按结果挑最好seed或中间checkpoint。

### 实际检查与冻结结果

两个seed的实际GPU检查分别完成：每组101帧零残差预测的bbox、score、query和模板与独立原生路径完全一致，包括第100帧模板写入；每组随后在chair01拟合序列运行96帧、3次优化，RGB/Depth/text分支有有限非零梯度，底座全部参数与buffer哈希不变。同输出上的零系数监督与原定位损失逐项一致。所有短测权重丢弃，没有进入正式训练。

| 检查 | seed2027 | seed2028 |
| --- | --- | --- |
| 同seed两组初始参数一致 | 通过 | 通过 |
| 每组101帧原生状态保真 | 通过 | 通过 |
| 两组共192次同输出原损失检查 | 通过 | 通过 |
| 每组96帧/3次优化与底座冻结 | 通过 | 通过 |
| 冻结后完整递归入口preflight | 通过 | 通过 |

这是执行器的真实数据/数值检查，不是独立模型审阅PASS，也不把不同优化轨迹要求为逐位相同。原已观察到的GPU数值非确定性仍需通过完整配对结果如实报告。

### 已启动的真实训练及监控

2026-09-07 17:25（北京时间）启动持久screen `m73_lexical_replication_20260907`，先seed2027 Empty/GPU0与Category/GPU1。两进程已按PID、启动时间、argv、cwd和GPU占用实际核验，随后从已完成序列日志检查真实优化进度。

| 组 | 已完成拟合序列 | 已完成跟踪调用 | 已完成优化步数 | 状态 |
| --- | ---: | ---: | ---: | --- |
| 2027/category | 1/130 | 2575 | 81 | 已核验训练进程存活 |
| 2027/empty | 1/130 | 2575 | 81 | 已核验训练进程存活 |
| 2028/category | 0/130 | 0 | 0 | 持久队列后续阶段 |
| 2028/empty | 0/130 | 0 | 0 | 持久队列后续阶段 |

该进度快照UTC：`2026-09-07T09:29:45.621757+00:00`。剩余磁盘约1.988 GB，两个Qwen模型分片均实际核验完整保留。本轮仅保存小型adapter初始化、latest和固定final，不复制视觉底座权重，不删除Qwen。

后续检查优先安排在预计阶段结束前几分钟。需要连续等待时使用180—300秒间隔，不对长训练频繁重复SSH查询。

### 完成后如何判定，以及正式评测边界

每个seed单独使用继承的11项要求：Category按帧均值至少超过原生0.002、同预算Empty0.001，序列等权均值、低重叠帧和H10均不弱于原生与Empty；保护原生、当前Empty的零H10序列，并保留M65 Control的既有五条保护序列。两seed都通过后才准备固定新权重的类别/空文本/替换类别内容对照，不根据结果改阈值。

两个seed都来自已反复使用的Train开发22，仅检验前瞻训练重复性与文本增量，不能称独立未见测试，也不能用两次结果宣称总体统计显著。若内容与完整递归检查通过，接着做同一权重的OPE/TraX接口一致性，再进入VOT低22开发验证；通过后才可能使用同一底座、adapter、文本协议和运行策略完成DepthTrack Test、CDTB和完整VOT。当前尚无M73完整开发指标或新的正式三数据集结果。
