# M73同一最终权重的OPE/TraX与低22入口准备

M73原四组训练及§5.134后台完成核验继续运行。本节完成后续同一最终权重的OPE/TraX和低22入口准备，尚未运行任何新的模型评测，也未改变训练、原生模板更新或既定门槛。

### 最小接口改动与实际CPU检查

M73与M67 Control使用相同`semantic_spatial_support_v1`结构。复用M67的初始化文本路由、运行时、OPE入口、VOT入口及M39桥接五个文件，逐字节一致，不新增预测头或兼容分支。实际候选模型仍是冻结STTrack底座＋289154参数密集适配器，推理使用seed2027 Category final，辅助支持损失权重0。

新准备仅绑定M73训练来源与候选选择规则。文本协议保持首帧自动描述、类别保留、属性有效槽置CLIP空词、原五槽/mask/padding；协议元数据改为M73来源。既有低22的303条初始化类别向量原样复用，没有重新生成caption、embedding或事后修正类别。

| 检查对象 | 实际检查量 | 本轮结果 |
| --- | ---: | --- |
| Train原始初始化图像哈希、框、文本槽与M73类别bank | 152条 | 全部匹配 |
| 新开发集初始化路由与M73直接递归输入 | 22条 | bbox/token/mask/empty逐项一致 |
| 既有VOT低22初始化图像与观察键 | 303条 | 全部匹配，词向量内容未改 |
| 公用运行接口 | 5个源码文件 | 与M67逐字节一致 |
| 新候选、保真、低22入口 | 3个Python＋2个shell | 目标Python编译与bash语法检查通过 |
| 在训练完成前请求绑定/保真准备/低22准备 | 3个实际调用 | 均被完成条件阻止，无评测产物副作用 |

这些是CPU数据与入口检查，不是新模型的GPU保真证明。当前没有读取M73 final，没有创建候选bundle、OPE/TraX结果或low22_run，没有打开后续GT；新跟踪调用和优化步数均为0。初始化图像/初始框的路由检查不等于新一轮测试集性能评估。

### 最终权重产生后的执行条件

候选绑定要求原M73两seed各自11项开发条件全部通过、完成产物核验通过、固定seed2027 Category权重内容对照通过，并检查原后台队列正常结束及对应receipt哈希。只允许该预先指定final；不会改用seed2028或中间权重，也不会借用历史M67 Control通过的内容结果。

获得资格后，计划用bag05、container01、mobilephone02三条固定Train序列各202帧，实际比较同一候选的直接预测、OPE文件和TraX传输结果。OPE保持六位小数输出与5.01e-7舍入误差界；TraX按原完整float32/四位小数/float32矩形传输规则比较，置信度误差界1e-6。合计6次初始化、1206次跟踪调用。这项实测尚未发生，CPU检查不能替代。

输出置信度继续使用原始Hann最大响应；模板更新仍每50帧且该原始分数严格大于0.75。不会采用M68中未获支持的Null乘法、分数缩放或归一化，也不会因为接口切换暗中改变写入条件。

实际OPE/TraX保真通过后，才有资格运行完整303个VOT低22 anchors，沿用M64/M67原冻结晋升要求：EAO至少57.635993、ACC至少75.719622、ROB至少73.022401，确认失败不超过118，保护原生七条零失败序列，并核验全部anchors与同一模型bundle。这里只继承已有条件，没有按M73结果调整。低22通过后，才继续同一底座、adapter、文本协议和运行策略的DepthTrack Test、CDTB及完整VOT验证。

### 当前状态与下一步

2026-09-07 18:09再次按/proc的PID、启动时间、argv和cwd核验：M73原整队列及后台完成核验进程均存活；后台仍以240秒间隔等待。为避免无意义轮询，本节不重复刷新或解释早期训练损失。

磁盘剩余约1.980 GB，两个Qwen模型的分片数量与总字节仍完整匹配。新准备主要复用已有输入，新增少量协议绑定bank和代码，没有复制底座或删除Qwen。

下一步等待实际训练与内容条件结果，再执行已准备的同权重接口保真；若条件失败，保留完成结果并分析损害/词义增量，不运行新的正式评测。本节未增加DepthTrack Test、CDTB、VOT或M73完整开发指标，也没有新的独立模型审阅PASS。
