# M73完成核验与有条件内容对照已接入后台队列

M73四组真实因果训练按§5.133继续运行。本节仅补齐训练结束后的完成核验和有条件的固定权重内容对照，没有修改M73原训练代码、配对、预算、数据顺序或晋升条件。网络仍是冻结原生STTrack＋289154参数的密集语义适配器，Null项与原生模板策略保持不变，架构与既有正式指标不重复列写。

### 完成核验已用真实历史产物检查

新核验器固定M73整体spec、两seed冻结spec、训练器、160个模型源码和词向量bank，完成后核查四组最终checkpoint、实际优化器状态、130条训练序列覆盖、有效监督量和每50帧的实际模板写入。随后从封存完整预测族独立重算连续IoU、序列等权均值、低重叠帧、H10和全部11项继承条件，并记录Category持续失败而参考路径区间内每帧IoU均不低于0.5的损害。

核验入口先在已完成的M67 Control真实产物上实际运行：130条序列、186694次训练调用、5798次优化、174479个有效监督帧、3917条抽样状态以及1768次模板写入全部匹配；实际读取初始化/final/latest参数、有限值和优化器步数。原生/Control/Support的三组开发集标量指标由已有独立标量实现复算一致。没有构造假结果，也没有打开尚不存在的M73最终权重。

底座不变检查的证明范围仍是训练器结束时对全部底座参数及buffer的断言与哈希记录；本核验不宣称重新执行整轮训练。历史校验也不是新的独立模型审阅PASS。

### 条件内容对照：现在只冻结入口，不提前推理

CPU阶段已核验22×5×768词向量bank、slot/mask/padding和非类别内容一致性。原类别与Empty直接绑定M73实际bank，替换类别复用M69已封存版本；没有重新生成caption或embedding。

预先指定的唯一候选仍是seed2027 Category最终权重。只有两seed各自通过原11项开发条件且最终产物完成核验，入口才读取该final并绑定其SHA；seed2028不可替代候选，也不选择中间checkpoint。现在预备代码与配置不等于已获得内容实验资格。

| 条件 | 使用的权重 | 推理范围 | 对照含义 |
| --- | --- | --- | --- |
| Category | 固定seed2027 Category final | 复用其已封存22条完整递归结果，并实际复现3条各102帧前缀 | 同权重原类别参照 |
| Empty | 同一Category final | 22条完整递归，共33108次跟踪调用 | 清空有效词槽内容，保持五槽、mask与padding |
| Swapped | 同一Category final | 22条完整递归，共33108次跟踪调用 | 替换原自动类别，其他内容不变 |

这里的固定权重Empty与训练出的Empty组是两个不同对照。前者测这个已训练模型是否依赖具体词义，后者测相同预算下文本条件参与训练的增量。三个预测族封存后才读取后续GT计算指标，推理过程无GT重置、在线caption或优化。

内容条件沿用每个对照四项检查：Category按帧均值至少高0.001，序列等权均值不低，低重叠帧及H10不多。即使全部通过，本入口也只允许继续准备OPE/TraX接口一致性检查，不自动运行VOT低22或完整三数据集。已有开发22仍属于反复使用的开发集；自动类别不等于语义真值，这里也不证明同类别细粒度身份辨别。

已实际测试“训练未完成却请求内容入口”的情形：入口因缺少M73完成核验而返回非零，没有创建activation、预测目录或模型输出，训练spec/冻结文件/原队列哈希均未变化。三个新Python源码通过目标Python编译，两份shell通过bash语法检查。

### 后台接续与当前实际进度

接续程序在独立screen中运行，等待原M73整队列的PID、启动时间、argv与cwd，不把seed2027某个训练子进程结束误当作全任务结束。每240秒检查一次。原整队列正常结束后，先核验四组完整训练和开发结果；任何seed未通过就生成明确的内容跳过记录；两组均通过才启动固定seed2027文字对照。执行错误停止并保留日志，不重启原训练或放宽条件。

持久screen启动时，大写`-D -m`使外围启动器同步等待。已核验screen独立会话后仅结束该外围启动器；原screen、接续进程及两条训练进程的PID和启动时间均保持不变，screen已由PID1接管。接续已用实际/proc身份验证，不能只依据一个旧状态文件宣称存活。

| seed/组 | 已完成拟合序列 | 已完成跟踪调用 | 已完成优化步数 | 状态 |
| --- | ---: | ---: | ---: | --- |
| 2027/category | 19/130 | 27455 | 861 | 原进程存活，训练中 |
| 2027/empty | 19/130 | 27455 | 861 | 原进程存活，训练中 |
| 2028/category | 0/130 | 0 | 0 | 后续固定队列 |
| 2028/empty | 0/130 | 0 | 0 | 后续固定队列 |

以上快照为UTC `2026-09-07T09:57:10.573696+00:00`，即北京时间2026-09-07 17:57。两GPU各约2461 MiB。磁盘剩余1.986 GB；Qwen3_8B五个权重分片与Qwen2.5-VL-3B-Instruct两个分片的数量、总字节均实际核验保留。本节没有删除权重或复制底座。

### 下一步与结果边界

预计seed2027训练及开发验证于今晚21—22时附近完成，全部两seed约在9月8日凌晨完成，具体依实际吞吐而定；若获准追加内容对照，还需其实际运行时间。中途以预计阶段完成时间安排人工检查，后台等待保持240秒，不频繁重复查询长训练。

当前只完成了核验工具的历史验证、内容入口的CPU准备与接续启动；M73尚无完整开发结果，也没有新的DepthTrack Test、CDTB或VOT指标。若未通过，应保留配对结果并按文字增量、成功序列损害及恢复可达性分析原因；若通过，接着检查同一最终权重的实际评测入口，再决定是否进入既定低22验证。M65/M67的原负结果与M72诊断范围不被本节改写。
