

## 5.154 M78原始响应竞争完成：开发10项与固定权重词义8项均通过，真实入口一致后启动VOT低22

本节更新至2026年9月8日北京时间09:15附近的完成态证据。仅使用seed2027，没有增加seed、挑选中间checkpoint或重写自动caption。训练与内容结果已封存；VOT低22刚启动，尚无完成态指标，完整DepthTrack Test、CDTB和VOT127尚未开始。旧M73/M77的原冻结结论保持不变。

M78是§5.151预先冻结的原始响应竞争对照：延续冻结原生STTrack与289154参数密集类别语义适配器，将M77的log(Hann×score)竞争改为log(score)，推理仍使用原Hann及默认每50步、score>0.75的动态模板更新。负例资格仍为解码中心在GT外且解码IoU≤0.1、几何detach、最多9个困难负例；评分排序改为原始响应，因此实际选出的困难负例集合可能变化，不能声称每帧负例完全相同。原生focal、2×GIoU、5×L1、GT坐标变换及回归监督位置保持不变。类别槽0保留，属性槽为空文本向量，五槽、mask与padding不变。

Category和独立训练的Empty均完成130条DepthTrack Train拟合序列、186694次真实预测裁剪跟踪调用、5798次优化，训练退出均为0。Category于UTC00:02:13完成，Empty于00:02:57完成。两组同初始化、物理序列顺序与预算，分别沿自己的预测状态训练；初始化之后不以GT重置，状态detach，没有跨时间/crop反向传播。

开发22每组33130帧、33108次跟踪调用，有效非初始化帧28897；指标和完整逐序列输出封存后独立重算。以下是反复使用的DepthTrack Train开发集，不是VOT低22或官方Test。均值为0—1尺度，H10为连续IoU≤0.1至少10帧的代理失败段。

| 配置 | 按有效帧平均IoU | 序列等权平均IoU | IoU≤0.1帧 | H10段 |
|---|---:|---:|---:|---:|
| 原生STTrack | 0.652226 | 0.684336 | 7397 | 75 |
| M73 Category，无额外竞争损失 | 0.723689 | 0.738935 | 5072 | 71 |
| M77 Category，Hann后竞争 | 0.658379 | 0.668491 | 7188 | 80 |
| M78 Category，原始响应竞争 | 0.719009 | 0.701514 | 5223 | 73 |
| M78独立Empty训练对照 | 0.634547 | 0.651447 | 8125 | 83 |

M78 Category相对原生按帧均值增加6.6782个百分点、宏平均增加1.7177个百分点、低重叠减少2174帧、H10减少2段；相对同预算独立Empty增加8.4461个百分点、低重叠减少2902帧、H10减少10段。§5.151在运行前固定的10项条件全部通过，包括原生及本轮Empty零H10序列保护。这是本轮新方案采用原生与匹配Empty参照的结果，不是把旧M73的11项或M77的15项保护要求事后改成10项。历史M65 Control等仍列作描述性参照，未要求逐序列支配所有历史版本。

三种训练目标的对照仍有明确负结果：M78 Category比M77增加6.0630个百分点，但比不加竞争损失的M73低0.4680个百分点、宏平均低3.7421个百分点，并多151个低重叠帧和2个H10段。M78 Empty相对M77 Empty反而下降6.4971个百分点。因此不能写成“原始竞争普遍优于Hann竞争”，也不能写成“新增竞争损失已超过原定位目标”。各策略访问自己的递归状态，这些差异是完整训练干预的效果，不能按单帧词义拆分。

收益和损害均需保留：Category相对原生11/22条序列均值提高，相对独立Empty为16/22；colacan01、ball07贡献较大。逐条移除任意一条序列后，Category相对原生和独立Empty的按帧均值差仍为正，最小分别为0.3594和2.3124个百分点；这仅是集中性描述，不是多seed不确定性估计。glass03从原生0.757594降至0.168709，flowerbasket从0.762025降至0.312127，mobilephone01从0.729845降至0.527276。审计中有7个去重持续损害区间、481帧满足“Category低重叠，至少一个独立原生/Empty参照在对应每帧IoU≥0.5”；其中相对原生单独为4段234帧。不同参照重复命中的区间不重复计帧。全部22条结果保留于CSV，不能用通过整体门槛代替逐序列无损的承诺。

随后固定同一个M78 Category最终权重，保持相同五槽结构、mask与padding，仅替换文字内容。3条序列×102帧类别前缀逐值复现原Category输出；之后Empty与Swapped各完成22条、33108次跟踪调用，零优化、零新caption。两条跟踪及内容分析均exit0，UTC01:05:29形成完成态结果。

| 同一个M78 Category权重的输入 | 按有效帧平均IoU | 序列等权平均IoU | IoU≤0.1帧 | H10段 | 模板写入 |
|---|---:|---:|---:|---:|---:|
| 原自动类别 | 0.719009 | 0.701514 | 5223 | 73 | 272 |
| 空文本 | 0.654715 | 0.647396 | 7397 | 86 | 303 |
| 替换自动类别 | 0.668375 | 0.671228 | 6886 | 93 | 315 |

原类别相对同权重空文本按帧增加6.4293个百分点、宏平均增加5.4117个百分点，低重叠少2174帧、H10少13段；相对替换类别分别增加5.0634和3.0286个百分点，低重叠少1663帧、H10少20段。原定8项内容条件全部通过。相对空文本13/22条均值更好，相对替换类别14/22；逐条移除任意序列后的最小按帧增量分别仍为1.8458和0.9374个百分点。

这比M77同权重原类别落后空词和替换词的结果更支持“当前权重对类别内容具有有益敏感性”。8.4461个百分点的独立训练差值与6.4293个百分点的同权重内容差值回答不同问题，不能相减后把余量精确归因为视觉训练。当前仍是自动类别条件证据，尚未证明caption类别全部正确、细粒度属性可靠或同类实例消歧成立。模板写入数随文字改变，收益仍可能通过位置、分数、模板和query共同传播，不能将其全部归为单帧语义定位。

同权重内容对照也有局部损害：原类别相对空词存在egg[1272,1326)、mobilephone01[1367,1381)、[1384,1418)三段共102帧严格损害；相对替换类别存在cup08[750,852)及上述两段手机区间，共150帧。按物理区间去重为4段204帧，不能把两参照简单相加为252个不同受损帧。完整8项通过表示满足预定总体内容约束，并不等于每个词在每条轨迹上均有益。

最终Category权重SHA256：`2c3b8acc538605abb240b17654a66ab0fbd324fa1b1ca6a8688dfdcce02b23a1`；独立Empty权重：`70f9f9f797b2203eb84b3f6cb768df8c718c993f45fd78642a1550f9e811eb1c`。
开发结果SHA256：`37e01fd17c9b747dc5c5d7654ef66fc60b8ba00e799d0a59fc71e7dfba82e16a`；完成态权重/源码/训练覆盖/标量审计：`5a402e032e0f18390ae469b5641c200ea30f24740753fd6c49460654efd977d7`；固定权重内容结果：`4771426a7029683b38a899917c7f19d603348ebad833fd407cb7808ae6c50054`。这些是实际确定性核查，不是gpt-6-astra独立模型审阅PASS。

全部开发和内容条件通过后，绑定既定Category最终权重，未从Empty或历史模型另择优。模型bundle SHA256为`d1507b789040ae936721f4be65385d40e902d7fcf7f9a9c325bb3d5e19d95e79`。随后完成3条Train序列各202帧的真实OPE与toolkit/TraX入口比较，共6次初始化、1206次模型跟踪调用；OPE六位小数输出相对直接输出最大误差不足5e-7，TraX的传输框逐值一致，confidence误差不足3e-8。所有进程及验证退出为0；真实入口结果SHA256为`a64b8e3186cd91338b1a8efc1da87550a0f4e463f75e2946d4eafcd6d158dfe0`。前缀一致不是正式指标，也不证明之后的完整轨迹无误。

VOT低22于UTC01:12:05启动，准备与模型绑定检查exit0，实际跟踪已进入4个分片：22条序列、303个anchors、220483个计划帧位置，使用GPU1、控制器轮询240秒；这些分片不是多seed。未预填结果，未新训权重或生成文字，沿用已封存303个初始化文本记录。低22仍按§5.153原定门槛：EAO≥57.635993、ACC≥75.719622、ROB≥73.022401、确认失败≤118、保护原生7条零失败序列、303个anchor全部完成，并保持bundle与文本协议一致。只有该门通过，才使用同一套最终模型完成DepthTrack Test、CDTB、VOT127；否则保留本次完整负结果，后续改动另立实验。本节不预报低22或全量成绩。

完成态CSV、封存记录、审计、入口一致性、低22启动证据发布至`projects/sttrack_lachtt_v1/diagnostics/m78_raw_competition/completed/`。原始逐帧预测、训练trace与权重保留服务器；交接文档同步服务器、Desktop/document和GitHub。磁盘仍约1.58GiB可用，本阶段未删除文件，两份Qwen继续保留。项目总目标尚未完成，正式三数据集参照仍为原生STTrack同一权重的历史完整结果。
