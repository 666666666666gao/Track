# M57原生初始化提取：真实GPU前缀验收完成

2026-09-06 13:50 CST在空闲GPU1完成，进程退出码0。三个路径各102帧，共306帧，均使用chair01_indoor的同一合法初始化框；没有解析后续GT、训练参数或保存正式特征。当前只是固定原生参照的工程验收，最终语言底座仍待M55完整配对决定。

| 实际检查 | 原生 | 观测+t1参照 | 观测+t0参照 |
| --- | --- | --- | --- |
| 完整前缀框、响应、query、模板和frame_id | 基准 | 逐项完全相同 | 逐项完全相同 |
| 默认动态模板实际写入 | 第100步 | 第100步 | 第100步 |
| 观测bank读取新动态模板 | 不适用 | 第101步 | 第101步 |
| t0提取前后注册网络/随机状态及模板对象身份 | 不适用 | 不适用 | 全部保持 |
| t0参照在模板更新后保持 | 不适用 | 不适用 | 通过 |

第50步没有实际模板写入，不能把两个定时检查点说成两次已覆盖的写入；本次实际覆盖一次写入和下一帧读取。t0参照为有限、无梯度的2×16×768 float32张量，初始参照SHA为cc9840a39ef6ccd32ab8b12bfa65101494cd05ade6b0d06773c52514b14da229。

## 执行边界

为利用M55 Clone完成后释放的GPU1，将原生参照的短工程验收提前执行。正式特征采集与训练仍等待底座选择；如果以后采用Clone，需在Clone上重新验收，当前结果不外推到它。运行使用M55 Control的157文件冻结代码快照，但加载的是原生STTrack权重cacbd799115be1aaeb049cee0db89270851e3b6dd68997553b4c2c31c1104f98，未加载微调后的Control权重。与M56共有的10个冻结来源文件逐项匹配。

首次预检误将提取器路径指向尚未安装的overlay，在GPU启动和创建实验目录前停止。随后绑定准备目录中已经存在、与公开版本SHA相同的initial_observation.py；没有新增兼容逻辑或改动提取器。实际运行13:49:34启动、13:50:05完成，三个phase和最终contract都成功生成。

此前CPU检查只证明候选头接口；这次补充真实初始化前向及默认更新后的短轨迹状态证据。它没有证明语义绑定正确、局部格点具有部件语义或长期跟踪改善，也没有执行新的独立结果审阅。

## 产物

私有13文件归档SHA为308f029ed312e45dace9cb2092dabf462f9a21d528acb5d0f92c0b11776e51cb。下载后逐文件核对，并直接比较三个原始rows列表相等。三份JSON含不同phase字段，文件SHA不同并不表示其rows不同。原始短预测记录保留私有归档，公开原始contract、spec、源码、启动收据与日志。

- [原始GPU结果](output/contract.json)
- [冻结运行输入](spec.json)
- [实际启动与预检修正](launch.json)
- [验收脚本](verify_t0_extraction.py)
