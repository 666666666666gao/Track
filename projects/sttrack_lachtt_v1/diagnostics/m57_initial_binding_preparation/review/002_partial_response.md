M57 实现本身与约定 2×2 相符，t0 helper 参数也匹配现有 STTrack.forward，未见阻断接口错误。checker 的梯度记录确在 optimizer.step 前，且第二/三步要求各组范数非零，零残差父头/共同初始化/参数量检查有效。主要 WARN 是来源探针把当前、上一、dynamic 全部同时 +0.5，只断言全体是否变化；错误的“池化所有可变对象”来源仍可能通过，且 candidate 在改 object20 时仅检查前20不变，没有检查仅 object20 改变。最小增强是逐对象非均匀扰动并核对未扰动对象的 conditioned K/V 不变、对应对象变化。另相同槽容量目前主要由源码保证；checker 未断言有效槽 queries 确实不同，MHA 分组梯度非零可由 V 分片贡献，不能单独证明已消除 Q/K 退化。完整带行号 delta 即将返回。