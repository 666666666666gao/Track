Scope clarification: the limitation applies to **my independent local review**, not as a categorical denial of the executor path. I did **not** have local raw VOT trajectories/GT available to independently recompute overlaps myself, so my audit could not independently certify a fresh raw trajectory/GT overlap recomputation. Separately, the inspected `export.py` path is consistent with the executor running a new in-memory `LRUCache`/`AnalysisProcessor` over the sealed workspace, where VOT `SeparableAnalysis` dependencies read `Trajectory` data and calculate overlaps; root’s `export.exit = 0` plus bound source supports that this exporter path completed.