# 差分适配器正式入口短测完成：只验证接口，不代表性能晋升

已封存M84最终权重 `c63605ebcb1f702de66f96967255e5301bfdca1e3c40450b6d4b8a952791b1e0` 在固定拟合集前两条序列 `cube04_indoor`、`bag04_indoor` 各102帧完成实际入口核验。使用旧拟合bank原向量，未生成新文字、未读取后续GT、未训练或运行外部测试集；M87运行代码、文字与冻结条件没有改变。

| 内容 | 序列 | 含初始化帧数 | OPE框最大舍入误差 | OPE分数最大舍入误差 |
|---|---|---:|---:|---:|
| category | cube04_indoor | 102 | 4.98535158e-07 | 4.96135712e-07 |
| category | bag04_indoor | 102 | 4.99267571e-07 | 4.88750458e-07 |
| empty | cube04_indoor | 102 | 4.97802738e-07 | 4.95262146e-07 |
| empty | bag04_indoor | 102 | 4.99046337e-07 | 4.89151001e-07 |

- 同权重Empty直接路径与独立原生路径共204帧的框和分数完全一致。
- 4个实际TraX跟踪会话共404次回报，框与直接路径经过实际wire转换后的值完全一致，置信度误差不超过1e-6；另1个小数初始化会话只验证传输和bank绑定，无跟踪帧。5个子进程均退出0。
- OPE复用同一个tracker并重新初始化第二条序列，TraX各自启动新进程；这两种运行方式在本次短片段上输出一致。各片段经历第50、100帧的默认更新资格检查，不等于每个检查点都发生模板写入。
- 通过比较的路径共1414次实际跟踪调用、15次初始化；计入首次退出失败后重跑的101次TraX跟踪，累计1515次跟踪、16次初始化。未计算F/EAO/ROB；没有证明完整外部泛化或长序列安全。

首次运行在Category第一个TraX会话的101次输出比较通过后，因退出码非零停止；旧断言未保存具体退出码，不能认定某个信号或唯一原因。该次记录封存在attempt1.tar.gz。实际工具包退出逻辑等待3秒后可能强制终止进程；客户端改为明确发送quit、等待30秒内自然退出0，然后清理。恢复后5个会话正常退出的实测耗时为1.368–4.127秒。已完成的原生/Category直接路径和Category OPE未重跑；未改变模型及比较容差。

发现并仅在隔离接口副本修复 `vot_wire_bbox`：旧函数只做float32，完整TraX 4.0.2传输还包括四位小数文本再解析。小数框实测验证了修复。旧归档和正在运行的训练均未修改。所用两个真实初始化框是整数，修复不改变本次跟踪前缀的初始化。

这些结果只绑定M84权重及本次旧文本协议。M87完成后仍需绑定自己的最终权重与文字；正式三数据集输入未因此自动就绪，也未授权晋升。新上下文代码审查为same-family/provisional，不是跨模型或人工验收。

结果SHA256：`bfdcf722cb8c9ac5888b310cbe5978f72c3c2e6e53b6724ee2c569741f5f63c0`。完整私有证据包SHA256：`e7008ec917fe937af9969bde8901f61c49b891e79d03e1aceefd5494308ececc`。
