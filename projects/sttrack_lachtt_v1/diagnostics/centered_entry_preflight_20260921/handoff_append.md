

### 5.182 差分适配器OPE与真实TraX入口核验完成，M87仍在训练（2026-09-21）

使用已封存M84最终权重`c63605ebcb1f702de66f96967255e5301bfdca1e3c40450b6d4b8a952791b1e0`、原拟合bank和冻结源码，在拟合清单前两条`cube04_indoor`、`bag04_indoor`各102帧完成部署接口短测。固定seed2027，无优化、无新caption、无后续GT读取、无外部测试或性能指标计算，不修改M87训练。

Category/Empty直接调用、OPE落盘以及真实vot-toolkit 0.7.1/TraX 4.0.2子进程输出一致：4组OPE比较均满足六位小数舍入误差不超过5.01e-7；4个跟踪会话共404次TraX回报的框按实际wire转换精确一致，分数误差不超过1e-6。另1个小数初始化会话验证传输及初始化key绑定。Empty与独立原生路径204个含初始化帧的框和分数完全一致；通过比较的路径共1414次实际跟踪、15次初始化。OPE跨序列复用tracker、TraX重新启动进程，未观察到本次短前缀重置差异。

首轮在101次TraX输出比较通过后，因退出码非零停止；原断言未记录数值，不认定具体信号或唯一原因。保留attempt1.tar.gz，客户端改为显式quit后等待30秒内自然退出0再清理；正常比较结果不变。恢复后所有会话退出0。包含失败尝试累计1515次推理、16次初始化。没有重跑已完成的原生/Category直接路径或Category OPE。

发现旧`initialization_text.vot_wire_bbox`只实现float32转换，遗漏已实测的四位小数文本序列化。在隔离接口副本中补齐并由实际小数初始化回报确认；旧归档、活动训练与数据bank不变。两个真实拟合初始化框为整数，补齐转换不改变本次前缀起点。新上下文代码审查为same-family/provisional；已修复清单校验遗漏并强化结果完整性检查后部署。

这只是M84旧文字协议的功能核验，不能推导M84晋升、M87完成、三数据集性能或全部长期安全。M87最终权重及新文字仍需单独绑定，完整外部输入仍未自动就绪。阶段观测`2026-09-20T19:30:37.276028+00:00`：M87控制器与训练进程仍存在，已完成42/130条、60534次跟踪、1887次优化；磁盘剩余1111871488字节。两个Qwen权重保留。

完整私有证据包SHA256 `e7008ec917fe937af9969bde8901f61c49b891e79d03e1aceefd5494308ececc`；结果SHA256 `bfdcf722cb8c9ac5888b310cbe5978f72c3c2e6e53b6724ee2c569741f5f63c0`。报告、隔离接口、测试脚本和审查位于`projects/sttrack_lachtt_v1/diagnostics/centered_entry_preflight_20260921/`。
