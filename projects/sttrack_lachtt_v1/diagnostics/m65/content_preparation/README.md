# M65同权重类别内容对照：输入与执行条件已冻结，尚未运行

2026-09-07 06:04:31 CST完成CPU输入核对、源文件哈希检查与shell语法检查。M65仍在原双卡训练流程中；新增内容对照位于独立目录，不修改已冻结训练、递归入口或门槛。没有加载未完成权重、运行新轨迹、打开新的GT文件、重新生成caption或编码embedding。

## 要回答的问题

M65的Control/Null结构对照只能检验无支持项的作用，不能单独证明具体词义有效。若Null通过全部原开发门，本步骤固定同一个Null final权重，检验原自动类别相对空文本与替换类别是否具有持续轨迹增量。

| 输入 | 类别槽0 | 有效属性槽1—4 | 槽数、mask与padding | 参数和运行策略 |
| --- | --- | --- | --- | --- |
| category | M65原自动类别 | 原CLIP空文本 | 原5槽结构完全保留 | 同一个Null final及原生模板规则 |
| empty | 原CLIP空文本 | 原CLIP空文本 | 与category完全相同 | 相同 |
| swapped | M60既有封存的替换类别向量 | 原CLIP空文本 | 与category完全相同 | 相同 |

22条替换类别向量均与原向量不同。替换映射直接复用M60，不依据M65轨迹结果挑选；非类别向量、所有mask与padding逐元素一致。空文本分支只替换有效词向量，保持结构信息不变。

原自动类别存在已记录的错误，不能称为语义真值；替换类别也不等于经过核验的目标冲突描述。这组实验至多支持具体类别条件的增量，不能仅凭输入敏感性声称同类实例属性消歧。

## 条件执行顺序

1. 原M65训练和两组开发递归全部完成，完成态复核器校验通过，Null的所有原冻结门为true。
2. 加载唯一固定Null final，先用category重放bag05、cup08、mobilephone02各102帧，与原Null完整轨迹对应前缀逐元素核对。三个前缀来自既有M60协议，在M65开发结果产生前冻结。
3. 前缀一致后，GPU0运行empty、GPU1运行swapped，各覆盖开发22全部33,130图像帧。category复用已有完整Null输出，不重复跟踪；两个新完整对照共66,216次调用，另有303次前缀调用。
4. 核对三组完整轨迹及同一head SHA后，读取GT计算IoU、低重叠帧、H10和由默认条件重建的模板写入次数。

原category按帧均值需分别比empty和swapped至少高0.001，序列等权均值不低于二者，低重叠帧与H10也不多于二者。内容门与原M65结构门同时成立，才允许准备一套新的明确绑定权重与文字协议的低22候选；本工具始终不允许直接启动完整三数据集评测。

门槛已在M65开发输出之前冻结。若原M65门失败，内容对照保持未执行；若内容门失败，保留负结果，不更换门槛、挑选中途权重或把结构训练收益归给词义。

## 文件与验证边界

服务器入口为`/root/autodl-tmp/m65_content_counterfactuals_20260907.py`，SHA `3e125d263862f8f301417423cc25eb06dc2191fbe9d28404e3e8562509ae9bd3`。

计划与两个小型输入bank在`/root/autodl-tmp/sttrack_m65_category_null_support_20260907/content_counterfactuals`。spec SHA为`221ecb9899d97763353281ee8e334dac445b6704182eac3f641512e327a953e2`。`prepare`与`check`已正常完成，`run_controls.sh`通过bash语法检查；prefix、完整对照和analysis均未运行，不能把接口准备完成写成学习权重的递归保真或语言收益已经通过。

本轮最新进程核查为05:56：两个既有Python PID均仍运行，各完成11条拟合序列、19,859次调用、622步优化，无退出文件。这是当时的完成序列前缀，未产生M65正式结果。两份Qwen保持保留，本步骤没有调用它们。
