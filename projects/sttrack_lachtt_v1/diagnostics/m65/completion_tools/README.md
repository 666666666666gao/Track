# M65完成态复核工具：历史轨迹参照通过，新实验完成审计仍待运行

2026-09-07 05:49:49 CST，在M65双卡训练期间补齐只读完成态复核器。它独立于已冻结训练和递归控制器，不修改运行中的源码、权重、输入或门槛，也不启动另一份训练或提前读取M65开发预测。

## 已验证的部分

复用M63已经验证的逐帧标量矩形IoU与连续区间计算，先核对M58文本、视觉两组封存轨迹和原生独立轨迹的全部文件，再读取GT复算。三组各22条完整序列、33,130图像帧，逐序列及聚合的IoU、低重叠帧、H10均与原结果一致。原生两份轨迹源文件也按既有哈希核对；初始化与无效GT保持排除，连续低重叠段不会跨越无效GT窗口。

这里验证的是复核工具的轨迹读取与计算部分，不是新的模型收益。原M58指标已经在前文记录，本节不重复列为新实验成绩。M65的完成态权重和结果尚未存在，因此completed模式尚未执行，M65_completion_audited=false。

## 完成后检查的范围

| 检查对象 | 具体证据与约束 |
| --- | --- |
| 运行及版本 | 六项训练、递归、分析、控制器退出码；冻结spec、160份源文件、独立训练入口、递归入口和指标代码哈希 |
| 权重 | 两组固定final的来源、架构、null_support、base SHA、参数数量与有限性；两组初始张量完全相同；final和最后latest参数一致 |
| 训练预算 | 130条顺序、186,694次调用、逐序列GT有效帧和每32帧有效梯度窗口；累计优化步数与AdamW实际state中的step一致 |
| 写入及状态记录 | 所有50帧检查点都包含在采样trace中，分数与0.75严格阈值对应；写入计数与逐序列训练收据一致 |
| 开发轨迹 | 两组共44条完整文件及原生22条独立轨迹；逐帧编号、长度、初始化、有限正宽高及分数合法性；全部封存后读取GT |
| 指标与晋升 | 使用标量IoU重新计算各序列和聚合值；重算所有原冻结门，不改变阈值或选择中途权重 |
| 长期损害 | 列出Null持续H10区间中，原生或Control在相同区间每帧都IoU≥0.5的事件，避免仅用一个平均值掩盖局部损害 |

最终检查点只保存adapter与优化器。底座未变这一项依据运行中对完整参数和buffer哈希的断言及其收据，并核对原生权重文件；不会把只包含adapter的文件宣称为独立保存的训练后完整底座快照。

## 使用与后续

服务器脚本：`/root/autodl-tmp/audit_m65_completed_20260907.py`，源码SHA `ef6a3f5f9f7b8635497fc993fded0924e7f37f5b67d14c3fab46fc27b7e596db`。

历史参照模式`reference`已退出0，收据在M65目录的`completion_auditor_reference.json`。待原控制器正常完成训练、开发递归及分析后，运行`completed`，输出`completed_evidence_audit.json`。任何缺失、退出异常或哈希不匹配都会中止复核。

即使开发门通过，输出也只允许进入同权重内容归因；public_evaluation_allowed仍为false，不能直接启动全量。若开发门未通过，保留结果并继续诊断，不改变这轮门槛。

05:41的进程核查确认两组真实Python训练PID仍在运行，各完成4条拟合序列、6,968次调用和219步优化；该记录只是当时的训练前缀。预计训练约09:06完成，随后自动完成开发22。本文没有新正式三数据集指标，也没有新的独立模型审阅结论。
