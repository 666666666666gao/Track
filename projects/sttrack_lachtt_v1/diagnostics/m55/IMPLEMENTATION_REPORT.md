# M55 实现与训练启动记录

截至 2026-09-06 08:45:55 CST，v2 Control 和 Clone 均在训练，最近已刷新记录分别为 550／3,840 与 300／3,840 次优化更新。两组首轮 2,048 个实际训练片段输入流完全一致。没有 M55 完成态权重、递归结果或公开数据集指标。

## 已完成的工作

- 为 Control／Clone 建立独立代码目录，冻结每组 157 个文件。唯一模型差异是 TSG 的两处 `.clone()`，原生 full127 使用的运行目录保持原样。
- 实现显式严格载入基础权重的训练入口。训练全部原有模块，按官方 GIoU／L1／Focal 目标计算损失；不调用原 actor 的静默 GIoU 异常处理。
- 两项训练检查分别通过：固定两片段的 3 步反向传播检查，以及正式采样、两种保留比例的 16 片段／2 步检查。它们是不同执行路径，没有保留可采用的权重。
- 按固定 63 fit／22 development 划分、相同种子和 3,840 次更新／组冻结配对微调；按批保存采样记录和完整输入张量散列。
- 补接已有 `toy07_indoor_320` 数据契约，并验证原失败片段真实 RGB-D 读取和增强。所有原始图像和 GT 文件不修改。

## 首次启动的失败及修复

v1 Control 在完成 20 次优化更新后因读取 `toy07_indoor_320/color/00001406.jpg` 退出，退出码为 1。批日志共有 82 个已进入训练循环的微批次，其中 80 个完成了 20 次累计优化，余下 2 个不构成一次优化更新。没有完整 epoch 检查点或最终权重。未启动的 Clone 队列被取消。

这条序列的实际文件为连续 1,367 对 RGB-D，GT 是固定 SHA 的 1,406 行；末尾 39 行没有图像。该例外早已在项目主文档 24.152.3 和既有 analyzer 中登记。本次新入口遗漏了该契约，v2 仅补接这一数据对应关系，并对其余 62 条 fit 序列严格核对文件名与标注长度。

v2 在新目录重新启动，保留 v1 的 spec、失败日志、部分训练日志及取消记录。两组仍从原始基础权重开始，训练种子、参数量、预算和晋升门不变。

## 合同检查的实际证据

| 检查 | Control | Clone |
|---|---:|---:|
| 固定片段检查实际优化步数 | 3 | 3 |
| 正式采样检查实际片段数 | 16 | 16 |
| 正式采样检查实际优化步数 | 2 | 2 |
| 正式采样检查实际搜索帧数 | 64 | 64 |
| 正式采样检查输入流是否逐字节一致 | 是 | 是 |
| 正式采样检查保留最终模型 | 否 | 否 |

正式采样检查的数据流 SHA 均为 `0616c78386ad3df13d7591a13417d28913d30ace77e2151d677fe3c7072d41c7`。v1 与 v2 的该小检查输入也一致；已知数据例外通过额外的第 160～167 片段检查覆盖，其中第 164 个是实际触发过错误的 `toy07`。

固定片段检查记录总参数元素 133,065,093；14 个已注册参数在该检查中没有梯度，两个分支的名单一致，各主要模块均获得非零梯度。不能声称每个注册参数都在该小检查中获得了梯度，也不能用检查批次损失高低判断跟踪性能。

## 当前状态与限制

v2 Control 于 08:10:26 在 GPU 0 启动；Clone 队列于 08:26:27 确认原生 full127 成功结束且 GPU 1 空闲后启动，队列退出码为 0。两组控制器分别为 309414／313370，08:45:55 的观测均未出现退出文件。

两组首轮各 2,048 片段、1,024 微批次、256 次优化更新，耗时约 987.33／984.79 秒。首轮批日志逐字节一致，SHA 均为 `19a40d3005d03524b9c34d897f941d71d279d0076d034d4c14176f945113c7b2`；初始张量散列清单也相同。最新已刷新更新数为 Control 550、Clone 300。首轮训练损失差异不能用于判断跟踪性能。

当前估计 Control 约 12:20、Clone 约 12:40 完成；后期保留设置可能改变吞吐，下一次训练检查计划接近第10轮，即约 10:40／11:00，之后再修正 ETA。训练后仍需完整数据流核对和固定 22 条开发递归。

原生 full127 现已完成，EAO／ACC／ROB 为 77.3216535962124／82.47118980990447／93.66910936768168%，只有 ACC 超过目标；独立报告位于 `../native_vot_full127`，不记作 M55 结果。

临时优化器状态仅在每轮结束后写入共享内存，会随机器重启丢失；当前训练器没有自动恢复入口。最终模型将落在持久磁盘并做本地归档。第一轮的 20 步失败结果没有用于初始化 v2。

训练和采样检查使用真实 DepthTrack Train 图像及数据集 GT；公开测试、开发集指标和递归晋升仍未执行。本轮不是预测裁剪训练、不是在线模板更新训练，也没有语言增益证据。
