# M55 完整性审阅记录

审阅为 GPT-5.5 xhigh 的同 GPT 模型族 Type-A advisory，不是跨模型族审计。审阅者只读列出的本机文件，没有连接服务器、使用 GPU 或重新训练。原始请求、回答与字节散列均保存在 `review/`。

最终结论：**训练入口和 v2 数据契约就绪的本机证据通过；实验成熟度仍为 WARN。**没有完成态训练、最终权重、递归指标或公开指标可供本轮审计。

| 审阅范围 | 结论 |
|---|---|
| 真实数据集 GT 来源、63 fit／22 development 隔离 | PASS |
| 严格基础权重载入、固定种子、预算、唯一模型差异 | PASS |
| 原采样及增强保留、明确传播数据与损失异常 | PASS |
| 梯度累积、损失、保留比例、LR 调度 | PASS |
| 固定片段及正式采样合同的实际执行证据 | PASS，仅覆盖小检查 |
| v1 数据失败和取消未启动 Clone 的记录 | PASS |
| v2 窄范围 GT 尾部契约和文件对应校验 | PASS |
| v2 修订下载绑定 | 40／40 文件字节数及 SHA 一致 |
| 完整 3,840 步训练、整个训练输入流、最终模型、递归效果 | 尚未产生完成态证据 |

第一次审阅只列出三步 `contract_v2`，因而质疑 spec 的两步合同字段。补充独立 `sampler_contract` 文件后，审阅者确认该字段对应 `train.py --mode contract` 的两步执行，撤回修改 spec 的建议。两次原始回答均保留，未改写审阅历史。

第三次审阅复核了实际数据失败：82 行旧批记录中，80 个微批次完成了 20 次优化更新；不能写为完成了 82 个优化批次。v2 仅在固定 GT 散列、1,406 行标注、1,367 对连续图像条件下，处理已有 `toy07_indoor_320` 的 39 行标注尾部；没有制造标签或修改原始数据。

限制：独立审阅只验证下载的代码子集、日志和绑定，未逐一下载 157 个远端源码文件或基础权重本体。运行中的训练器在载入前会验证全部已绑定代码文件和基础权重，并逐张量检查初始状态；这与审阅者独立核对权重本体是两种证据。自定义 CUDA 内核没有逐位复现保证；写出的临时优化器状态没有自动恢复入口。

下一次效果审计应在两组训练完成后进行：核对最终权重散列、完整输入流、全部步数／样本数及退出码，再独立复算完整 22 条开发递归指标。此前不作 M55 精度或公开基准收益宣称。
