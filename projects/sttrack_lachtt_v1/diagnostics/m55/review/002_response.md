M55 closure addendum complete. This remains read-only same-GPT-family Type-A advisory review. I did not edit files, use SSH/GPU, rerun training, or infer retained-training/remote completion.

Overall update: **the earlier “2-versus-3 optimizer-step mismatch” is resolved once the newly listed sampler-contract artifacts are included.** `training_spec.contract` refers to the `train.py --mode contract` sampler contract, which executed **2 optimizer steps** over epochs 1 and 10. The separate `contract_v2` artifact from the prior review is a different three-step checker driven by `check_training_v2.py`, not the executable contract described by `training_spec.contract`. A spec edit is therefore **not necessary** for the 2-step contract field; the only wording requirement is to keep the two contract families clearly labeled.

| Check | Status | Evidence | Finding |
|---|---:|---|---|
| Request scope | PASS | `2026-09-06_m55_preflight\002_request.txt:1-24` | The closure task asks specifically for the additional sampler-contract artifacts, no reruns, and reassessment of the prior contract-step mismatch. |
| SHA binding | PASS | `E\sampler_contract_download_binding.json:2-20`, `E\sampler_contract_download_binding.json:38-84` | I rehashed all 21 entries in `sampler_contract_download_binding.json`; there were no missing files and no hash/byte mismatches. |
| Contract executable identity | PASS | `E\training_spec.json:220-228`, `E\train.py:162-163` | `training_spec.contract` declares epochs `[1, 10]`, `clips_per_epoch: 8`, `optimizer_steps: 2`, and no retained weights. `train.py` implements contract mode with `epochs = [1, 10]` and `sample_count = 8`. |
| Actual optimizer counts | PASS | `E\sampler_contract\control\result.json:7-29`, `E\sampler_contract\clone\result.json:7-29`, `E\sampler_contract\control\steps.jsonl:1-2`, `E\sampler_contract\clone\steps.jsonl:1-2` | Both control and clone sampler-contract runs completed 2 optimizer steps, 8 microbatches, 16 clips, and 64 search frames. This matches `training_spec.contract`. |
| Prior 2-vs-3 finding | PASS / corrected | `E\check_training_v2.py:100-125`, `E\check_training_v2.py:147-152`; prior `contract_v2` results had `training_steps: 3` | The prior warning applies only if `contract_v2` is treated as the spec contract. With the new artifacts, the correct interpretation is two distinct artifacts: `sampler_contract` is the 2-step spec contract; `contract_v2` is a separate 3-step fixed-clip optimizer smoke test. No spec edit is needed. |
| Same initial state | PASS | `E\sampler_contract_download_binding.json:62-68`, `E\sampler_contract\control\execution_binding.json:5-8`, `E\sampler_contract\clone\execution_binding.json:5-8` | Both initial-state files are byte-identical and have SHA-256 `a7eb400cfe17820ed3ccc5ce98c0de960b5379ba038e154c95a3e261f9a414dd`. The execution bindings point to the same initial-state hash and same trainer hash, with only the model-source hash differing by arm. |
| Same input/sample stream | PASS | `E\sampler_contract\control\batches.jsonl:1-8`, `E\sampler_contract\clone\batches.jsonl:1-8`, `E\sampler_contract\control\result.json:29`, `E\sampler_contract\clone\result.json:29`, `E\sampler_contract\control\result.json:37`, `E\sampler_contract\clone\result.json:37` | Control and clone `batches.jsonl` are byte-identical, with the same eight batches, sample seeds, sequence names, template IDs, search IDs, and tensor hashes. Both results record the same `data_stream_sha256` / `batches_sha256`: `0616c78386ad3df13d7591a13417d28913d30ace77e2151d677fe3c7072d41c7`. |
| Actual sample/frame coverage | PASS | `E\sampler_contract\control\batches.jsonl:1-8` and identical clone file | The sampler contract covers 16 clips total: 8 clips at epoch 1 and 8 clips at epoch 10, two samples per batch, four batches per epoch. It includes replacement behavior as expected: e.g. `ball02_indoor` appears twice at epoch 10 and `toy08_indoor` has duplicated search id `812`, consistent with the declared causal-with-replacement sampler. |
| Both keep-rate regimes | PASS | `E\training_spec.json:199-207`, `E\train.py:188-189`, `E\sampler_contract\control\steps.jsonl:1-2`, `E\sampler_contract\clone\steps.jsonl:1-2` | Epoch 1 used keep-rate `[1.0]`; epoch 10 used `[1.0, 0.8500000238418579, 0.699999988079071]`. That verifies both regimes specified in `training_spec.json`, including float32 rounding. |
| Finite loss/gradient evidence | PASS | `E\train.py:213-224`, `E\sampler_contract\control\steps.jsonl:1-2`, `E\sampler_contract\clone\steps.jsonl:1-2` | The trainer asserts finite loss and finite gradient norm before stepping. The sampler-contract logs contain finite losses and gradient norms for both optimizer steps in both arms. |
| No retained contract weight | PASS | `E\training_spec.json:220-228`, `E\sampler_contract\control\result.json:25-41`, `E\sampler_contract\clone\result.json:25-41`, `E\train.py:270-282` | Contract mode produced no `weight_path` and no `weight_sha256`; both results set `development_evaluation=false`, `public_evaluation=false`, and `adopted_weight=false`. This contract did not create or adopt trained weights. |
| Exit status | PASS | `E\sampler_contract_control.exit:1`, `E\sampler_contract_clone.exit:1`, `E\sampler_contract_controller.exit:1` | All three sampler-contract exits are `0`. |
| CUDA determinism claim | WARN / limitation | `E\sampler_contract\control\execution_binding.json:13-15`, `E\sampler_contract\clone\execution_binding.json:13-15` | Both executions record `cudnn_deterministic=false` and `exact_repeat_of_custom_cuda_kernels_guaranteed=false`. The paired input/initial-state evidence is strong, but exact kernel-level repeatability is not guaranteed and should not be claimed. |

Fresh conclusion on the earlier action item: **do not edit `training_spec.json` to change `contract.optimizer_steps` from 2 to 3.** The spec’s contract field is correct for the `sampler_contract` executable path. The previous three-step observation belongs to `contract_v2`, whose own checker explicitly performs three optimizer steps and records no retained/adopted weights. The correct fix is wording/labeling only: call them `sampler_contract` and `contract_v2 smoke test` if both are mentioned together.

Remaining limitations preserved:

- No retained 15-epoch training completion is verified here.
- No recursive M55 metric, public benchmark result, or tracking improvement is verified here.
- The sampler contract validates a tiny two-epoch/16-clip contract path, not the full retained 3,840-step-per-arm budget.
- The execution bindings explicitly decline exact custom-CUDA reproducibility guarantees.
- I verified local downloaded artifacts only; I did not contact the remote server or infer current remote state.