# M55：两组训练完成，完整递归尚未完成

Control 于2026-09-06 12:20:17 CST、Clone于12:35:30正常结束。冻结第15轮权重已生成，完成态核验通过；没有新VOT、DepthTrack或CDTB指标，也没有采用任一新权重作为最终底座。

这仍是同一原生STTrack起点上的TSG方向修正对照，没有文本或候选关联头。冻结训练设计见上级目录，不重复列全部超参数。

| 完成态项目 | Control | Clone |
| --- | ---: | ---: |
| 固定最终轮次 | 15 | 15 |
| 实际优化步数 | 3840 | 3840 |
| 实际训练片段／搜索帧 | 30720 / 122880 | 30720 / 122880 |
| 实际覆盖拟合序列 | 63 | 63 |
| 训练耗时（秒） | 14983.666 | 14936.417 |
| 第15轮平均四帧损失（训练量） | 2.040377956 | 2.024263240 |
| 训练进程／控制器退出码 | 0 / 0 | 0 / 0 |

训练损失是四个搜索帧的定位／响应损失之和的均值，不是完整递归IoU或正式跟踪指标。Clone的该损失略低，不能据此判定修正有效或选择底座。

## 实际绑定核验

复用了已冻结并审阅的 `recursive_preparation/run_recursive.py::binding()`，读取了两组真实最终checkpoint，核对内部variant、训练spec、第15轮和3840步；同时验证权重SHA、初始状态文件、执行绑定、trainer、两组各157个源码文件与日志SHA。全部15轮的数据流与初始张量摘要一致。

另解析实际日志，确认优化编号1—3840连续、microbatch计数及epoch对应正确，30720个(epoch,index)训练片段完整且唯一；覆盖63条fit，未出现development序列。两组输入张量指纹及采样记录的完整SHA相同，不只是seed相同。模型文件的计算差异确实为两处 `.clone()`。

- Control 最终权重SHA：`fe1d4b2faddc7f0dc523b6ec9cacbbf878f523cfc1985cd2a0d8a3fb4c4ba284`
- Clone 最终权重SHA：`d1ce44c82593fe5f508954a536762ba9ef772e9264da12500e62f80064e46c81`
- 完整训练数据流SHA：`7990c9ae35381ad10ac5d99baa12988f6587ec960546ddbfa2499509db96200a`

原始权重和含采样帧号的完整batch记录保留在服务器，本目录提供其绑定、结果、每轮及每步训练统计。

## 导出修订与范围

首次导出器使用单空格还原Control赋值行，而原源码等号后有两个空格，导致文本级差异检查误报。实际diff已核对，仅为两处clone调用及对应空格。v2导出器按真实原文修正字符串后通过；训练源码、训练spec、递归runner和两组权重均未改变，训练未重跑。失败导出器、队列、退出码及修订日志保留在 `export_history/`，首次exit=1不表示训练失败。

本次执行了冻结绑定和完成态日志检查，没有新的独立结果审阅；此前Astra/max审阅覆盖入口源码，不能扩写成已审阅新权重性能。

## 递归接续

12:40 CST实查：M56 attributes进程326519在GPU0运行并完成11条序列文件；pooled进程327331在GPU1运行并完成1条，empty仍待attributes结束。M55两组队列322434/322595存活，按原计划等待对应M56控制器结束，再运行同一22条、33130帧完整递归。进度文件数不是性能分数。

M55全部预测封存后才读取后续GT并与独立原生轨迹比较。只有通过既定递归门才进入冻结低22比较；当前没有自动全量评测或底座晋升。M57的真实t0提取、正式缓存与训练继续等待底座决定，同一最终模型仍须验证三个完整数据集。

- [完成态核验报告](completion_report.json)
- [完成态导出器](export_completion.py)
- [实际接续观察](training_completion_observation.json)
- [已冻结的递归入口与计划](../recursive_preparation/README.md)
