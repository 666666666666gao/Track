# M88完成态自动收集

等待已经启动的原控制器PID48174，实际命令为M88 run_m88.sh；每240秒检查进程，不修改、不重启训练。控制器消失/僵尸后，必须验证训练、Category/Empty/Swapped三组递归、分析与controller六个退出码均0，才收集完成态。失败则停止，不制造完成声明。

保持原14项条件。CPU收集最终完整遍历权重、初始小型adapter权重、130条原始序列日志、采样状态日志、三组22条预测、实际源码/spec/bank/回执/GT及原始分析。后续开发GT仅在所有预测/收据验证后打开。复制M84 Category和Empty两组已有原始轨迹作为结构与功能参照，并核验对应封存回执；没有新增原生推理，不复制大底座或Qwen。

Empty逐帧bbox/score与M84 Empty的比较单独报告，不改变原指标级Empty门。M84 Empty对独立原生的证据沿用此前完成态核验。所有结果不论通过几项都导出，不能按性能选择文件。最终参数有限性、逐帧指标、14门、LOO及strict损害/改善由后续完整审查复算，不在等待时读取latest。

保留两次GPU前检证据：第一次前检成功但队列CRLF导致冻结失败；第二次对应LF修正后的最终spec。两次短训练均丢弃，不是独立性能实验。历史spec和说明保留，不覆盖原记录。

只使用CPU，显式CUDA_VISIBLE_DEVICES为空。预计证据包约20MB、峰值磁盘额外不超过100MB；当前约1GB余量。不调用caption、不运行推理、不修改任何权重、训练状态和运行队列，不发送消息，不启动正式三数据集评测。完成后下载并独立核验，再同步主交接文档和公开去掉GT坐标/图片/权重的结果。
