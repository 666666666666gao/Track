# M71等窗口分析的持久CPU接续

2026-09-07，14:05 CST实查。已将M71原定的等细窗口预算分析接成持久CPU后续任务，等待现有M67后诊断队列成功结束后执行。没有修改M67训练、M68/M69/M70实验及现有队列，没有新增模型、优化步或公开评测资格。

## 实际运行及接续条件

远端目录为`/root/autodl-tmp/sttrack_m71_equal_budget_routing_20260907/post_queue_runner`，screen为`m71_after_diagnostics_20260907`。实查Python PID474465，start_ticks4578576825；命令行、工作目录与登记一致。它按240秒间隔核验并等待原队列PID471187、start_ticks4578045533。

接续条件为原队列controller.exit=0，结果状态为`completed_authorized_post_M67_diagnostic_sequence`，绑定同一队列spec和M70封存结果。随后执行原M71的`analyze`。M71自己还会核对M70两分片、分析及控制退出码，逐项校验候选元数据、NPZ、公开路径一致性和未提交分支证明，然后才读取GT计算诊断。

如果前序失败，后续进程停止并保留日志与退出码；不会自动重跑训练、改变门槛或使用未完成的候选。分析成功后记录原M71结果SHA。当前只有实际等待进程，尚没有M71候选归因结果。

## 分析内容保持原计划

M71使用相同已封存的M65 Null权重与历史状态，比较类别、空文本、历史位置所选的细窗口。主对照对每个事件使用两种文字路由都具备的共同窗口数，同时报告固定1/2/3个窗口的可比子集及被排除事件。

类别与空文本均需要一次全图粗前向，历史位置不需要；因此匹配的是细窗口数量，不宣称全部计算成本相同。分别报告路由文字、细窗口文字与组合内容的影响，区分候选容量与分数最高候选的正确性。这里没有新增跟踪调用，不是完整恢复轨迹或部署触发验证，也没有不匹配类别对照。

## 磁盘检查

按M70实际冻结的11,555个窗口、两种文字条件及数组类型计算：256个float64四维框、两组256个float32响应及两组10个int16峰索引，正式数组未压缩数据量为237,570,800字节，约237.57 MB。

这个数字只含数组数据，不含NPZ头、事件JSON、smoke、其他队列阶段或公开评测输出，不能当作完整任务磁盘上界。当前磁盘空闲2,245,652,480字节，约2.246 GB；保留各阶段原有空闲空间检查，没有因为这次计算删除权重。

实际核验Qwen3_8B的5个safetensors共16,381,516,776字节，Qwen2.5-VL-3B-Instruct的2个safetensors共7,509,337,976字节，两个模型均完整保留。它们不是两个视觉模型：Qwen3_8B为文本模型。

## M67同一时刻进度

| 组别 | 已完成序列 | 跟踪调用 | 优化步 |
| --- | ---: | ---: | ---: |
| Control | 126/130 | 181,289 | 5,627 |
| Support | 124/130 | 177,474 | 5,507 |

两组原训练进程均实际存活，训练和完整递归控制尚未结束。新任务没有使用GPU。后续仍须完成M67原冻结门的审计；M71诊断不会给失败版本增加晋升资格。没有新增正式DepthTrack Test、CDTB或VOT指标，也没有新的独立模型审阅通过。

执行源码SHA：`ee8376769bbc827813e60324baae1d688241c1818cc3a063094585a09ace5023`。
spec SHA：`35ff57b50096ec28a3e51bfc78efd2605faa853eaecda0644f3568c2bd6acd10`。
实际启动核验SHA：`d56b3fb7105dd144ffcfc4d48af6c6b00019f9fe8cb413f556af9d4c986b0020`。
