# M77 single-seed evaluation preparation

Prepared while the two seed2027 training arms and their existing deferred content controller were still running. Only evaluation bindings changed: the frozen model inference source set and all five inherited interface files are unchanged. No training process, loss, promotion gate, checkpoint selection or additional seed was introduced by this preparation.

CPU checks used the real banks: 152 Train initialization observations, 22 development routes and all303 existing low22 observations matched exactly. The category transform reproduced the prepared low22 bank without changing its source. A premature full-pipeline call was expected to fail because the final activation record did not yet exist; its saved traceback confirms rejection before bundle creation, public evaluation files or CUDA initialization. This is an interface prerequisite check, not a benchmark failure.

The prospectively fixed Category final must pass all15 development conditions and all8 existing same-head content comparisons before binding. Real OPE and TraX checks on three202-frame Train prefixes come next, then all303 low22 anchors. The inherited low22 gate is unchanged: EAO >=57.635993, ACC >=75.719622, ROB >=73.022401, at most118 failures, with all seven native zero-failure sequences preserved.

Full sources cover DepthTrack Test50/76373 frames, CDTB80/101956 frames and VOT127/1765 anchors. A single bundle and text protocol bind every dataset. Only after a low22 pass may the missing130 OPE first-frame and1462 VOT initialization caption cases be generated. Existing low22 inputs and predictions can be reused only for the identical bundle. The final verifier checks saved outputs and official metrics against the retained project targets.

These files are flattened evidence copies of the server interface and wrappers; the live paths are bound in spec.json. Small banks remain on the server and are referenced by hash. Real learned-entry parity, public tracking, final metrics and an independent model-review PASS have not occurred. No additional automatic evaluation controller was started. See EXPERIMENT_TRACKER.md and master section5.146.
